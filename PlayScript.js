(() => { "use strict"; const PSNow = () => Date.now(); const PSLog = (...a) => console.log("[PlayScript]", ...a); const PSErr = (...a) => console.error("[PlayScript]", ...a); const PS_SAVE_DEBOUNCE_MS = 400; const PS_VERSION = "1.8.0"; let PSLastOutfitBlocked = []; let PSLastOutfitCleared = []; const PS_MAX_SCRIPTS = 50; const PS_MAX_NODES = 100; const PS_MAX_TEXT = 2000; const PS_MAX_CODE = 200000; const PS_MAX_CLOUD = 400000; const PS_CLOUD_TOTAL_LIMIT = 180000; const PS_CLOUD_DEFAULT_CAP_KB = 50; const PS_BIO_MARK = "[PlayScript:1]"; const PS_BIO_MAX = 10000; const PS_BIO_START = "━━━━━━【BCPS·PlayScript 数据，请勿修改】━━━━━━"; const PS_BIO_END = "━━━━━━【BCPS·PlayScript 数据结束】━━━━━━"; const PS_MAX_DELAY = 3600; const PS_MAX_KEYWORD = 64; const PS_QUEUE_CAP = 8; const PS_MAX_INTERACT_ACTIONS = 200; const PS_MIN_WIN_W = 720, PS_MIN_WIN_H = 480; const PSStorage = { lastError: null, get(key) { try { return localStorage.getItem(key); } catch (e) { return null; } }, set(key, value) { try { localStorage.setItem(key, value); return true; } catch (e) { this.lastError = e; return false; } }, remove(key) { try { localStorage.removeItem(key); } catch (e) { } }, }; const PS_OBF_KEY = "PlayScript#R131#BondageClub#2026"; function PSObfuscate(str) { const key = PS_OBF_KEY; const utf8 = []; for (let i = 0; i < str.length; i++) { const c = str.charCodeAt(i); if (c < 0x80) utf8.push(c); else if (c < 0x800) utf8.push(0xC0 | (c >> 6), 0x80 | (c & 63)); else if (c < 0x10000) utf8.push(0xE0 | (c >> 12), 0x80 | ((c >> 6) & 63), 0x80 | (c & 63)); else utf8.push(0xF0 | (c >> 18), 0x80 | ((c >> 12) & 63), 0x80 | ((c >> 6) & 63), 0x80 | (c & 63)); } const bytes = []; for (let i = 0; i < utf8.length; i++) { bytes.push((utf8[i] ^ key.charCodeAt(i % key.length) ^ ((i * 17 + 11) & 0xFF)) & 0xFF); } let bin = ""; for (const b of bytes) bin += String.fromCharCode(b); if (typeof btoa === "function") return btoa(bin); return Buffer.from(bin, "binary").toString("base64"); } function PSDeobfuscate(str) { let bin = ""; if (typeof atob === "function") bin = atob(str); else bin = Buffer.from(str, "base64").toString("binary"); const key = PS_OBF_KEY; const bytes = []; for (let i = 0; i < bin.length; i++) { bytes.push((bin.charCodeAt(i) ^ key.charCodeAt(i % key.length) ^ ((i * 17 + 11) & 0xFF)) & 0xFF); } let out = ""; for (let i = 0; i < bytes.length; i++) { const b = bytes[i]; if (b < 0x80) out += String.fromCharCode(b); else if ((b & 0xE0) === 0xC0) out += String.fromCharCode(((b & 31) << 6) | (bytes[++i] & 63)); else if ((b & 0xF0) === 0xE0) out += String.fromCharCode(((b & 15) << 12) | ((bytes[++i] & 63) << 6) | (bytes[++i] & 63)); else out += String.fromCharCode(((b & 7) << 18) | ((bytes[++i] & 63) << 12) | ((bytes[++i] & 63) << 6) | (bytes[++i] & 63)); } return out; } function PSStorageInfo() { const out = { total: 0, keys: [], top: [], self: null }; try { for (let i = 0; i < localStorage.length; i++) { const k = localStorage.key(i); if (!k) continue; let bytes = 0; try { const v = localStorage.getItem(k); if (v != null) bytes = v.length * 2; } catch (e) { } out.total += bytes; out.keys.push({ key: k, bytes }); } out.keys.sort((a, b) => b.bytes - a.bytes); out.top = out.keys.slice(0, 12); } catch (e) { } const mine = PSStore.baseKey(); out.self = out.keys.find((x) => x.key === mine) || { key: mine, bytes: 0 }; return out; } function PSCleanLSCGBackups() { let n = 0; try { for (let i = localStorage.length - 1; i >= 0; i--) { const k = localStorage.key(i); if (k && /^LSCG_.*_Backup$/.test(k)) { try { localStorage.removeItem(k); n++; } catch (e) { } } } } catch (e) { } return n; } const PSClamp = (v, lo, hi) => Math.min(Math.max(v, lo), hi); const PSDeepClone = (o) => JSON.parse(JSON.stringify(o)); const PSEsc = (s) => String(s ?? "").replace(/[&<>\u0022\u0027]/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;", }[c])); let PSUidCounter = 0; const PSUid = (p) => (p || "x") + (++PSUidCounter) + "_" + Date.now().toString(36); const PSTypeMeta = { chat: { icon: "", color: "#4ea1ff" }, rp: { icon: "", color: "#ff9f43" }, narr: { icon: "", color: "#a29bfe" }, action: { icon: "", color: "#ffd166" }, leave: { icon: "", color: "#f08080" }, outfit: { icon: "👗", color: "#9fd8a0" }, judge: { icon: "🎲", color: "#f0b3ff" }, }; const PSTypeNames = ["chat", "rp", "narr", "action", "leave", "outfit", "judge"]; const PSStore = { legacyKey: "PlayScriptData", cloudKey: "PlayScriptData", accountNum: null, state: null, saveTimer: null, lastWarnAt: null, lastCloudWarnAt: null, lastBioWarnAt: null, lastBioHoldWarnAt: null, lastAccountSnapshot: null, lastLoginData: null, baseKey() { return this.accountNum ? ("PlayScriptData:" + this.accountNum) : null; }, emptyState() { return { v: 1, scripts: [], ui: { win: null, dotPos: null } }; }, normalize(s) { if (!s || typeof s !== "object") s = this.emptyState(); s.v = 1; if (!Array.isArray(s.scripts)) s.scripts = []; s.scripts = s.scripts.filter((x) => x && typeof x === "object" && typeof x.id === "string") .map((sc) => { sc.name = String(sc.name ?? "").slice(0, 64); if (!sc.name) sc.name = "未命名剧本"; sc.keyword = String(sc.keyword ?? "").slice(0, PS_MAX_KEYWORD); sc.matchMode = sc.matchMode === "exact" ? "exact" : "contains"; sc.caseSensitive = !!sc.caseSensitive; sc.triggerWho = ["self", "anyone", "others"].includes(sc.triggerWho) ? sc.triggerWho : "self"; sc.cooldown = PSClamp(Number(sc.cooldown) || 0, 0, PS_MAX_DELAY); sc.onRetrigger = ["ignore", "restart", "queue"].includes(sc.onRetrigger) ? sc.onRetrigger : "ignore"; sc.enabled = sc.enabled !== false; sc.actionEnabled = !!sc.actionEnabled; sc.actionName = String(sc.actionName ?? "").slice(0, 64); sc.actionGroup = PSActBaseGroup(String(sc.actionGroup ?? "any").slice(0, 64)) || "any"; sc.actionTarget = sc.actionTarget === "self" ? "self" : "others"; sc.roomTrigger = !!sc.roomTrigger; sc.roomTriggerOnReconnect = sc.roomTriggerOnReconnect !== false; sc.playerTrigger = !!sc.playerTrigger; const legacyTarget = Number(sc.playerTarget) || 0; sc.playerTargets = Array.isArray(sc.playerTargets) ? sc.playerTargets.filter((n) => Number.isInteger(n) && n > 0).slice(0, 50) : (legacyTarget > 0 ? [legacyTarget] : []); sc.playerTarget = sc.playerTargets[0] || 0; sc.timeTrigger = !!sc.timeTrigger; sc.timeRules = PSNormalizeTimeTriggerRules(sc.timeRules); sc.orgasmTrigger = !!sc.orgasmTrigger; sc.orgasmDenyTrigger = !!sc.orgasmDenyTrigger; sc.interactTrigger = !!sc.interactTrigger; sc.interactActions = PSInteractNormalizeActions(sc.interactActions); sc.cloudBio = !!sc.cloudBio; sc.nodes = Array.isArray(sc.nodes) ? sc.nodes.filter((n) => n && typeof n.id === "string").map((n) => { const ntype = PSTypeNames.includes(n.type) ? n.type : "chat"; return { id: n.id, type: ntype, text: String(n.text ?? "").slice(0, PSNodeTextCap(ntype)), delay: PSClamp(Number(n.delay) || 0, 0, PS_MAX_DELAY), enabled: n.enabled !== false, force: !!n.force, outfitTarget: n.outfitTarget === "self" ? "self" : "target", outfitClothes: PSOutfitFlagsFromNode(n).outfitClothes, outfitBody: PSOutfitFlagsFromNode(n).outfitBody, outfitItems: PSOutfitFlagsFromNode(n).outfitItems, outfitCos: PSOutfitFlagsFromNode(n).outfitCos, outfitLocks: !!n.outfitLocks, outfitGroups: Array.isArray(n.outfitGroups) ? n.outfitGroups.filter((g) => typeof g === "string") : null, timeRules: PSNormalizeTimeRules(n.timeRules), nextId: (typeof n.nextId === "string" && n.nextId) ? n.nextId : null, yesId: (typeof n.yesId === "string" && n.yesId) ? n.yesId : null, noId: (typeof n.noId === "string" && n.noId) ? n.noId : null, judgeType: PSNormalizeJudgeType(n.judgeType), coinYesIs: n.coinYesIs === "tails" ? "tails" : "heads", diceExpr: PSValidDiceExpr(n.diceExpr) ? String(n.diceExpr).trim() : "1d6", diceRule: n.diceRule === "lte" ? "lte" : "gte", diceThreshold: PSClamp(Number(n.diceThreshold) || 4, 1, 100000), showResult: n.showResult !== false, judgeLineType: ["chat", "rp", "narr", "action"].includes(n.judgeLineType) ? n.judgeLineType : "chat", diceBranches: PSNormalizeDiceBranches(n.diceBranches), playerIds: PSNormalizePlayerIds(n.playerIds), playerBranches: PSNormalizePlayerBranches(n.playerBranches), relationBranches: PSNormalizeRelationBranches(n.relationBranches), elseId: (typeof n.elseId === "string" && n.elseId) ? n.elseId : null, }; }) : []; return sc; }); const ui = s.ui && typeof s.ui === "object" ? s.ui : {}; s.ui = { win: (ui.win && typeof ui.win === "object") ? ui.win : null, dotPos: (ui.dotPos && typeof ui.dotPos === "object") ? ui.dotPos : null }; const capN = Number(s.cloudCapKB); s.cloudCapKB = (isFinite(capN) && capN >= 0) ? capN : PS_CLOUD_DEFAULT_CAP_KB; return s; }, load() { const key = this.baseKey(); this.state = this.normalize(this.emptyState()); if (!key) { if (!this.state.scripts.length) this.state.scripts = [PSMakeDemoScript()]; return this.state; } let raw = PSStorage.get(key); if (raw === null) { const legacy = PSStorage.get(this.legacyKey); if (legacy !== null) { raw = legacy; PSStorage.remove(this.legacyKey); } } if (raw !== null) { try { this.state = this.normalize(this.decode(raw)); } catch (e) { PSErr("数据解析失败，已重置", e); this.state = this.normalize(this.emptyState()); } } if (!this.state.scripts.length) this.state.scripts = [PSMakeDemoScript()]; return this.state; }, encode(state) { const json = JSON.stringify(state); try { if (typeof LZString !== "undefined" && LZString && typeof LZString.compressToBase64 === "function") { return "p:" + PSObfuscate(LZString.compressToBase64(json)); } } catch (e) { } return json; }, decode(raw) { let text = raw; if (typeof raw === "string" && raw.indexOf("p:") === 0) { try { const dec = PSDeobfuscate(raw.slice(2)); if (typeof LZString !== "undefined" && LZString && typeof LZString.decompressFromBase64 === "function") { const d = LZString.decompressFromBase64(dec); if (typeof d === "string" && d) text = d; } } catch (e) { } } else if (typeof raw === "string" && raw.indexOf("z:") === 0) { try { if (typeof LZString !== "undefined" && LZString && typeof LZString.decompressFromBase64 === "function") { const d = LZString.decompressFromBase64(raw.slice(2)); if (typeof d === "string" && d) text = d; } } catch (e) { } } return JSON.parse(text); }, ensureAccount(num, cloudState) { if (!Number.isInteger(num) || num <= 0) return; if (this.accountNum !== num) { const prevUnbound = (this.accountNum === null && this.state && Array.isArray(this.state.scripts)) ? this.state.scripts.filter((s) => s && s.id) : null; const demo = PSMakeDemoScript(); const userScripts = prevUnbound ? prevUnbound.filter((s) => s.id !== "demo1" || JSON.stringify(s) !== JSON.stringify(demo)) : null; if (this.saveTimer) { clearTimeout(this.saveTimer); this.saveTimer = null; } this.accountNum = num; this.load(); if (cloudState) { this.state = this.normalize(cloudState); const key = this.baseKey(); if (key) PSStorage.set(key, this.encode(this.state)); } if (userScripts && userScripts.length) { const byId = {}; this.state.scripts.forEach((s) => { byId[s.id] = s; }); for (const s of userScripts) { if (byId[s.id]) Object.assign(byId[s.id], s); else if (this.state.scripts.length < PS_MAX_SCRIPTS) this.state.scripts.push(s); } } this.mergeBio(num); this.save(); } }, mergeBio(num) { try { if (!Number.isInteger(num) || num <= 0 || !this.state) return 0; if (typeof Player === "undefined" || !Player) return 0; if (!Number.isInteger(Player.MemberNumber) || Player.MemberNumber !== num) return 0; if (typeof Player.Description !== "string") return 0; const bioScripts = PSBioUnpack(Player.Description); if (!bioScripts || !bioScripts.length) { if (Player.Description.indexOf(PS_BIO_START) >= 0) { PSErr("BIO 备份：玩家描述里有 BCPS 数据块但解析失败（可能是旧版本数据、块被截断或被改动）"); } return 0; } const norm = this.normalize({ v: 1, scripts: bioScripts }).scripts; const byId = {}; this.state.scripts.forEach((s) => { byId[s.id] = s; }); let merged = 0; for (const s of norm) { const existing = byId[s.id]; if (existing && existing.cloudBio === false) { continue; } s.cloudBio = true; if (existing) { Object.assign(existing, s); merged++; } else if (this.state.scripts.length < PS_MAX_SCRIPTS) { this.state.scripts.push(s); merged++; } } return merged; } catch (e) { return 0; } }, requestSave() { if (this.saveTimer) return; this.saveTimer = setTimeout(() => { this.saveTimer = null; this.save(); }, PS_SAVE_DEBOUNCE_MS); }, save() { if (!this.state) return false; const key = this.baseKey(); if (!key) return false; const normalScripts = this.state.scripts.filter((s) => !s.cloudBio); const payload = this.encode({ v: 1, scripts: normalScripts, ui: this.state.ui, cloudCapKB: this.state.cloudCapKB }); const ok = PSStorage.set(key, payload); if (!ok) this.warnQuota(); const cloudAccepted = this.saveCloud(payload); this.saveBio(cloudAccepted); return ok; }, saveCloud(payload) { try { if (typeof ServerAccountUpdate === "undefined" || !ServerAccountUpdate || typeof ServerAccountUpdate.QueueData !== "function") return false; if (typeof payload !== "string") return false; const capKB = this.cloudCapKB(); if (capKB > 0 && PSUTF8Bytes(payload) > capKB * 1024) { this.warnCloudSize(payload, capKB); return false; } if (typeof Player === "undefined" || !Player || typeof Player.CharacterID !== "string" || Player.CharacterID === "") return false; const obj = {}; obj[this.cloudKey] = payload; ServerAccountUpdate.QueueData(obj); return true; } catch (e) { return false; } }, cloudCapKB() { const n = Number(this.state && this.state.cloudCapKB); return (isFinite(n) && n >= 0) ? n : PS_CLOUD_DEFAULT_CAP_KB; }, saveBio(cloudAccepted) { try { if (typeof Player === "undefined" || !Player) return; if (typeof ServerAccountUpdate === "undefined" || !ServerAccountUpdate || typeof ServerAccountUpdate.QueueData !== "function") return; if (typeof LZString === "undefined" || !LZString || typeof LZString.compressToUTF16 !== "function") return; const scripts = (this.state && Array.isArray(this.state.scripts)) ? this.state.scripts.filter((s) => s && s.cloudBio) : []; const cur = (typeof Player.Description === "string") ? Player.Description : ""; const oldBio = PSBioUnpack(cur) || []; const removed = oldBio.filter((s) => s && s.id && !scripts.some((x) => x.id === s.id)); if (removed.length && !cloudAccepted) { for (const s of removed) scripts.push(s); this.warnBioUncheckHold(); } const pack = PSBioPack(scripts); const b = PSBioBlockBounds(cur); let next = null; if (pack) { const block = PS_BIO_START + "\n" + pack + "\n" + PS_BIO_END; if (b && b.packIdx >= 0 && b.endIdx >= 0) { const before = cur.slice(0, b.startIdx).trimEnd(); const after = cur.slice(b.endIdx + PS_BIO_END.length).trimStart(); next = (before ? before + "\n" : "") + block + (after ? "\n" + after : ""); } else if (b) { const base = cur.trimEnd(); next = (base ? base + "\n" : "") + block; } else { const mIdx = cur.indexOf(PS_BIO_MARK); let base = cur; if (mIdx >= 0) { const rest = cur.slice(mIdx + PS_BIO_MARK.length); const tok = rest.trim().split(/\s+/)[0] || ""; if (/^[A-Za-z0-9+/=]{8,}$/.test(tok)) { const tokIdx = rest.indexOf(tok); const beforePart = cur.slice(0, mIdx).trimEnd(); const afterPart = (tokIdx >= 0 ? rest.slice(tokIdx + tok.length) : rest).trimStart(); base = beforePart + (beforePart && afterPart ? "\n" : "") + afterPart; } } const trimmed = base.trimEnd(); next = (trimmed ? trimmed + "\n" : "") + block; } } else if (cloudAccepted) { if (b && b.packIdx >= 0 && b.endIdx >= 0) { const before = cur.slice(0, b.startIdx).trimEnd(); const after = cur.slice(b.endIdx + PS_BIO_END.length).trimStart(); next = before + (before && after ? "\n" : "") + after; } else { const mIdx = cur.indexOf(PS_BIO_MARK); if (mIdx < 0) return; const rest = cur.slice(mIdx + PS_BIO_MARK.length); const tok = rest.trim().split(/\s+/)[0] || ""; if (!/^[A-Za-z0-9+/=]{8,}$/.test(tok)) return; const tokIdx = rest.indexOf(tok); const beforePart = cur.slice(0, mIdx).trimEnd(); const afterPart = (tokIdx >= 0 ? rest.slice(tokIdx + tok.length) : rest).trimStart(); next = beforePart + (beforePart && afterPart ? "\n" : "") + afterPart; } } if (next == null) return; if (next.length > PS_BIO_MAX) { this.warnBioTooBig(); return; } try { if (typeof Player.CharacterID === "string" && Player.CharacterID !== "") { Player.Description = next; } } catch (e) { } const comp = String.fromCharCode(9580) + LZString.compressToUTF16(next); const payload = (comp.length < next.length || next.startsWith(String.fromCharCode(9580))) ? comp : next; ServerAccountUpdate.QueueData({ Description: payload }); } catch (e) { } }, warnBioTooBig() { const now = PSNow(); if (this.lastBioWarnAt && now - this.lastBioWarnAt < 30000) return; this.lastBioWarnAt = now; try { PSToast(PST("bioTooBig")); } catch (e) { } }, warnBioUncheckHold() { const now = PSNow(); if (this.lastBioHoldWarnAt && now - this.lastBioHoldWarnAt < 30000) return; this.lastBioHoldWarnAt = now; try { PSToast(PST("bioUncheckHold")); } catch (e) { } }, warnCloudSize(payload, capKB) { const now = PSNow(); if (this.lastCloudWarnAt && now - this.lastCloudWarnAt < 30000) return; this.lastCloudWarnAt = now; try { PSToast(PST("cloudTooBig", PSByteToKB(PSUTF8Bytes(payload)), capKB)); } catch (e) { } }, warnQuota() { const now = PSNow(); if (this.lastWarnAt && now - this.lastWarnAt < 15000) return; this.lastWarnAt = now; try { PSErr("保存失败（localStorage 配额）", PSStorage.lastError); } catch (e) { } try { PSToast(PST("saveQuota")); } catch (e) { } }, }; function PSBioPack(scripts) { try { if (!scripts || !scripts.length) return ""; if (typeof LZString === "undefined" || !LZString || typeof LZString.compressToUTF16 !== "function") return ""; const json = JSON.stringify({ v: 1, scripts }); return PS_BIO_MARK + PSObfuscate(LZString.compressToUTF16(json)); } catch (e) { return ""; } } function PSBioBlockBounds(desc) { try { if (typeof desc !== "string") return null; const startIdx = desc.indexOf(PS_BIO_START); if (startIdx < 0) return null; const rest = desc.slice(startIdx + PS_BIO_START.length); let skip = 0; if (rest.startsWith("\r\n")) skip = 2; else if (rest.startsWith("\n")) skip = 1; else if (rest.startsWith("\\r\\n")) skip = 4; else if (rest.startsWith("\\n")) skip = 2; if (!skip) return { startIdx, packIdx: -1, endIdx: -1 }; const packIdx = startIdx + PS_BIO_START.length + skip; const endIdx = desc.indexOf(PS_BIO_END, packIdx); return { startIdx, packIdx, endIdx }; } catch (e) { return null; } } function PSBioUnpack(desc) { try { if (typeof desc !== "string") return null; let section = null; const b = PSBioBlockBounds(desc); if (b && b.packIdx >= 0 && b.endIdx >= 0) { section = desc.slice(b.packIdx, b.endIdx); } if (section == null) { const idx = desc.indexOf(PS_BIO_MARK); if (idx < 0) return null; section = desc.slice(idx); } const mIdx = section.indexOf(PS_BIO_MARK); if (mIdx < 0) return null; let encoded = section.slice(mIdx + PS_BIO_MARK.length).trim().split(/\s+/)[0] || ""; while (encoded.startsWith("\\n")) encoded = encoded.slice(2); while (encoded.endsWith("\\n")) encoded = encoded.slice(0, -2); if (!encoded) return null; const dec = PSDeobfuscate(encoded); if (typeof LZString === "undefined" || !LZString || typeof LZString.decompressFromUTF16 !== "function") return null; const json = LZString.decompressFromUTF16(dec); if (!json) return null; const obj = JSON.parse(json); if (!obj || !Array.isArray(obj.scripts)) return null; return obj.scripts; } catch (e) { return null; } } function PSUTF8Bytes(str) { let n = 0; const s = String(str); for (let i = 0; i < s.length; i++) { const c = s.charCodeAt(i); if (c < 0x80) n += 1; else if (c < 0x800) n += 2; else if (c < 0xD800 || c >= 0xE000) n += 3; else n += 4; } return n; } function PSMeasureDataSize(data) { const record = {}; const entries = []; const src = (data && typeof data === "object") ? data : {}; for (const key of Object.keys(src)) { let bytes = NaN; try { bytes = PSUTF8Bytes(JSON.stringify(src[key] ?? "")); } catch (e) { } entries.push([key, bytes]); } entries.sort((a, b) => { const na = Math.round(a[1] / 100); const nb = Math.round(b[1] / 100); if (na === nb || (isNaN(na) && isNaN(nb))) return String(a[0]).localeCompare(String(b[0])); if (isNaN(na) || isNaN(nb)) return isNaN(nb) ? -1 : 1; return nb - na; }); for (const [k, v] of entries) record[k] = v; return record; } function PSByteToKB(nByte) { return Math.round(nByte / 100) / 10; } function PSCloudSelfBytes() { try { if (!PSStore.state) return 0; const normalScripts = PSStore.state.scripts.filter((s) => !s.cloudBio); const payload = PSStore.encode({ v: 1, scripts: normalScripts, ui: PSStore.state.ui, cloudCapKB: PSStore.state.cloudCapKB }); return PSUTF8Bytes(payload); } catch (e) { return 0; } } function PSCloudLimit() { try { if (typeof ServerSocket !== "undefined" && ServerSocket && ServerSocket.io) { const eng = ServerSocket.io.engine; if (eng) { const raw = eng._maxPayload ?? (eng.opts && eng.opts.maxPayload) ?? (eng.transport && eng.transport.opts && eng.transport.opts.maxPayload); const n = Number(raw); if (isFinite(n) && n > 0) return n; } } } catch (e) { } return PS_CLOUD_TOTAL_LIMIT; } function PSCloudInfo() { let snap = PSStore.lastAccountSnapshot; if (!snap && PSStore.lastLoginData) { try { const keys = PSMeasureDataSize(PSStore.lastLoginData); let total = 0, selfBytes = 0; for (const k of Object.keys(keys)) { const b = keys[k]; if (!isNaN(b)) { total += b; if (k === PSStore.cloudKey) selfBytes = b; } } snap = PSStore.lastAccountSnapshot = { total, self: selfBytes, keys }; } catch (e) { snap = null; } } if (!snap) snap = { total: null, self: null, keys: null }; const limit = PSCloudLimit(); let extensionChars = 0, extensionBytes = 0; try { const es = (typeof Player !== "undefined" && Player && typeof Player.ExtensionSettings === "object" && Player.ExtensionSettings) ? Player.ExtensionSettings : {}; const json = JSON.stringify(es); extensionChars = json.length; extensionBytes = PSUTF8Bytes(json); } catch (e) { } return { limit, used: snap.total, available: (snap.total == null) ? null : Math.max(0, limit - snap.total), self: (snap.self != null) ? snap.self : PSCloudSelfBytes(), capKB: PSStore.cloudCapKB(), keys: snap.keys, extensionChars, extensionBytes, }; } function PSMakeDemoScript() { return { id: "demo1", name: "示例：苹果", keyword: "苹果", matchMode: "contains", caseSensitive: false, triggerWho: "self", cooldown: 10, onRetrigger: "ignore", enabled: true, actionEnabled: false, actionName: "", actionGroup: "any", actionTarget: "others", roomTrigger: false, roomTriggerOnReconnect: true, playerTrigger: false, playerTarget: 0, playerTargets: [], timeTrigger: false, timeRules: [], orgasmTrigger: false, orgasmDenyTrigger: false, interactTrigger: false, interactActions: [], cloudBio: false, nodes: [ { id: "demo1n1", type: "rp", text: "从怀里掏出一个红苹果，在手里抛了抛", delay: 0.5, enabled: true }, { id: "demo1n2", type: "narr", text: "一阵清甜的果香在空气中弥漫开来", delay: 2, enabled: true }, { id: "demo1n3", type: "chat", text: "这苹果可甜了，要尝尝吗？", delay: 1, enabled: true }, { id: "demo1n4", type: "action", text: "<nick>咔嚓咬了一口苹果，汁水四溅", delay: 1.5, enabled: true }, ], }; } function PSFindScript(id) { if (!PSStore.state) return null; return PSStore.state.scripts.find((s) => s.id === id) || null; } function PSAddScript(name, keyword) { if (PSStore.state.scripts.length >= PS_MAX_SCRIPTS) { PSToast(PST("toastMaxScripts", PS_MAX_SCRIPTS)); return null; } const s = { id: PSUid("s"), name: String(name ?? "").slice(0, 64) || "未命名剧本", keyword: String(keyword ?? "").slice(0, PS_MAX_KEYWORD), matchMode: "contains", caseSensitive: false, triggerWho: "self", cooldown: 10, onRetrigger: "ignore", enabled: true, actionEnabled: false, actionName: "", actionGroup: "any", actionTarget: "others", roomTrigger: false, roomTriggerOnReconnect: true, playerTrigger: false, playerTarget: 0, playerTargets: [], timeTrigger: false, timeRules: [], orgasmTrigger: false, orgasmDenyTrigger: false, interactTrigger: false, interactActions: [], cloudBio: false, nodes: [], }; PSStore.state.scripts.push(s); PSStore.requestSave(); PSToast(PST("toastScriptAdded")); return s; } function PSDeleteScript(id) { const before = PSStore.state.scripts.length; PSStore.state.scripts = PSStore.state.scripts.filter((s) => s.id !== id); if (PSStore.state.scripts.length === before) return false; if (PSUI.selScriptId === id) { PSUI.selScriptId = null; PSUI.selNodeId = null; } PSStore.requestSave(); PSToast(PST("toastScriptDeleted")); return true; } function PSDuplicateScript(id) { const sc = PSFindScript(id); if (!sc) return null; if (PSStore.state.scripts.length >= PS_MAX_SCRIPTS) { PSToast(PST("toastMaxScripts", PS_MAX_SCRIPTS)); return null; } const copy = PSDeepClone(sc); copy.id = PSUid("s"); copy.name = (String(sc.name).slice(0, 50) + " 副本").slice(0, 64); copy.nodes.forEach((n) => { n.id = PSUid("n"); }); const idx = PSStore.state.scripts.indexOf(sc); PSStore.state.scripts.splice(idx + 1, 0, copy); PSStore.requestSave(); PSToast(PST("toastDup")); return copy; } function PSUpdateScript(id, patch) { const sc = PSFindScript(id); if (!sc || !patch || typeof patch !== "object") return false; if ("name" in patch) sc.name = String(patch.name ?? "").slice(0, 64) || "未命名剧本"; if ("keyword" in patch) sc.keyword = String(patch.keyword ?? "").slice(0, PS_MAX_KEYWORD); if ("matchMode" in patch) sc.matchMode = patch.matchMode === "exact" ? "exact" : "contains"; if ("caseSensitive" in patch) sc.caseSensitive = !!patch.caseSensitive; if ("triggerWho" in patch) sc.triggerWho = ["self", "anyone", "others"].includes(patch.triggerWho) ? patch.triggerWho : "self"; if ("cooldown" in patch) sc.cooldown = PSClamp(Number(patch.cooldown) || 0, 0, PS_MAX_DELAY); if ("onRetrigger" in patch) sc.onRetrigger = ["ignore", "restart", "queue"].includes(patch.onRetrigger) ? patch.onRetrigger : "ignore"; if ("enabled" in patch) sc.enabled = patch.enabled !== false; if ("actionEnabled" in patch) sc.actionEnabled = !!patch.actionEnabled; if ("actionName" in patch) sc.actionName = String(patch.actionName ?? "").slice(0, 64); if ("actionGroup" in patch) sc.actionGroup = PSActBaseGroup(String(patch.actionGroup ?? "any").slice(0, 64)) || "any"; if ("actionTarget" in patch) sc.actionTarget = patch.actionTarget === "self" ? "self" : "others"; if ("roomTrigger" in patch) sc.roomTrigger = !!patch.roomTrigger; if ("roomTriggerOnReconnect" in patch) sc.roomTriggerOnReconnect = !!patch.roomTriggerOnReconnect; if ("playerTrigger" in patch) sc.playerTrigger = !!patch.playerTrigger; if ("playerTarget" in patch) { const n = Number(patch.playerTarget) || 0; sc.playerTargets = (n > 0) ? [n] : []; sc.playerTarget = sc.playerTargets[0] || 0; } if ("playerTargets" in patch) { sc.playerTargets = Array.isArray(patch.playerTargets) ? patch.playerTargets.map((n) => Number(n)).filter((n) => Number.isInteger(n) && n > 0).slice(0, 50) : []; sc.playerTarget = sc.playerTargets[0] || 0; } if ("timeTrigger" in patch) sc.timeTrigger = !!patch.timeTrigger; if ("timeRules" in patch) sc.timeRules = PSNormalizeTimeTriggerRules(patch.timeRules); if ("orgasmTrigger" in patch) sc.orgasmTrigger = !!patch.orgasmTrigger; if ("orgasmDenyTrigger" in patch) sc.orgasmDenyTrigger = !!patch.orgasmDenyTrigger; if ("interactTrigger" in patch) sc.interactTrigger = !!patch.interactTrigger; if ("interactActions" in patch) sc.interactActions = PSInteractNormalizeActions(patch.interactActions); if ("cloudBio" in patch) sc.cloudBio = !!patch.cloudBio; PSStore.requestSave(); return true; } function PSAddNode(scriptId, node, atIdx) { const sc = PSFindScript(scriptId); if (!sc) return null; if (sc.nodes.length >= PS_MAX_NODES) { PSToast(PST("toastMaxNodes", PS_MAX_NODES)); return null; } const n = { id: PSUid("n"), type: PSTypeNames.includes(node && node.type) ? node.type : "chat", text: String((node && node.text) ?? "").slice(0, PSNodeTextCap(PSTypeNames.includes(node && node.type) ? node.type : "chat")), delay: PSClamp(Number((node && node.delay) || 0) || 0, 0, PS_MAX_DELAY), enabled: !node || node.enabled !== false, force: !!(node && node.force), outfitTarget: (node && node.outfitTarget === "self") ? "self" : "target", outfitClothes: PSOutfitFlagsFromNode(node).outfitClothes, outfitBody: PSOutfitFlagsFromNode(node).outfitBody, outfitItems: PSOutfitFlagsFromNode(node).outfitItems, outfitCos: PSOutfitFlagsFromNode(node).outfitCos, outfitLocks: !!(node && node.outfitLocks), outfitItemsMode: (node && node.outfitItemsMode === "override") ? "override" : "add", outfitGroups: (node && Array.isArray(node.outfitGroups)) ? node.outfitGroups.filter((g) => typeof g === "string") : null, timeRules: PSNormalizeTimeRules(node && node.timeRules), nextId: (node && typeof node.nextId === "string" && node.nextId) ? node.nextId : null, yesId: (node && typeof node.yesId === "string" && node.yesId) ? node.yesId : null, noId: (node && typeof node.noId === "string" && node.noId) ? node.noId : null, judgeType: PSNormalizeJudgeType(node && node.judgeType), coinYesIs: (node && node.coinYesIs === "tails") ? "tails" : "heads", diceExpr: PSValidDiceExpr(node && node.diceExpr) ? String(node.diceExpr).trim() : "1d6", diceRule: (node && node.diceRule === "lte") ? "lte" : "gte", diceThreshold: PSClamp(Number((node && node.diceThreshold) || 4), 1, 100000), showResult: !node || node.showResult !== false, judgeLineType: (node && ["chat", "rp", "narr", "action"].includes(node.judgeLineType)) ? node.judgeLineType : "chat", diceBranches: PSNormalizeDiceBranches(node && node.diceBranches), playerIds: PSNormalizePlayerIds(node && node.playerIds), playerBranches: PSNormalizePlayerBranches(node && node.playerBranches), relationBranches: PSNormalizeRelationBranches(node && node.relationBranches), elseId: (node && typeof node.elseId === "string" && node.elseId) ? node.elseId : null, }; if (atIdx === undefined || atIdx === null || atIdx < 0) sc.nodes.push(n); else sc.nodes.splice(PSClamp(atIdx, 0, sc.nodes.length), 0, n); PSStore.requestSave(); return n; } function PSDeleteNode(scriptId, nodeId) { const sc = PSFindScript(scriptId); if (!sc) return false; const node = sc.nodes.find((n) => n.id === nodeId); if (!node) return false; const sub = (node.type === "judge") ? PSJudgeBranchSubtree(sc, node) : new Set(); const before = sc.nodes.length; sc.nodes = sc.nodes.filter((n) => n.id !== nodeId && !sub.has(n.id)); if (sc.nodes.length === before) return false; sc.nodes.forEach((n) => { if (n.nextId && (n.nextId === nodeId || sub.has(n.nextId))) n.nextId = null; if (n.yesId && (n.yesId === nodeId || sub.has(n.yesId))) n.yesId = null; if (n.noId && (n.noId === nodeId || sub.has(n.noId))) n.noId = null; if (Array.isArray(n.diceBranches)) n.diceBranches = n.diceBranches.filter((b) => b && b.nodeId && b.nodeId !== nodeId && !sub.has(b.nodeId)); if (Array.isArray(n.playerBranches)) n.playerBranches = n.playerBranches.filter((b) => b && b.nodeId && b.nodeId !== nodeId && !sub.has(b.nodeId)); if (Array.isArray(n.relationBranches)) n.relationBranches = n.relationBranches.filter((b) => b && b.nodeId && b.nodeId !== nodeId && !sub.has(b.nodeId)); if (n.elseId && (n.elseId === nodeId || sub.has(n.elseId))) n.elseId = null; }); if (PSUI.selNodeId && (PSUI.selNodeId === nodeId || sub.has(PSUI.selNodeId))) PSUI.selNodeId = null; PSStore.requestSave(); return true; } function PSMoveNode(scriptId, nodeId, dir) { const sc = PSFindScript(scriptId); if (!sc) return -1; const from = sc.nodes.findIndex((n) => n.id === nodeId); if (from < 0) return -1; const to = PSClamp(from + dir, 0, sc.nodes.length - 1); if (to === from) return from; PSMoveNodeTo(sc, nodeId, to); return to; } function PSMoveNodeTo(sc, nodeId, toIdx) { const from = sc.nodes.findIndex((n) => n.id === nodeId); if (from < 0) return false; const node = sc.nodes[from]; sc.nodes.splice(from, 1); sc.nodes.splice(PSClamp(toIdx, 0, sc.nodes.length), 0, node); PSStore.requestSave(); return true; } function PSUpdateNode(scriptId, nodeId, patch) { const sc = PSFindScript(scriptId); if (!sc) return false; const n = sc.nodes.find((x) => x.id === nodeId); if (!n || !patch || typeof patch !== "object") return false; if ("type" in patch && PSTypeNames.includes(patch.type)) n.type = patch.type; if ("text" in patch) n.text = String(patch.text ?? "").slice(0, PSNodeTextCap(n.type)); if ("delay" in patch) n.delay = PSClamp(Number(patch.delay) || 0, 0, PS_MAX_DELAY); if ("enabled" in patch) n.enabled = patch.enabled !== false; if ("force" in patch) n.force = !!patch.force; if ("outfitTarget" in patch) n.outfitTarget = patch.outfitTarget === "self" ? "self" : "target"; if ("outfitMode" in patch) { const f = PSOutfitFlagsFromNode(patch); n.outfitClothes = f.outfitClothes; n.outfitBody = f.outfitBody; n.outfitItems = f.outfitItems; n.outfitCos = f.outfitCos; } if ("outfitClothes" in patch) n.outfitClothes = patch.outfitClothes !== false; if ("outfitBody" in patch) n.outfitBody = patch.outfitBody !== false; if ("outfitItems" in patch) n.outfitItems = patch.outfitItems !== false; if ("outfitCos" in patch) n.outfitCos = patch.outfitCos !== false; if ("outfitLocks" in patch) n.outfitLocks = !!patch.outfitLocks; if ("outfitItemsMode" in patch) n.outfitItemsMode = patch.outfitItemsMode === "override" ? "override" : "add"; if ("outfitGroups" in patch) n.outfitGroups = Array.isArray(patch.outfitGroups) ? patch.outfitGroups.filter((g) => typeof g === "string") : null; if ("timeRules" in patch) n.timeRules = PSNormalizeTimeRules(patch.timeRules); if ("nextId" in patch) n.nextId = (patch.nextId && typeof patch.nextId === "string") ? patch.nextId : null; if ("yesId" in patch) n.yesId = (patch.yesId && typeof patch.yesId === "string") ? patch.yesId : null; if ("noId" in patch) n.noId = (patch.noId && typeof patch.noId === "string") ? patch.noId : null; if ("judgeType" in patch) n.judgeType = PSNormalizeJudgeType(patch.judgeType); if ("coinYesIs" in patch) n.coinYesIs = patch.coinYesIs === "tails" ? "tails" : "heads"; if ("diceExpr" in patch) n.diceExpr = PSValidDiceExpr(patch.diceExpr) ? String(patch.diceExpr).trim() : (n.diceExpr || "1d6"); if ("diceRule" in patch) n.diceRule = patch.diceRule === "lte" ? "lte" : "gte"; if ("diceThreshold" in patch) n.diceThreshold = PSClamp(Number(patch.diceThreshold) || 4, 1, 100000); if ("showResult" in patch) n.showResult = patch.showResult !== false; if ("judgeLineType" in patch && ["chat", "rp", "narr", "action"].includes(patch.judgeLineType)) n.judgeLineType = patch.judgeLineType; if ("diceBranches" in patch) n.diceBranches = PSNormalizeDiceBranches(patch.diceBranches); if ("playerIds" in patch) n.playerIds = PSNormalizePlayerIds(patch.playerIds); if ("playerBranches" in patch) n.playerBranches = PSNormalizePlayerBranches(patch.playerBranches); if ("relationBranches" in patch) n.relationBranches = PSNormalizeRelationBranches(patch.relationBranches); if ("elseId" in patch) n.elseId = (patch.elseId && typeof patch.elseId === "string") ? patch.elseId : null; PSStore.requestSave(); return true; } const PSEscapeRegExp = (s) => String(s).replace(/[.*+?^${}()|[\]\\]/g, "\\$&"); const PS_WORD_CHAR = /[\w\u4e00-\u9fff\u3400-\u4dbf\u3040-\u30ff\uac00-\ud7af]/; function PSMatchInfo(text, sc) { if (!text || !sc || !sc.keyword) return null; const a = String(text).trim(); const kw = String(sc.keyword); if (!kw) return null; const hay = sc.caseSensitive ? a : a.toLowerCase(); const needle = sc.caseSensitive ? kw : kw.toLowerCase(); if (sc.matchMode === "exact") { return hay === needle ? { start: 0, end: hay.length } : null; } const re = new RegExp(PSEscapeRegExp(needle), "g"); let m; while ((m = re.exec(hay))) { const start = m.index; const end = start + needle.length; const before = start > 0 ? hay[start - 1] : ""; const after = end < hay.length ? hay[end] : ""; if (before && PS_WORD_CHAR.test(before)) continue; if (after && PS_WORD_CHAR.test(after)) continue; return { start, end }; } return null; } function PSMatchText(text, sc) { return !!PSMatchInfo(text, sc); } function PSTriggerTargetOf(text, sc, match) { const a = String(text); const left = a.slice(0, match.start); const right = a.slice(match.end); const joined = left + " " + right; const members = []; const push = (arr) => { if (!Array.isArray(arr)) return; for (const C of arr) { if (C && typeof C.MemberNumber === "number" && typeof C.Name === "string" && C.Name) members.push(C); } }; try { push(typeof ChatRoomCharacter !== "undefined" ? ChatRoomCharacter : null); } catch (e) { } try { push(typeof PrivateCharacter !== "undefined" ? PrivateCharacter : null); } catch (e) { } if (typeof Player !== "undefined" && Player && typeof Player.MemberNumber === "number" && typeof Player.Name === "string" && Player.Name) members.push(Player); if (!members.length) return null; const SEP = "[\\s,，。.!！?？、;；:：\"'（）()【】\\[\\]<>《》…~～|·•—－]"; const sepTrim = new RegExp("^" + SEP + "+|" + SEP + "+$", "g"); const full = joined.replace(sepTrim, "").trim(); const chunkRe = new RegExp("[^" + SEP + "]+", "g"); const chunks = []; for (const cm of joined.matchAll(chunkRe)) chunks.push(cm[0]); const eq = (x, y) => String(x).toLowerCase() === String(y).toLowerCase(); const matchOne = (candidate) => { if (!candidate) return null; for (const C of members) { if (eq(C.Name, candidate)) return { num: C.MemberNumber, name: C.Name }; } if (/^\d+$/.test(candidate)) { const num = Number(candidate); for (const C of members) { if (typeof C.MemberNumber === "number" && C.MemberNumber === num) return { num: C.MemberNumber, name: C.Name }; } } return null; }; let found = matchOne(full); if (!found) { for (const ch of chunks) { found = matchOne(ch); if (found) break; } } return found; } function PSTriggerTargetAttempt(text, sc, match) { const a = String(text); const left = a.slice(0, match.start); const right = a.slice(match.end); const SEP = "[\\s,，。.!！?？、;；:：\"'（）()【】\\[\\]<>《》…~～|·•—－]"; const sepTrim = new RegExp("^" + SEP + "+|" + SEP + "+$", "g"); const l = left.replace(sepTrim, "").trim(); const r = right.replace(sepTrim, "").trim(); if (!l && !r) return null; if (l && r) return null; return l || r; } function PSExportJSON() { if (!PSStore.state) return "{}"; return JSON.stringify({ app: "PlayScript", v: 1, exportedAt: PSNow(), scripts: PSStore.state.scripts, }); } function PSExportScriptJSON(id) { if (!PSStore.state) return null; const sc = PSStore.state.scripts.find((s) => s.id === id); if (!sc) return null; return JSON.stringify({ app: "PlayScript", v: 1, exportedAt: PSNow(), scripts: [sc], }); } function PSImportJSON(text) { let data = null; try { data = JSON.parse(String(text ?? "")); } catch (e) { return null; } if (!data || typeof data !== "object") return null; const arr = Array.isArray(data) ? data : (Array.isArray(data.scripts) ? data.scripts : (data.data && Array.isArray(data.data.scripts) ? data.data.scripts : null)); if (!arr) return null; const state = PSStore.state; const byId = {}; state.scripts.forEach((s) => { byId[s.id] = s; }); let added = 0, replaced = 0, bad = 0; for (const raw of arr) { if (!raw || typeof raw !== "object") { bad++; continue; } if (typeof raw.id !== "string") raw.id = PSUid("s"); const norm = PSStore.normalize({ v: 1, scripts: [raw] }).scripts[0]; if (!norm) { bad++; continue; } if (byId[norm.id]) { Object.assign(byId[norm.id], norm); replaced++; } else { if (state.scripts.length >= PS_MAX_SCRIPTS) { bad++; continue; } state.scripts.push(norm); byId[norm.id] = norm; added++; } } PSStore.requestSave(); return { added, replaced, bad }; } function PSClearAll() { PSQueue = []; if (PSActive) PSAbortCore(true, "clear"); PSStore.state.scripts = []; PSStore.requestSave(); PSToast(PST("toastCleared")); } let PSActive = null; let PSQueue = []; const PSLastFire = {}; let PSRoomRelogArmed = 0; let PSTimeScanTimer = null; function PSNormalizeText(t) { return String(t ?? "").replace(/\r\n?/g, "\n").trim().slice(0, PS_MAX_TEXT); } function PSNodeTextCap(type) { return type === "outfit" ? PS_MAX_CODE : PS_MAX_TEXT; } function PSNormalizeCode(t) { return String(t ?? "").replace(/\r\n?/g, "\n").trim().slice(0, PS_MAX_CODE); } const PS_NICK_TOKEN = "<nick>"; const PS_TARGET_TOKEN = "<target>"; const PS_TIME_TOKEN = "<time>"; const PS_ROLL_TOKEN = "<roll>"; function PSCharDisplayName(C) { if (!C) return null; try { if (typeof CharacterNickname === "function") { const n = CharacterNickname(C); if (n && String(n).trim()) return String(n); } } catch (e) { } if (C.Name && String(C.Name).trim()) return String(C.Name); return (C.MemberNumber !== undefined && C.MemberNumber !== null) ? ("#" + C.MemberNumber) : null; } function PSCharObjectOf(num) { if (typeof Player !== "undefined" && Player && Player.MemberNumber === num) return Player; if (typeof Character !== "undefined" && Array.isArray(Character)) { const c = Character.find((x) => x && x.MemberNumber === num); if (c) return c; } return null; } function PSCharIsSelf(C) { if (!C) return false; try { if (typeof C.IsPlayer === "function") return !!C.IsPlayer(); } catch (e) { } if (typeof Player !== "undefined" && Player && typeof C.MemberNumber === "number" && Player.MemberNumber === C.MemberNumber) return true; return false; } function PSTargetInRoom(num) { if (!Number.isInteger(num) || num <= 0) return false; try { if (typeof ChatRoomCharacter !== "undefined" && Array.isArray(ChatRoomCharacter)) { return ChatRoomCharacter.some((c) => c && c.MemberNumber === num); } } catch (e) { } return false; } function PSTriggerName(num) { let C = (num === null || num === undefined) ? ((typeof Player !== "undefined" && Player) ? Player : null) : PSCharObjectOf(num); if (!C && typeof Player !== "undefined" && Player) C = Player; const n = PSCharDisplayName(C); return n || "我"; } function PSApplyTokens(text, triggerNum, targetNum, timeRules, rollLabel) { let t = String(text ?? ""); t = t.split(PS_NICK_TOKEN).join(PSTriggerName(triggerNum)); t = t.split(PS_TARGET_TOKEN).join(PSTriggerName((targetNum === undefined || targetNum === null) ? triggerNum : targetNum)); t = t.split(PS_TIME_TOKEN).join(PSTimeRuleText(timeRules) ?? ""); const rl = (rollLabel === undefined || rollLabel === null) ? (PSActive && PSActive.lastJudge ? PSActive.lastJudge.label : "") : rollLabel; t = t.split(PS_ROLL_TOKEN).join(rl ?? ""); return t; } function PSTimeHMToMin(v) { const s = String(v ?? "").trim(); const m = /^(\d{1,2}):(\d{2})$/.exec(s); if (!m) return null; const h = Number(m[1]), mi = Number(m[2]); if (h > 24 || mi > 59 || (h === 24 && mi !== 0)) return null; return h * 60 + mi; } function PSTimeRuleMinutes(tz) { try { const z = String(tz ?? "").trim(); if (!z || z === "local") { const d = new Date(); return d.getHours() * 60 + d.getMinutes(); } const m = /^UTC([+-])(\d{1,2})(?::(\d{2}))?$/i.exec(z); if (m) { const off = (parseInt(m[2], 10) || 0) * 60 + (parseInt(m[3], 10) || 0); const d = new Date(); const utc = d.getUTCHours() * 60 + d.getUTCMinutes(); const sign = m[1] === "-" ? -1 : 1; return (((utc + sign * off) % 1440) + 1440) % 1440; } const fmt = new Intl.DateTimeFormat("en-US", { timeZone: z, hour: "2-digit", minute: "2-digit", hour12: false }); const parts = fmt.formatToParts(new Date()); let h = 0, mi = 0; for (const p of parts) { if (p.type === "hour") h = Number(p.value) % 24; else if (p.type === "minute") mi = Number(p.value); } return h * 60 + mi; } catch (e) { return null; } } function PSTimeRuleMatch(rules, nowMin) { if (!Array.isArray(rules)) return null; for (const r of rules) { if (!r) continue; const from = PSTimeHMToMin(r.from); const to = PSTimeHMToMin(r.to); if (from == null || to == null) continue; const now = (nowMin === undefined || nowMin === null) ? PSTimeRuleMinutes(r.tz) : nowMin; if (now == null) continue; const hit = from <= to ? (now >= from && now < to) : (now >= from || now < to); if (hit) return r; } return null; } function PSTimeRuleText(rules, nowMin) { const r = PSTimeRuleMatch(rules, nowMin); return (r && typeof r.text === "string" && r.text.trim()) ? r.text : null; } function PSNormalizeTimeRules(arr) { if (!Array.isArray(arr)) return []; const out = []; for (const r of arr) { if (!r || typeof r !== "object") continue; const from = PSTimeHMToMin(r.from); const to = PSTimeHMToMin(r.to); if (from == null || to == null) continue; out.push({ id: typeof r.id === "string" ? r.id : PSUid("tr"), from: String(r.from).trim(), to: String(r.to).trim(), tz: String(r.tz ?? "").trim().slice(0, 64), text: String(r.text ?? "").trim().slice(0, 200), }); } return out; } function PSNormalizeTimeTriggerRules(arr) { if (!Array.isArray(arr)) return []; const out = []; for (const r of arr) { if (!r || typeof r !== "object") continue; const from = PSTimeHMToMin(r.from); const to = PSTimeHMToMin(r.to); if (from == null || to == null) continue; out.push({ id: typeof r.id === "string" ? r.id : PSUid("tt"), from: String(r.from).trim(), to: String(r.to).trim(), tz: String(r.tz ?? "").trim().slice(0, 64), }); } return out; } function PSTimeTriggerScan() { try { if (!PSStore.state) return; if (!PSCanSend()) return; const me = (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber)) ? Player.MemberNumber : null; for (const sc of PSStore.state.scripts) { if (sc.enabled === false || !sc.timeTrigger) continue; if (!PSTimeRuleMatch(sc.timeRules)) continue; PSLog("时间触发：", sc.name); PSFire(sc.id, { senderNum: me }); } } catch (e) { PSErr(e); } } function PSValidDiceExpr(v) { const s = String(v ?? "").trim(); const m = /^(\d{1,3})[dD](\d{1,4})$/.exec(s); return !!m && Number(m[1]) >= 1 && Number(m[2]) >= 2; } function PSNormalizeJudgeType(v) { if (v === "dice") return "dice"; if (v === "player") return "player"; if (v === "relation") return "relation"; return "coin"; } function PSNormalizePlayerIds(arr) { if (!Array.isArray(arr)) return []; const out = []; for (const v of arr) { const n = Number(v); if (Number.isInteger(n) && n > 0 && !out.includes(n)) out.push(n); if (out.length >= 50) break; } return out; } function PSNormalizePlayerBranches(arr) { if (!Array.isArray(arr)) return []; const out = []; for (const b of arr) { if (!b || typeof b !== "object") continue; out.push({ id: typeof b.id === "string" ? b.id : PSUid("pb"), ids: PSNormalizePlayerIds(b.ids), nodeId: (typeof b.nodeId === "string" && b.nodeId) ? b.nodeId : null, }); if (out.length >= 5) break; } return out; } function PSPortTarget(node, port) { if (!node || !port) return null; if (port === "next") return node.nextId || null; if (port === "yes") return node.yesId || null; if (port === "no") return node.noId || null; if (port === "else") return node.elseId || null; const pm = /^player:(.+)$/.exec(String(port)); if (pm && Array.isArray(node.playerBranches)) { const b = node.playerBranches.find((x) => x.id === pm[1]); return (b && b.nodeId) ? b.nodeId : null; } const rm = /^relation:(.+)$/.exec(String(port)); if (rm && Array.isArray(node.relationBranches)) { const b = node.relationBranches.find((x) => x.id === rm[1]); return (b && b.nodeId) ? b.nodeId : null; } return null; } const PS_RELATION_ORDER = ["owner", "lover", "white", "friend", "none"]; function PSRelationLabel(rel) { const m = { owner: PST("relOwner"), lover: PST("relLover"), white: PST("relWhite"), friend: PST("relFriend"), none: PST("relNone") }; return m[rel] || rel; } function PSNormalizeRelationBranches(arr) { if (!Array.isArray(arr)) return []; const out = []; const seenRel = new Set(); for (const b of arr) { if (!b || typeof b !== "object") continue; const rel = PS_RELATION_ORDER.includes(b.rel) ? b.rel : null; if (!rel || seenRel.has(rel)) continue; seenRel.add(rel); out.push({ id: typeof b.id === "string" ? b.id : PSUid("rb"), rel, nodeId: (typeof b.nodeId === "string" && b.nodeId) ? b.nodeId : null, }); if (out.length >= 5) break; } return out; } function PSRelationOf(C) { try { if (C && typeof C.IsOwner === "function" && C.IsOwner()) return "owner"; } catch (e) {} try { if (C && typeof C.IsLoverOfPlayer === "function" && C.IsLoverOfPlayer()) return "lover"; } catch (e) {} try { if (typeof Player !== "undefined" && Player && typeof Player.HasOnWhitelist === "function" && Player.HasOnWhitelist(C)) return "white"; } catch (e) {} try { if (typeof Player !== "undefined" && Player && typeof Player.HasOnFriendlist === "function" && Player.HasOnFriendlist(C)) return "friend"; } catch (e) {} return "none"; } function PSJudgeRoll(node, targetNum) { try { if (node && node.judgeType === "player") { const me = (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber)) ? Player.MemberNumber : null; const t = (targetNum === undefined) ? (PSActive ? PSActive.targetNum : null) : targetNum; const other = (Number.isInteger(t) && t > 0 && t !== me) ? t : null; if (other) { const branches = PSNormalizePlayerBranches(node.playerBranches); const br = branches.find((b) => b.ids && b.ids.includes(other)); if (br) return { yes: false, label: "#" + other, targetId: br.nodeId || null }; if (node.elseId) return { yes: false, label: "#" + other, targetId: node.elseId }; } return { yes: false, label: other ? ("#" + other) : "无目标", targetId: null }; } if (node && node.judgeType === "relation") { const me = (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber)) ? Player.MemberNumber : null; const t = (targetNum === undefined) ? (PSActive ? PSActive.targetNum : null) : targetNum; const other = (Number.isInteger(t) && t > 0 && t !== me) ? t : null; if (!other) return { yes: false, label: "无目标", targetId: null }; const C = PSCharObjectOf(other) || null; const rel = PSRelationOf(C); const branches = PSNormalizeRelationBranches(node.relationBranches); const br = branches.find((b) => b.rel === rel) || branches.find((b) => b.rel === "none"); return { yes: false, label: PSRelationLabel(rel), targetId: br ? br.nodeId : null }; } if (node && node.judgeType === "dice") { const m = /^(\d{1,3})[dD](\d{1,4})$/.exec(String(node.diceExpr || "1d6")); const n = m ? Math.min(Number(m[1]), 100) : 1; const sides = m ? Math.min(Number(m[2]), 100000) : 6; let total = 0; for (let i = 0; i < n; i++) total += Math.floor(Math.random() * sides) + 1; if (Array.isArray(node.diceBranches) && node.diceBranches.length) { const br = node.diceBranches.find((b) => total >= b.from && total <= b.to); return { yes: false, label: String(total), targetId: br ? br.nodeId : null }; } const th = Math.min(100000, Math.max(1, Number(node.diceThreshold) || 4)); const yes = node.diceRule === "lte" ? (total <= th) : (total >= th); return { yes, label: String(total) }; } const heads = Math.random() < 0.5; const yes = (node && node.coinYesIs === "tails") ? !heads : heads; return { yes, label: heads ? "正" : "反" }; } catch (e) { return { yes: false, label: "?" }; } } function PSNormalizeDiceBranches(arr) { if (!Array.isArray(arr)) return []; const out = []; for (const b of arr) { if (!b || typeof b !== "object") continue; const from = Math.max(1, Math.min(100000, Number(b.from) || 1)); const to = Math.max(1, Math.min(100000, Number(b.to) || 1)); if (from > to) continue; out.push({ id: typeof b.id === "string" ? b.id : PSUid("db"), from, to, nodeId: (typeof b.nodeId === "string" && b.nodeId) ? b.nodeId : null, }); } return out.slice(0, 5); } function PSJudgeAddDiceBranch(scriptId, nodeId) { const sc = PSFindScript(scriptId); if (!sc) return null; const node = sc.nodes.find((n) => n.id === nodeId); if (!node || node.type !== "judge") return null; const branches = PSNormalizeDiceBranches(node.diceBranches); if (branches.length >= 5) { PSToast(PST("judgeBranchMax")); return null; } const last = branches[branches.length - 1]; const from = last ? last.to + 1 : 1; const to = from + 9; const n = PSAddNode(scriptId, { type: "chat", text: PST("judgeBranchPh", branches.length + 1), delay: 0, enabled: true }); if (!n) return null; const branch = { id: PSUid("db"), from, to, nodeId: n.id }; branches.push(branch); PSUpdateNode(scriptId, nodeId, { diceBranches: branches }); return branch; } function PSJudgeAddPlayerBranch(scriptId, nodeId) { const sc = PSFindScript(scriptId); if (!sc) return null; const node = sc.nodes.find((n) => n.id === nodeId); if (!node || node.type !== "judge") return null; const branches = PSNormalizePlayerBranches(node.playerBranches); if (branches.length >= 5) { PSToast(PST("judgeBranchMax")); return null; } const n = PSAddNode(scriptId, { type: "chat", text: PST("judgeBranchPh", branches.length + 1), delay: 0, enabled: true }); if (!n) return null; const branch = { id: PSUid("pb"), ids: [], nodeId: n.id }; branches.push(branch); PSUpdateNode(scriptId, nodeId, { playerBranches: branches }); return branch; } function PSJudgeAddRelationBranch(scriptId, nodeId) { const sc = PSFindScript(scriptId); if (!sc) return null; const node = sc.nodes.find((n) => n.id === nodeId); if (!node || node.type !== "judge") return null; const branches = PSNormalizeRelationBranches(node.relationBranches); if (branches.length >= 5) { PSToast(PST("judgeBranchMax")); return null; } const used = new Set(branches.map((b) => b.rel)); const rel = PS_RELATION_ORDER.find((r) => !used.has(r)) || "none"; const n = PSAddNode(scriptId, { type: "chat", text: PST("judgeBranchPh", branches.length + 1), delay: 0, enabled: true }); if (!n) return null; const branch = { id: PSUid("rb"), rel, nodeId: n.id }; branches.push(branch); PSUpdateNode(scriptId, nodeId, { relationBranches: branches }); return branch; } function PSJudgeBranchSubtree(sc, judge) { const byId = new Map(sc.nodes.map((n) => [n.id, n])); const out = new Set(); const stack = []; const push = (id) => { if (id && byId.has(id)) stack.push(id); }; if (judge.yesId) push(judge.yesId); if (judge.noId) push(judge.noId); if (Array.isArray(judge.diceBranches)) judge.diceBranches.forEach((b) => { if (b && b.nodeId) push(b.nodeId); }); if (Array.isArray(judge.playerBranches)) judge.playerBranches.forEach((b) => { if (b && b.nodeId) push(b.nodeId); }); if (Array.isArray(judge.relationBranches)) judge.relationBranches.forEach((b) => { if (b && b.nodeId) push(b.nodeId); }); if (judge.elseId) push(judge.elseId); while (stack.length) { const id = stack.pop(); if (out.has(id)) continue; const n = byId.get(id); if (!n) continue; out.add(id); if (n.nextId) push(n.nextId); if (n.type === "judge") { if (n.yesId) push(n.yesId); if (n.noId) push(n.noId); if (Array.isArray(n.diceBranches)) n.diceBranches.forEach((b) => { if (b && b.nodeId) push(b.nodeId); }); if (Array.isArray(n.playerBranches)) n.playerBranches.forEach((b) => { if (b && b.nodeId) push(b.nodeId); }); if (Array.isArray(n.relationBranches)) n.relationBranches.forEach((b) => { if (b && b.nodeId) push(b.nodeId); }); if (n.elseId) push(n.elseId); } } return out; } function PSJudgeSwitchType(scriptId, nodeId, newType) { const sc = PSFindScript(scriptId); if (!sc) return false; const node = sc.nodes.find((n) => n.id === nodeId); if (!node || node.type !== "judge") return false; if (node.judgeType === newType) return true; const sub = PSJudgeBranchSubtree(sc, node); if (sub.size) { sc.nodes = sc.nodes.filter((n) => !sub.has(n.id)); sc.nodes.forEach((n) => { if (n.nextId && sub.has(n.nextId)) n.nextId = null; if (n.yesId && sub.has(n.yesId)) n.yesId = null; if (n.noId && sub.has(n.noId)) n.noId = null; if (Array.isArray(n.diceBranches)) n.diceBranches = n.diceBranches.filter((b) => b && b.nodeId && !sub.has(b.nodeId)); if (Array.isArray(n.playerBranches)) n.playerBranches = n.playerBranches.filter((b) => b && b.nodeId && !sub.has(b.nodeId)); if (Array.isArray(n.relationBranches)) n.relationBranches = n.relationBranches.filter((b) => b && b.nodeId && !sub.has(b.nodeId)); if (n.elseId && sub.has(n.elseId)) n.elseId = null; }); if (PSUI.selNodeId && sub.has(PSUI.selNodeId)) PSUI.selNodeId = nodeId; } const mk = (text) => PSAddNode(scriptId, { type: "chat", text, delay: 0, enabled: true }); if (newType === "dice") { const a = mk(PST("judgeBranchPh", 1)); const b = mk(PST("judgeBranchPh", 2)); PSUpdateNode(sc.id, nodeId, { judgeType: newType, yesId: null, noId: null, elseId: null, playerBranches: [], relationBranches: [], diceBranches: [ { id: PSUid("db"), from: 1, to: 10, nodeId: a ? a.id : null }, { id: PSUid("db"), from: 11, to: 20, nodeId: b ? b.id : null }, ], }); } else if (newType === "player") { const a = mk(PST("judgeBranchPh", 1)); const b = mk(PST("judgeBranchPh", 2)); const e = mk(PST("judgeNoPh")); PSUpdateNode(sc.id, nodeId, { judgeType: newType, yesId: null, noId: null, diceBranches: [], relationBranches: [], elseId: e ? e.id : null, playerBranches: [ { id: PSUid("pb"), ids: [], nodeId: a ? a.id : null }, { id: PSUid("pb"), ids: [], nodeId: b ? b.id : null }, ], }); } else if (newType === "relation") { const a = mk(PST("judgeBranchPh", 1)); const b = mk(PST("judgeBranchPh", 2)); PSUpdateNode(sc.id, nodeId, { judgeType: newType, yesId: null, noId: null, diceBranches: [], playerBranches: [], elseId: null, relationBranches: [ { id: PSUid("rb"), rel: "owner", nodeId: a ? a.id : null }, { id: PSUid("rb"), rel: "none", nodeId: b ? b.id : null }, ], }); } else { const a = mk(PST("judgeYesPh")); const b = mk(PST("judgeNoPh")); PSUpdateNode(sc.id, nodeId, { judgeType: newType, yesId: a ? a.id : null, noId: b ? b.id : null, diceBranches: [], playerBranches: [], relationBranches: [], elseId: null, }); } return true; } function PSNodeEdges(nodes) { const byId = new Map(nodes.map((n) => [n.id, n])); const edges = new Map(nodes.map((n) => [n.id, []])); nodes.forEach((n, i) => { if (n.type === "judge") { if (n.yesId && byId.has(n.yesId)) edges.get(n.id).push(n.yesId); if (n.noId && byId.has(n.noId)) edges.get(n.id).push(n.noId); if (Array.isArray(n.diceBranches)) n.diceBranches.forEach((b) => { if (b && b.nodeId && byId.has(b.nodeId)) edges.get(n.id).push(b.nodeId); }); if (Array.isArray(n.playerBranches)) n.playerBranches.forEach((b) => { if (b && b.nodeId && byId.has(b.nodeId)) edges.get(n.id).push(b.nodeId); }); if (Array.isArray(n.relationBranches)) n.relationBranches.forEach((b) => { if (b && b.nodeId && byId.has(b.nodeId)) edges.get(n.id).push(b.nodeId); }); if (n.elseId && byId.has(n.elseId)) edges.get(n.id).push(n.elseId); } else if (n.nextId && byId.has(n.nextId)) { edges.get(n.id).push(n.nextId); } else if (nodes[i + 1]) { edges.get(n.id).push(nodes[i + 1].id); } }); return edges; } function PSNodeConnectionWouldCycle(nodes, fromId, toId) { if (!Array.isArray(nodes) || !fromId || !toId) return true; if (fromId === toId) return true; const edges = PSNodeEdges(nodes); const seen = new Set([fromId]); const stack = [toId]; while (stack.length) { const cur = stack.pop(); if (cur === fromId) return true; if (seen.has(cur)) continue; seen.add(cur); (edges.get(cur) || []).forEach((x) => stack.push(x)); } return false; } function PSBranchNodeSet(nodes) { const byId = new Map(nodes.map((n) => [n.id, n])); const set = new Set(); const walk = (id) => { let cur = byId.get(id); while (cur && !set.has(cur.id)) { set.add(cur.id); cur = (cur.nextId && byId.has(cur.nextId)) ? byId.get(cur.nextId) : null; } }; nodes.forEach((n) => { if (n.type === "judge") { if (n.yesId) walk(n.yesId); if (n.noId) walk(n.noId); if (Array.isArray(n.diceBranches)) n.diceBranches.forEach((b) => { if (b && b.nodeId) walk(b.nodeId); }); if (Array.isArray(n.playerBranches)) n.playerBranches.forEach((b) => { if (b && b.nodeId) walk(b.nodeId); }); if (Array.isArray(n.relationBranches)) n.relationBranches.forEach((b) => { if (b && b.nodeId) walk(b.nodeId); }); if (n.elseId) walk(n.elseId); } }); return set; } function PSNodeConnect(scriptId, nodeId, port, targetId) { const sc = PSFindScript(scriptId); if (!sc) return { ok: false, why: "noscript" }; const node = sc.nodes.find((n) => n.id === nodeId); const target = sc.nodes.find((n) => n.id === targetId); if (!node || !target) return { ok: false, why: "bad" }; if (PSNodeConnectionWouldCycle(sc.nodes, nodeId, targetId)) { try { PSToast(PST("connectWouldLoop")); } catch (e) { } return { ok: false, why: "loop" }; } let patch; if (port === "yes") patch = { yesId: targetId }; else if (port === "no") patch = { noId: targetId }; else if (port === "else") patch = { elseId: targetId }; else if (typeof port === "string" && port.indexOf("player:") === 0) { const bid = port.slice("player:".length); const branches = PSNormalizePlayerBranches(node.playerBranches); if (!branches.some((b) => b.id === bid)) return { ok: false, why: "bad" }; patch = { playerBranches: branches.map((b) => b.id === bid ? Object.assign({}, b, { nodeId: targetId }) : b) }; } else if (typeof port === "string" && port.indexOf("relation:") === 0) { const bid = port.slice("relation:".length); const branches = PSNormalizeRelationBranches(node.relationBranches); if (!branches.some((b) => b.id === bid)) return { ok: false, why: "bad" }; patch = { relationBranches: branches.map((b) => b.id === bid ? Object.assign({}, b, { nodeId: targetId }) : b) }; } else { patch = { nextId: targetId }; } PSUpdateNode(sc.id, nodeId, patch); return { ok: true }; } function PSNodeDisconnect(scriptId, nodeId, port) { const sc = PSFindScript(scriptId); if (!sc) return false; const node = sc.nodes.find((n) => n.id === nodeId); if (!node) return false; let patch; if (port === "yes") patch = { yesId: null }; else if (port === "no") patch = { noId: null }; else if (port === "else") patch = { elseId: null }; else if (typeof port === "string" && port.indexOf("player:") === 0) { const bid = port.slice("player:".length); const branches = PSNormalizePlayerBranches(node.playerBranches); patch = { playerBranches: branches.map((b) => b.id === bid ? Object.assign({}, b, { nodeId: null }) : b) }; } else if (typeof port === "string" && port.indexOf("relation:") === 0) { const bid = port.slice("relation:".length); const branches = PSNormalizeRelationBranches(node.relationBranches); patch = { relationBranches: branches.map((b) => b.id === bid ? Object.assign({}, b, { nodeId: null }) : b) }; } else { patch = { nextId: null }; } return PSUpdateNode(sc.id, nodeId, patch); } function PSAddNodeAfter(scriptId, nodeId, opts) { const sc = PSFindScript(scriptId); if (!sc) return null; const ref = sc.nodes.find((n) => n.id === nodeId); if (!ref) return null; if (sc.nodes.length >= PS_MAX_NODES) { PSToast(PST("toastMaxNodes", PS_MAX_NODES)); return null; } const n = PSAddNode(scriptId, opts, sc.nodes.indexOf(ref) + 1); if (n && ref.type !== "judge") { PSUpdateNode(scriptId, ref.id, { nextId: n.id }); } return n; } function PSCanSend() { if (typeof ServerPlayerIsInChatRoom === "function") { try { return !!ServerPlayerIsInChatRoom(); } catch (e) { return false; } } return true; } function PSSendChat(text) { text = PSNormalizeText(text); if (!text || !PSCanSend()) return false; if (typeof ChatRoomSendChatMessage === "function") { try { return ChatRoomSendChatMessage(text) !== false; } catch (e) { PSErr("PSSendChat", e); } } try { ServerSend("ChatRoomChat", { Content: text, Type: "Chat", Dictionary: [] }); return true; } catch (e) { PSErr("PSSendChat", e); return false; } } function PSEmoteBlocked() { if (typeof ChatRoomOwnerPresenceRule === "function") { try { return !!ChatRoomOwnerPresenceRule("BlockEmote", null); } catch (e) { return false; } } return false; } function PSChatReplyId() { try { if (typeof ChatRoomMessageGetReplyId === "function") return ChatRoomMessageGetReplyId(); } catch (e) { } return undefined; } function PSSendEmoteDirect(content) { if (!content || !PSCanSend()) return false; if (PSEmoteBlocked()) return false; try { if (typeof ChatRoomGenerateChatRoomChatMessage === "function" && typeof ServerSend === "function") { ServerSend("ChatRoomChat", ChatRoomGenerateChatRoomChatMessage("Emote", content, PSChatReplyId())); return true; } } catch (e) { PSErr("PSSendEmoteDirect", e); } try { ServerSend("ChatRoomChat", { Content: content, Type: "Emote", Dictionary: [] }); return true; } catch (e) { PSErr("PSSendEmoteDirect", e); return false; } } function PSSendRp(text) { text = PSNormalizeText(text).replace(/^\*+/, ""); if (!text) return false; return PSSendEmoteDirect(text); } function PSSendNarr(text) { text = PSNormalizeText(text).replace(/^\*+/, ""); if (!text) return false; return PSSendEmoteDirect("*" + text); } function PSSendAction(text) { text = PSNormalizeText(text); if (!text || !PSCanSend()) return false; try { ServerSend("ChatRoomChat", { Content: "Beep", Type: "Action", Dictionary: [ { Tag: "Beep", Text: "msg" }, { Tag: "发送私聊", Text: "msg" }, { Tag: "Biep", Text: "msg" }, { Tag: "Sonner", Text: "msg" }, { Tag: "msg", Text: text }, ], }); return true; } catch (e) { PSErr("PSSendAction", e); return false; } } let PSLeaveNodeActive = false; function PSSendLeave(node) { const force = !!(node && node.force); const leaveNow = () => { if (typeof ChatRoomLeave === "function") ChatRoomLeave(); if (typeof CommonSetScreen === "function") { try { CommonSetScreen("Online", "ChatSearch"); } catch (e) { } } return true; }; if (!force) { try { if (typeof ChatRoomAttemptLeave === "function") { PSLeaveNodeActive = true; ChatRoomAttemptLeave(); PSLeaveNodeActive = false; PSLog("演出结束（台词节点离开房间）"); return true; } } catch (e) { PSErr("PSSendLeave", e); } } try { PSLeaveNodeActive = true; const ok = leaveNow(); PSLeaveNodeActive = false; if (ok) PSLog(force ? "演出结束（强行离开房间）" : "演出结束（台词节点离开房间）"); return ok; } catch (e) { PSLeaveNodeActive = false; PSErr("PSSendLeave", e); return false; } } const PSSendByType = { chat: PSSendChat, rp: PSSendRp, narr: PSSendNarr, action: PSSendAction, leave: PSSendLeave, outfit: PSSendOutfit }; function PSSendJudge(node) { const res = PSJudgeRoll(node, PSActive ? PSActive.targetNum : null); if (PSActive) PSActive.lastJudge = res; if (node && node.showResult !== false) { let text = PSApplyTokens(node.text, PSActive ? PSActive.triggerNum : null, PSActive ? PSActive.targetNum : null, node.timeRules, res.label); text = PSNormalizeText(text); if (!text) text = res.label; const send = PSSendByType[node.judgeLineType] || PSSendChat; try { send(text); } catch (e) { PSErr("PSSendJudge", e); } } return true; } PSSendByType.judge = PSSendJudge; function PSOutfitClassOf(b, family) { let g = null; try { if (typeof AssetGroupGet === "function") g = AssetGroupGet(family, b && b.Group); } catch (e) { } if (!g) return "none"; if (g.Category === "Appearance") { if (!g.Clothing) return "body"; if (g.AllowNone === false) return "none"; return g.BodyCosplay ? "cos" : "clothes"; } if (g.Category === "Item" && !g.BodyCosplay) return "items"; return "none"; } function PSOutfitFlagsFromNode(n) { if (n && typeof n === "object" && ("outfitClothes" in n || "outfitBody" in n || "outfitItems" in n || "outfitCos" in n)) { return { outfitClothes: n.outfitClothes !== false, outfitBody: n.outfitBody !== false, outfitItems: n.outfitItems !== false, outfitCos: n.outfitCos !== false, }; } const mode = (n && n.outfitMode) ? n.outfitMode : "all"; if (mode === "clothes") return { outfitClothes: true, outfitBody: false, outfitItems: false, outfitCos: false }; if (mode === "body") return { outfitClothes: true, outfitBody: true, outfitItems: false, outfitCos: false }; if (mode === "items") return { outfitClothes: false, outfitBody: false, outfitItems: true, outfitCos: false }; return { outfitClothes: true, outfitBody: true, outfitItems: true, outfitCos: true }; } function PSOutfitClassAllowed(cls, flags) { if (cls === "clothes") return flags.outfitClothes; if (cls === "body") return flags.outfitBody; if (cls === "cos") return flags.outfitCos; if (cls === "items") return flags.outfitItems; return false; } function PSDecodeOutfitCode(code) { let text = String(code || "").trim(); if (!text) return null; try { if (text[0] !== "[") { if (typeof LZString === "undefined" || typeof LZString.decompressFromBase64 !== "function") return null; const decompressed = LZString.decompressFromBase64(text); if (!decompressed || typeof decompressed !== "string") return null; text = decompressed.trim(); } const arr = JSON.parse(text); if (!Array.isArray(arr)) return null; const out = []; for (const it of arr) { if (!it || typeof it !== "object") continue; if (typeof it.Group !== "string" || typeof it.Name !== "string") continue; const b = { Group: it.Group, Name: it.Name }; if (it.Color !== undefined) b.Color = it.Color; if (it.Property !== undefined) b.Property = it.Property; if (it.Craft !== undefined) b.Craft = it.Craft; if (it.Difficulty !== undefined) b.Difficulty = it.Difficulty; out.push(b); } return out.length ? out : null; } catch (e) { PSErr("PSDecodeOutfitCode", e); return null; } } function PSApplyOutfit(C, bundle, opts) { opts = opts || {}; const clearOnly = opts.itemsMode === "override" && Array.isArray(opts.selGroups) && opts.selGroups.length > 0; if (!C || !Array.isArray(bundle) || (!bundle.length && !clearOnly)) return "fail"; const family = (typeof C.AssetFamily === "string" && C.AssetFamily) ? C.AssetFamily : "Female3DCG"; const src = (typeof Player !== "undefined" && Player && typeof Player.MemberNumber === "number") ? Player.MemberNumber : ((typeof C.MemberNumber === "number") ? C.MemberNumber : null); const fromSelf = src != null && typeof C.MemberNumber === "number" && C.MemberNumber === src; let params = null; if (typeof ValidationCreateDiffParams === "function") { try { params = ValidationCreateDiffParams(C, src); } catch (e) { params = null; } } const addOnlyItemGroups = new Set(); if (opts.itemsMode !== "override") { for (const b of bundle) { if (b && typeof b.Group === "string" && PSOutfitClassOf(b, family) === "items") addOnlyItemGroups.add(b.Group); } } let didPreremove = false; if (params && typeof ValidationCanRemoveItem === "function" && Array.isArray(C.Appearance)) { try { const groups = new Set(bundle.filter((b) => b && typeof b.Group === "string").map((b) => b.Group)); const kept = []; for (const it of C.Appearance) { if (it && it.Asset && it.Asset.Group && groups.has(it.Asset.Group.Name)) { if (addOnlyItemGroups.has(it.Asset.Group.Name)) { kept.push(it); continue; } const locked = !!(it.Property && (it.Property.Lock || it.Property.LockedBy)); if (locked && !fromSelf) { kept.push(it); continue; } if (ValidationCanRemoveItem(it, params, true)) continue; kept.push(it); } else { kept.push(it); } } C.Appearance = kept; didPreremove = true; } catch (e) { } } let applied = 0, blocked = 0; PSLastOutfitBlocked = []; const skipBlocked = (b, why) => { blocked++; const label = (b && typeof b.Group === "string") ? (b.Group + "/" + (typeof b.Name === "string" ? b.Name : "?")) : "?"; PSLastOutfitBlocked.push(label + (why ? "（" + why + "）" : "")); }; for (const b of bundle) { if (!b || typeof b.Group !== "string" || typeof b.Name !== "string") { skipBlocked(b, "无效条目"); continue; } const existing = (Array.isArray(C.Appearance) && C.Appearance.find((a) => a && a.Asset && a.Asset.Group && a.Asset.Group.Name === b.Group)) || null; if (existing) { const bCls = PSOutfitClassOf(b, family); if (bCls === "items" && opts.itemsMode !== "override") { skipBlocked(b, "已有道具（添加模式不覆盖）"); continue; } if (!fromSelf && existing.Property && (existing.Property.Lock || existing.Property.LockedBy)) { skipBlocked(b, "旧部件锁住不可移除"); continue; } if (didPreremove) { skipBlocked(b, "旧部件锁住不可移除"); continue; } } if (!fromSelf) { try { if (typeof ValidationIsItemBlockedOrLimited === "function" && ValidationIsItemBlockedOrLimited(C, src, b.Group, b.Name)) { skipBlocked(b, "受限/封禁清单"); continue; } if (typeof InventoryIsPermissionBlocked === "function" && InventoryIsPermissionBlocked(C, b.Name, b.Group)) { skipBlocked(b, "权限锁定"); continue; } const G = (typeof AssetGroupGet === "function") ? AssetGroupGet(family, b.Group) : null; if (G && C.OnlineSharedSettings && typeof C.OnlineSharedSettings === "object") { if (G.BodyCosplay && C.OnlineSharedSettings.BlockBodyCosplay) { skipBlocked(b, "对方禁cosplay"); continue; } if (G.Category === "Appearance" && !G.Clothing && !C.OnlineSharedSettings.AllowFullWardrobeAccess) { skipBlocked(b, "对方未开完整衣柜权限"); continue; } } } catch (e) { } } const A = (typeof AssetGet === "function") ? AssetGet(family, b.Group, b.Name) : null; if (!A) { skipBlocked(b, "本地无此部件"); continue; } let item = null; try { if (typeof CharacterAppearanceSetItem === "function") { item = CharacterAppearanceSetItem(C, b.Group, A, b.Color, 0); } else if (typeof ServerBundledItemToAppearanceItem === "function") { item = ServerBundledItemToAppearanceItem(family, b); if (item) { if (!Array.isArray(C.Appearance)) C.Appearance = []; const idx = C.Appearance.findIndex((x) => x && x.Asset && x.Asset.Group && x.Asset.Group.Name === b.Group); if (idx >= 0) C.Appearance[idx] = item; else C.Appearance.push(item); } } } catch (e) { PSErr("PSApplyOutfit(set)", e); item = null; } if (!item) { skipBlocked(b, "应用失败"); continue; } if (typeof b.Difficulty === "number") item.Difficulty = b.Difficulty; if (b.Craft != null) item.Craft = b.Craft; if (b.Property && typeof b.Property === "object") { if (!item.Property || typeof item.Property !== "object") item.Property = {}; for (const k of Object.keys(b.Property)) item.Property[k] = b.Property[k]; } applied++; } if (!applied && !clearOnly) return "fail"; if (Array.isArray(C.Appearance)) { const classes = new Set(); const codeGroups = new Set(); for (const b of bundle) { if (b && typeof b.Group === "string") { codeGroups.add(b.Group); classes.add(PSOutfitClassOf(b, family)); } } const clearClothes = classes.has("clothes"); const clearBody = classes.has("body"); const clearCos = classes.has("cos"); const clearScope = (opts.selGroups && Array.isArray(opts.selGroups)) ? opts.selGroups : null; const clearItems = opts.itemsMode === "override" && (classes.has("items") || (clearScope && clearScope.length > 0)); PSLastOutfitCleared = []; if (clearClothes || clearBody || clearCos || clearItems) { for (let i = C.Appearance.length - 1; i >= 0; i--) { const it = C.Appearance[i]; if (!it || !it.Asset || !it.Asset.Group) continue; const g = it.Asset.Group; if (codeGroups.has(g.Name)) continue; if (g.AllowNone === false) continue; const cls = PSOutfitClassOf({ Group: g.Name }, family); let shouldClear = false; if (cls === "clothes" && clearClothes) shouldClear = true; else if (cls === "body" && clearBody) shouldClear = true; else if (cls === "cos" && clearCos) shouldClear = true; else if (cls === "items" && clearItems) { if (clearScope && !clearScope.includes(g.Name)) continue; shouldClear = true; } if (!shouldClear) continue; let removable = true; if (!fromSelf) { const locked = !!(it.Property && (it.Property.Lock || it.Property.LockedBy)); if (locked) { removable = false; } else if (params && typeof ValidationCanRemoveItem === "function") { try { removable = !!ValidationCanRemoveItem(it, params, false); } catch (e) { } } } if (!removable) continue; try { if (typeof CharacterAppearanceSetItem === "function") { CharacterAppearanceSetItem(C, g.Name, null); } else { C.Appearance.splice(i, 1); } PSLastOutfitCleared.push(g.Name); } catch (e) { } } } } if (!applied && !PSLastOutfitCleared.length) return "fail"; try { if (typeof CharacterRefresh === "function") { try { CharacterRefresh(C, true); } catch (e) { } } } catch (e) { } try { if (typeof ServerPlayerIsInChatRoom === "function" && ServerPlayerIsInChatRoom() && typeof ChatRoomCharacterUpdate === "function") { ChatRoomCharacterUpdate(C); } } catch (e) { } return blocked > 0 ? "partial" : "full"; } function PSOutfitPrepareBundle(node, bundle, family) { const flags = PSOutfitFlagsFromNode(node); const locks = !!(node && node.outfitLocks); const selGroups = Array.isArray(node && node.outfitGroups) ? node.outfitGroups : null; const out = []; for (const b of bundle) { const cls = PSOutfitClassOf(b, family); if (!PSOutfitClassAllowed(cls, flags)) continue; if (selGroups && cls === "items" && !selGroups.includes(b.Group)) continue; const nb = PSDeepClone(b); if (nb.Property && typeof nb.Property === "object") { if (!locks) { delete nb.Property.Lock; delete nb.Property.LockedBy; delete nb.Property.LockMemberNumber; } else if (nb.Property.Lock || nb.Property.LockedBy) { nb.Property.LockMemberNumber = (typeof Player !== "undefined" && Player && typeof Player.MemberNumber === "number") ? Player.MemberNumber : null; } } out.push(nb); } return out; } function PSSendOutfit(node) { const code = PSNormalizeCode(node && node.text); if (!code) return false; if (!PSCanSend()) return false; const bundle = PSDecodeOutfitCode(code); if (!bundle) { PSToast(PST("outfitBad")); return false; } let C = null; if (node && node.outfitTarget === "self") { C = (typeof Player !== "undefined" && Player) ? Player : null; } else { const num = PSActive ? PSActive.targetNum : null; C = PSCharObjectOf(num); if (!C) C = (typeof Player !== "undefined" && Player) ? Player : null; } if (!C) return false; if (typeof Player === "undefined" || !Player || C !== Player) { if (typeof C.MemberNumber === "number" && !PSTargetInRoom(C.MemberNumber)) { PSToast(PST("outfitTargetGone")); return false; } } const family = (typeof C.AssetFamily === "string" && C.AssetFamily) ? C.AssetFamily : "Female3DCG"; const filtered = PSOutfitPrepareBundle(node, bundle, family); const selGroups = (node && Array.isArray(node.outfitGroups)) ? node.outfitGroups : null; const clearOnly = (node && node.outfitItemsMode === "override") && !!selGroups && selGroups.length > 0; if (!filtered.length && !clearOnly) { PSToast(PST("outfitNone")); return false; } const noneSkipped = bundle.filter((b) => b && PSOutfitClassOf(b, family) === "none"); const r = PSApplyOutfit(C, filtered, { itemsMode: (node && node.outfitItemsMode === "override") ? "override" : "add", selGroups, }); if (r === "fail") { if (PSLastOutfitBlocked.length) { const list = PSLastOutfitBlocked.slice(0, 6).join("、") + (PSLastOutfitBlocked.length > 6 ? "…" : ""); PSToast(PST("outfitBlockedList", list)); } else { PSToast(PST("outfitBlocked")); } return false; } if (r === "partial") { const list = PSLastOutfitBlocked.slice(0, 6).join("、") + (PSLastOutfitBlocked.length > 6 ? "…" : ""); PSToast(PST("outfitPartial", list)); return true; } if (noneSkipped.length > 0) { const list = noneSkipped.slice(0, 6).map((b) => b.Group + "/" + b.Name).join("、") + (noneSkipped.length > 6 ? "…" : ""); PSToast(PST("outfitAppliedSkip", PSCharDisplayName(C) || "", noneSkipped.length, list)); } else if (PSLastOutfitCleared.length > 0) { PSToast(PST("outfitAppliedClear", PSCharDisplayName(C) || "", PSLastOutfitCleared.length)); } else { PSToast(PST("outfitApplied", PSCharDisplayName(C) || "")); } return true; } function PSSendNode(node) { if (!PSCanSend()) return "offline"; const fn = PSSendByType[node.type]; if (!fn) return "blocked"; const triggerNum = PSActive ? PSActive.triggerNum : null; const targetNum = PSActive ? PSActive.targetNum : null; const text = PSApplyTokens(node.text, triggerNum, targetNum, node.timeRules); const arg = (node.type === "leave" || node.type === "outfit" || node.type === "judge") ? node : text; try { return fn(arg) ? "sent" : "blocked"; } catch (e) { PSErr("PSSendNode", e); return "blocked"; } } function PSFire(scriptId, opts) { opts = opts || {}; const s = PSFindScript(scriptId); if (!s) return { ok: false, why: "noscript" }; if (s.enabled === false && !opts.manual) return { ok: false, why: "disabled" }; const enabledNodes = s.nodes.filter((n) => n.enabled !== false && (n.type === "leave" || n.type === "judge" || PSNormalizeText(n.text))); if (!enabledNodes.length) { PSToast(PST("toastEmptyNodes", s.name)); return { ok: false, why: "empty" }; } if (!PSCanSend()) { PSToast(PST("toastNeedRoom")); return { ok: false, why: "noroom" }; } let triggerNum = opts.senderNum; if (triggerNum === undefined) { triggerNum = (typeof Player !== "undefined" && Player && typeof Player.MemberNumber === "number") ? Player.MemberNumber : null; } let targetNum = opts.targetNum; if (targetNum === undefined) targetNum = triggerNum; if (!opts.manual) { const cd = Number(s.cooldown) || 0; if (cd > 0) { const wait = ((PSLastFire[s.id] || 0) + cd * 1000) - PSNow(); if (wait > 0) { PSToast(PST("toastCooldown", Math.ceil(wait / 1000))); return { ok: false, why: "cooldown" }; } } PSLastFire[s.id] = PSNow(); } if (PSActive) { const mode = opts.manual ? "restart" : s.onRetrigger; if (mode === "ignore") { PSToast(PST("toastBusy")); return { ok: false, why: "busy" }; } if (mode === "queue") { if (PSQueue.length >= PS_QUEUE_CAP) { PSToast(PST("toastQueueFull")); return { ok: false, why: "queuefull" }; } PSQueue.push({ id: s.id, triggerNum, targetNum }); PSToast(PST("toastQueued", s.name, PSQueue.length)); PSLog("已排队：", s.name, "（第", PSQueue.length, "位）"); PSUIRenderStatus(); return { ok: true, queued: true }; } PSAbortCore(true, "restart"); } PSRun(s, enabledNodes, triggerNum, targetNum); return { ok: true }; } function PSRun(s, nodes, triggerNum, targetNum) { PSActive = { scriptId: s.id, name: s.name, nodes: nodes.map((n) => ({ id: n.id, type: n.type, text: (n.type === "outfit" ? PSNormalizeCode(n.text) : PSNormalizeText(n.text)), delay: n.delay, force: !!n.force, outfitTarget: n.outfitTarget === "self" ? "self" : "target", outfitClothes: n.outfitClothes !== false, outfitBody: n.outfitBody !== false, outfitItems: n.outfitItems !== false, outfitCos: n.outfitCos !== false, outfitLocks: !!n.outfitLocks, outfitItemsMode: n.outfitItemsMode === "override" ? "override" : "add", outfitGroups: Array.isArray(n.outfitGroups) ? n.outfitGroups.slice() : null, timeRules: Array.isArray(n.timeRules) ? n.timeRules.slice() : [], nextId: (typeof n.nextId === "string" && n.nextId) ? n.nextId : null, yesId: (typeof n.yesId === "string" && n.yesId) ? n.yesId : null, noId: (typeof n.noId === "string" && n.noId) ? n.noId : null, judgeType: PSNormalizeJudgeType(n.judgeType), coinYesIs: n.coinYesIs === "tails" ? "tails" : "heads", diceExpr: PSValidDiceExpr(n.diceExpr) ? String(n.diceExpr) : "1d6", diceRule: n.diceRule === "lte" ? "lte" : "gte", diceThreshold: Number(n.diceThreshold) || 4, showResult: n.showResult !== false, judgeLineType: ["chat", "rp", "narr", "action"].includes(n.judgeLineType) ? n.judgeLineType : "chat", diceBranches: Array.isArray(n.diceBranches) ? n.diceBranches.slice() : [], playerIds: Array.isArray(n.playerIds) ? n.playerIds.slice() : [], playerBranches: Array.isArray(n.playerBranches) ? n.playerBranches.map((b) => ({ id: b.id, ids: (Array.isArray(b.ids) ? b.ids.slice() : []), nodeId: b.nodeId })) : [], relationBranches: Array.isArray(n.relationBranches) ? n.relationBranches.map((b) => ({ id: b.id, rel: b.rel, nodeId: b.nodeId })) : [], elseId: (typeof n.elseId === "string" && n.elseId) ? n.elseId : null, })), idx: -1, timer: null, nextAt: PSNow(), startedAt: PSNow(), triggerNum: (triggerNum === undefined ? null : triggerNum), targetNum: (targetNum === undefined ? null : targetNum), lastJudge: null, steps: 0, hasLinks: nodes.some((n) => !!n.nextId || (n.type === "judge" && (!!n.yesId || !!n.noId))), branchNodes: PSBranchNodeSet(nodes), }; PSLog("演出开始：", s.name, "共", PSActive.nodes.length, "句"); PSToast(PST("toastStarted", s.name, PSActive.nodes.length)); PSAdvance(); } function PSNodeNext(A, cur) { if (cur.type === "judge") { const j = A.lastJudge || { yes: false }; if (j.targetId) return A.nodes.find((n) => n.id === j.targetId) || null; const id = j.yes ? cur.yesId : cur.noId; return id ? (A.nodes.find((n) => n.id === id) || null) : null; } if (cur.nextId) { const t = A.nodes.find((n) => n.id === cur.nextId); if (t) return t; } if (A.branchNodes && A.branchNodes.has(cur.id)) return null; const idx = A.nodes.indexOf(cur); return (idx >= 0 && idx < A.nodes.length - 1) ? A.nodes[idx + 1] : null; } function PSAdvance() { const A = PSActive; if (!A) return; const cur = A.idx >= 0 ? A.nodes[A.idx] : null; const next = cur ? PSNodeNext(A, cur) : A.nodes[0]; if (!next) { PSFinish(); return; } A.steps = (A.steps || 0) + 1; if (A.steps > Math.max(200, A.nodes.length * 20)) { PSErr("判定连接疑似成环，已中止演出"); PSAbortCore(false, "loop"); return; } A.idx = A.nodes.indexOf(next); const waitMs = Math.max(0, (Number(next.delay) || 0) * 1000); A.nextAt = PSNow() + waitMs; A.timer = setTimeout(() => { A.timer = null; const r = PSSendNode(next); if (r === "offline") { PSAbortCore(false, "noroom"); return; } PSAdvance(); }, waitMs); PSUIRenderStatus(); } function PSFinish() { const A = PSActive; PSActive = null; if (A) { PSLog("演出结束：", A.name); PSToast(PST("toastFinished", A.name)); } if (PSQueue.length) { const next = PSQueue.shift(); setTimeout(() => { const s = PSFindScript(next.id); if (!s) return; const nodes = s.nodes.filter((n) => n.enabled !== false && (n.type === "leave" || PSNormalizeText(n.text))); if (nodes.length) PSRun(s, nodes, next.triggerNum, next.targetNum); }, 300); } PSUIRenderStatus(); } function PSAbortCore(silent, reason) { const A = PSActive; if (!A) return false; if (A.timer) { clearTimeout(A.timer); A.timer = null; } PSActive = null; if (!silent) { if (reason === "noroom") PSToast(PST("toastAbortRoom")); else PSToast(PST("toastAborted", A.name)); } PSLog("演出中止：", A.name, reason || "manual"); PSUIRenderStatus(); return true; } function PSStop() { if (!PSActive && !PSQueue.length) { PSToast(PST("toastNothing")); return false; } PSQueue = []; return PSAbortCore(false, "manual"); } function PSScriptUsesTarget(sc) { if (!sc || !Array.isArray(sc.nodes)) return false; return sc.nodes.some((n) => { if (n && n.type === "outfit") return n.outfitTarget !== "self"; if (n && typeof n.text === "string") return n.text.includes(PS_TARGET_TOKEN); return false; }); } function PSTriggerFromText(text, senderNum) { if (!PSStore.state) return; let me = null; if (typeof Player !== "undefined" && Player && typeof Player.MemberNumber === "number") me = Player.MemberNumber; const fromMe = (senderNum === null || senderNum === undefined) ? true : (me !== null && senderNum === me); for (const sc of PSStore.state.scripts) { if (!sc.keyword) continue; const who = sc.triggerWho; if (who === "self" && !fromMe) continue; if (who === "others" && fromMe) continue; const info = PSMatchInfo(text, sc); if (!info) continue; const target = PSTriggerTargetOf(text, sc, info); const attempt = PSTriggerTargetAttempt(text, sc, info); if (attempt && !target && PSScriptUsesTarget(sc)) { PSToast(PST("triggerNoTarget", attempt)); PSLog("目标未找到：", attempt); continue; } PSLog("触发命中：", sc.name, "←", String(text).slice(0, 80), target ? "目标：" + target.name : ""); PSFire(sc.id, { senderNum, targetNum: target ? target.num : undefined }); } } const PS_ACTION_GROUPS = [ { name: "any", zh: "任意部位", en: "Any part" }, { name: "ItemHead", zh: "头部", en: "Head" }, { name: "ItemMouth", zh: "嘴部", en: "Mouth" }, { name: "ItemNeck", zh: "颈部", en: "Neck" }, { name: "ItemTorso", zh: "躯干", en: "Torso" }, { name: "ItemArms", zh: "手臂", en: "Arms" }, { name: "ItemHands", zh: "手部", en: "Hands" }, { name: "ItemLegs", zh: "腿部", en: "Legs" }, { name: "ItemFeet", zh: "脚部", en: "Feet" }, { name: "ItemBoots", zh: "鞋子", en: "Boots" }, { name: "ItemButt", zh: "臀部", en: "Butt" }, { name: "ItemVulva", zh: "私处", en: "Crotch" }, { name: "ItemPenis", zh: "私处", en: "Crotch" }, { name: "ItemNipples", zh: "胸部", en: "Chest" }, { name: "ItemAddon", zh: "装饰", en: "Add-on" }, ]; function PSActGroupLabel(name) { const g = PS_ACTION_GROUPS.find((x) => x.name === name); if (!g) return name; return PSUI && PSUI.lang === "en" ? g.en : g.zh; } function PSInteractNormalizeActions(v) { const out = []; if (!Array.isArray(v)) return out; for (const it of v) { if (!it || typeof it !== "object") continue; const g = String(it.g ?? it.group ?? "").trim(); const a = String(it.a ?? it.action ?? "").trim(); if (!g || !a) continue; if (out.length >= PS_MAX_INTERACT_ACTIONS) break; if (!out.some((x) => x.g === g && x.a === a)) out.push({ g, a }); } return out; } function PSInteractGroups(sc) { if (!sc || !Array.isArray(sc.interactActions)) return []; const seen = new Set(); const out = []; for (const x of sc.interactActions) { if (x && typeof x.g === "string" && !seen.has(x.g)) { seen.add(x.g); out.push(x.g); } } return out; } function PSInteractGroupCount(sc, group) { if (!sc || !Array.isArray(sc.interactActions)) return 0; return sc.interactActions.filter((x) => x && x.g === group).length; } function PSInteractHas(sc, group, action) { if (!sc || !Array.isArray(sc.interactActions)) return false; return sc.interactActions.some((x) => x && x.g === group && x.a === action); } function PSInteractActivityNames(baseGroup) { const family = (typeof Player !== "undefined" && Player && typeof Player.AssetFamily === "string" && Player.AssetFamily) ? Player.AssetFamily : "Female3DCG"; const seen = new Set(); const out = []; const push = (name) => { if (name && typeof name === "string" && !seen.has(name)) { seen.add(name); out.push(name); } }; const actual = []; if (typeof AssetGroup !== "undefined" && Array.isArray(AssetGroup)) { for (const g of AssetGroup) { try { if (!g || typeof g.IsItem !== "function" || !g.IsItem()) continue; } catch (e) { continue; } if (g.Name && PSActBaseGroup(g.Name) === baseGroup) actual.push(g.Name); } } if (!actual.length) actual.push(baseGroup); for (const gname of actual) { try { if (typeof AssetActivitiesForGroup === "function") { const acts = AssetActivitiesForGroup(family, gname, "other") || []; for (const a of acts) push(a && a.Name); } } catch (e) { } } try { if (typeof ActivityAllowedForGroup === "function" && typeof Player !== "undefined" && Player) { const allowed = ActivityAllowedForGroup(Player, baseGroup) || []; for (const e of allowed) { if (!e || e.__psCustom) continue; if (e.Activity) push(e.Activity.Name); else if (typeof e.Name === "string") push(e.Name); } } } catch (e) { } return out; } function PSInteractActivityLabel(baseGroup, activityName) { try { const family = (typeof Player !== "undefined" && Player && typeof Player.AssetFamily === "string" && Player.AssetFamily) ? Player.AssetFamily : "Female3DCG"; let group = null; try { if (typeof ActivityGetGroupOrMirror === "function") group = ActivityGetGroupOrMirror(family, baseGroup); } catch (e) {} if (!group) { try { if (typeof AssetGroupGet === "function") group = AssetGroupGet(family, baseGroup); } catch (e) {} } const gname = (group && group.Name) ? group.Name : baseGroup; let realGroup = gname; const map = { "ItemVulva": "ItemPenis", "ItemVulvaPiercings": "ItemGlans" }; try { if (typeof Player !== "undefined" && Player && typeof Player.HasPenis === "function" && Player.HasPenis() && map[gname]) realGroup = map[gname]; } catch (e) {} const tag = "Label-ChatOther-" + realGroup + "-" + activityName; if (typeof ActivityDictionaryText === "function") { const txt = ActivityDictionaryText(tag); if (txt && typeof txt === "string" && txt.length && txt !== tag) return txt; } return activityName; } catch (e) { return activityName; } } function PSActEntriesFor(C) { const out = []; if (!C || !C.FocusGroup || !PSStore.state) return out; const gname = C.FocusGroup.Name; const gbase = PSActBaseGroup(gname); const isSelf = PSCharIsSelf(C); for (const sc of PSStore.state.scripts) { if (sc.enabled === false) continue; if (!sc.actionEnabled) continue; const label = String(sc.actionName || "").trim(); if (!label) continue; if (sc.actionGroup && sc.actionGroup !== "any" && PSActBaseGroup(sc.actionGroup) !== gbase) continue; if (sc.actionTarget === "self" && !isSelf) continue; if (sc.actionTarget === "others" && isSelf) continue; out.push({ Activity: { Name: "PlayScriptCustom", MaxProgress: 0 }, Group: gname, __psCustom: true, __psScriptId: sc.id, __psLabel: label.slice(0, 64), }); } return out; } function PSInstallActivityButtonHook(mod) { PSHookWhen(mod, "ElementButton.CreateForActivity", 10, (args, next) => { const activity = args[1]; if (activity && activity.__psCustom) { const labelText = String(activity.__psLabel || "PlayScript"); const options = (args[4] = args[4] || {}); options.label = "▶ " + labelText; options.image = null; options.tooltip = [labelText]; const htmlOptions = (args[5] = args[5] || {}); htmlOptions.label = Object.assign({}, htmlOptions.label); htmlOptions.label.style = Object.assign({}, (htmlOptions.label && htmlOptions.label.style), { color: "#2ec4b6", fontWeight: "700", }); } return next(args); }); } function PSInputClear(input) { if (!input) return; input.value = ""; try { input.dispatchEvent(new InputEvent("input")); } catch (e) { } } function PSHookWhen(mod, name, prio, hook) { const attempt = () => { try { mod.hookFunction(name, prio, hook); return "ok"; } catch (e) { const msg = (e && e.message) ? String(e.message) : ""; return (msg.indexOf("not found") >= 0 || msg.indexOf("is not object") >= 0) ? "wait" : e; } }; const first = attempt(); if (first === "ok") return; if (first !== "wait") { PSErr("hook " + name + " 失败", first); return; } const startAt = PSNow(); const timer = setInterval(() => { const r = attempt(); if (r === "ok") { clearInterval(timer); return; } if (r !== "wait") { clearInterval(timer); PSErr("hook " + name + " 失败", r); return; } if (PSNow() - startAt > 120000) { clearInterval(timer); PSErr("hook " + name + " 失败：2 分钟内目标函数未出现"); } }, 300); } function PSInstallHooks(mod) { const safe = (fn) => (...args) => { try { return fn(...args); } catch (e) { PSErr(e); } }; PSHookWhen(mod, "ChatRoomSendChat", 10, (args, next) => { if (typeof document !== "undefined") { const input = document.getElementById("InputChat"); const raw = input ? input.value : ""; const t = (raw || "").trim().toLowerCase(); if (t === "/ps" || t === "/play" || t === "/剧本" || t === "/演出") { PSInputClear(input); safe(PlayScriptToggle)(); return; } const res = next(args); safe(() => { if (typeof Player !== "undefined" && Player) PSStore.ensureAccount(Player.MemberNumber); const txt = (raw || "").trim(); if (txt) PSTriggerFromText(txt, null); })(); return res; } return next(args); }); PSHookWhen(mod, "ChatRoomMessage", 10, (args, next) => { const res = next(args); safe(() => { const data = args[0]; if (!data || typeof data.Sender !== "number") return; if (typeof Player !== "undefined" && Player && data.Sender === Player.MemberNumber) return; if (data.Type !== "Chat" && data.Type !== "Whisper" && data.Type !== "Emote") return; const txt = data.Content; if (txt && String(txt).trim()) PSTriggerFromText(String(txt), data.Sender); })(); return res; }); PSHookWhen(mod, "ChatRoomLeave", 10, (args, next) => { const res = next(args); safe(() => { if (PSActive) PSAbortCore(PSLeaveNodeActive === true, "noroom"); if (PSQueue.length) PSQueue = []; })(); return res; }); PSHookWhen(mod, "ActivityAllowedForGroup", 10, (args, next) => { const res = next(args); safe(() => { const C = args[0]; const entries = PSActEntriesFor(C); if (entries.length && Array.isArray(res)) { PSLog("动作面板注入", entries.length, "条（部位:", C && C.FocusGroup && C.FocusGroup.Name, "）"); for (const e of entries) res.push(e); } })(); return res; }); PSHookWhen(mod, "DialogActivityClick", 10, (args, next) => { const C = args[0]; const clicked = args[1]; if (clicked && clicked.__psCustom && clicked.__psScriptId) { safe(() => { const sc = PSFindScript(clicked.__psScriptId); if (sc) { PSLog("动作触发：", sc.name, "目标：", C && C.Name); PSFire(sc.id, { manual: true, senderNum: (typeof Player !== "undefined" && Player) ? Player.MemberNumber : null, targetNum: (C && typeof C.MemberNumber === "number") ? C.MemberNumber : null, }); try { if (typeof DialogLeave === "function" && typeof CurrentScreen !== "undefined" && CurrentScreen === "ChatRoom") { DialogLeave(); } } catch (e) { } } })(); return; } return next(args); }); PSInstallActivityButtonHook(mod); PSHookWhen(mod, "LoginResponse", 10, (args, next) => { const C = args[0]; if (C && typeof C === "object") { PSStore.lastLoginData = C; try { const keys = PSMeasureDataSize(C); let total = 0, selfBytes = 0; for (const k of Object.keys(keys)) { const b = keys[k]; if (!isNaN(b)) { total += b; if (k === PSStore.cloudKey) selfBytes = b; } } PSStore.lastAccountSnapshot = { total, self: selfBytes, keys }; } catch (e) { } } const res = next(args); safe(() => { if (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber) && Player.MemberNumber > 0) { let cloudState = null; if (C && typeof C === "object" && typeof C[PSStore.cloudKey] === "string") { try { cloudState = PSStore.normalize(PSStore.decode(C[PSStore.cloudKey])); } catch (e) { cloudState = null; } } const wasBound = (PSStore.accountNum === Player.MemberNumber); PSStore.ensureAccount(Player.MemberNumber, cloudState); if (wasBound) { const merged = PSStore.mergeBio(Player.MemberNumber); if (merged > 0) { PSStore.save(); PSLog("BIO 备份：从玩家描述读回", merged, "个剧本"); } } if (PSUI.open) PSUIRenderAll(); PSToast(PST("toastDataLoaded", PSStore.state ? PSStore.state.scripts.length : 0)); } })(); return res; }); PSHookWhen(mod, "ServerHandleRelog", 10, (args, next) => { const res = next(args); safe(() => { if (res && typeof RelogData === "object" && RelogData && RelogData.ChatRoomName) { PSRoomRelogArmed = PSNow(); } })(); return res; }); PSHookWhen(mod, "ChatRoomSync", 10, (args, next) => { const res = next(args); const run = safe(() => { if (!PSStore.state) return; const reconnecting = (PSRoomRelogArmed && (PSNow() - PSRoomRelogArmed) < 30000); PSRoomRelogArmed = 0; const me = (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber)) ? Player.MemberNumber : null; for (const sc of PSStore.state.scripts) { if (sc.enabled === false) continue; if (sc.roomTrigger) { if (reconnecting && sc.roomTriggerOnReconnect === false) { PSLog("进房触发（重连回房，按设置跳过）：", sc.name); continue; } PSLog("进房触发：", sc.name); PSFire(sc.id, { senderNum: me }); } if (sc.playerTrigger && Array.isArray(sc.playerTargets) && sc.playerTargets.length) { const inRoom = sc.playerTargets.find((num) => PSTargetInRoom(num)); if (inRoom) { PSLog("进房见到玩家触发：", sc.name, "#" + inRoom); PSFire(sc.id, { senderNum: me, targetNum: inRoom }); } } } }); if (res && typeof res.then === "function") { res.then(() => { run(); }, () => {}).catch(() => {}); return res; } run(); return res; }); PSHookWhen(mod, "ChatRoomSyncMemberJoin", 10, (args, next) => { const res = next(args); safe(() => { const data = args[0]; const num = data && (data.SourceMemberNumber || (data.Character && data.Character.MemberNumber)); if (!Number.isInteger(num) || num <= 0 || !PSStore.state) return; const me = (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber)) ? Player.MemberNumber : null; if (num === me) return; for (const sc of PSStore.state.scripts) { if (sc.enabled === false) continue; if (sc.playerTrigger && Array.isArray(sc.playerTargets) && sc.playerTargets.includes(num)) { PSLog("玩家进房触发：", sc.name, "#" + num); PSFire(sc.id, { senderNum: me, targetNum: num }); } } })(); return res; }); PSHookWhen(mod, "ActivityOrgasmStart", 10, (args, next) => { const res = next(args); safe(() => { const C = args[0]; if (!C || typeof C.IsPlayer !== "function" || !C.IsPlayer() || !PSStore.state) return; if (!PSCanSend()) return; const me = (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber)) ? Player.MemberNumber : null; const ruined = (typeof ActivityOrgasmRuined !== "undefined" && ActivityOrgasmRuined); for (const sc of PSStore.state.scripts) { if (sc.enabled === false) continue; if (!ruined && sc.orgasmTrigger) { PSLog("高潮触发：", sc.name); PSFire(sc.id, { senderNum: me }); } else if (ruined && sc.orgasmDenyTrigger) { PSLog("拒绝高潮触发（毁坏高潮）：", sc.name); PSFire(sc.id, { senderNum: me }); } } })(); return res; }); PSHookWhen(mod, "ActivityEffect", 10, (args, next) => { const res = next(args); safe(() => { const S = args[0], C = args[1], A = args[2], Z = args[3]; if (!PSStore.state) return; if (!C || typeof C.IsPlayer !== "function" || !C.IsPlayer()) return; if (!S || typeof S.IsPlayer !== "function" || S.IsPlayer()) return; if (typeof S.IsNpc === "function" && S.IsNpc()) return; if (!PSCanSend()) return; const actName = (A && typeof A === "object") ? (A.Name || "") : String(A || ""); const group = PSActBaseGroup(String(Z || "")); if (!actName || !group) return; const me = (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber)) ? Player.MemberNumber : null; for (const sc of PSStore.state.scripts) { if (sc.enabled === false || !sc.interactTrigger) continue; if (!PSInteractHas(sc, group, actName)) continue; PSLog("互动触发：", sc.name, "#" + (S.MemberNumber != null ? S.MemberNumber : "?"), actName, "@" + group); PSFire(sc.id, { senderNum: me, targetNum: S.MemberNumber }); } })(); return res; }); } function PSMain() { if (typeof window !== "undefined" && window.PlayScriptInstalled) { PSLog("已安装，跳过重复加载"); return; } const tryRegister = (tries) => { if (typeof bcModSdk === "undefined" || !bcModSdk.registerMod) { if (tries > 600) { PSErr("等待 bcModSdk 超时（60 秒），mod 未能加载"); return; } setTimeout(() => tryRegister(tries + 1), 100); return; } try { const mod = bcModSdk.registerMod({ name: "PlayScript", fullName: "PlayScript — 关键词演出脚本引擎", version: PS_VERSION, repository: "", }, { allowReplace: true }); PSStore.load(); try { if (typeof Player !== "undefined" && Player && typeof Player.MemberNumber === "number") { PSStore.ensureAccount(Player.MemberNumber); } } catch (e) { } PSUI.lang = PSStorage.get("PlayScriptLang") === "en" ? "en" : "zh"; PSExposeAPI(); PSInstallHooks(mod); if (PSTimeScanTimer) { clearInterval(PSTimeScanTimer); PSTimeScanTimer = null; } try { PSTimeScanTimer = setInterval(() => { try { PSTimeTriggerScan(); } catch (e) { PSErr(e); } }, 15000); } catch (e) { } if (typeof window !== "undefined") window.PlayScriptInstalled = true; try { PSUIDotBuild(); } catch (e) { PSErr("小圆点按钮构建失败", e); } PSLog("PlayScript v" + PS_VERSION + " 已加载。聊天室输入 /ps 开关演出脚本浮窗；控制台可用 PlayScriptOpen() / window.PlayScript"); } catch (e) { PSErr("注册 mod 失败", e); } }; tryRegister(0); } const PSText = { zh: { title: "PlayScript 演出脚本", tips: "拖标题栏移动 · 拖右下角拉伸 · 点节点卡片编辑台词 · 聊天里说出关键词自动演出", cmd: "聊天室输入 /ps 开关本窗", scriptsHeader: "剧本", scriptsHint: "点左侧选择剧本", newScript: "新建剧本", testRun: "手动触发", stop: "停止演出", exportBtn: "导出", exportOneBtn: "导出本剧本", importBtn: "导入", clearAll: "清空", clearAllConfirm: "确认清空", langBtn: "EN", minimizeTitle: "最小化（再点标题栏恢复）", closeTitle: "关闭浮窗", resizeTitle: "拖动拉伸窗口", editorResizeTitle: "拖动拉宽/收窄设置区", scriptSettings: "剧本设置", scriptName: "剧本名", keyword: "触发关键词", keywordPh: "如：天降正义", triggerSettings: "触发设置", chipOther: "其他", editorHint: "点画布顶部的 ▶ 触发块设置触发方式；点台词节点卡片编辑该句；点左侧剧本行回到这里", triggerTargetHint: "对指定玩家演出：说「关键词，对方名字/数字ID」或反过来（如「午时已到，瓦提娜」「天降正义 安缇妮」「255225，英雄不朽」「Vatina 在这停顿」），<target> 自动指向该玩家（英文昵称不分大小写）。剧本有用到 <target> 或服装节点「对目标角色」时，找不到该玩家 → 不执行并提示；剧本不用目标时照常触发。防误触：关键词前后必须是空格或符号，紧挨其它文字不会触发（「我喜欢苹果因为它好吃」不触发，「我喜欢 苹果，因为它好吃」触发）", triggerNoTarget: "找不到目标玩家：房间里没有「{0}」（名字或数字 ID 不匹配），本句未触发", matchMode: "匹配方式", matchContains: "包含关键词", matchExact: "完全相等", caseSensitive: "区分大小写", triggerWho: "谁能触发", whoSelf: "只有我", whoAnyone: "任何人", whoOthers: "除我以外", cooldown: "冷却（秒，0=关闭）", onRetrigger: "演出中再触发", retryIgnore: "忽略", retryRestart: "重新开始", retryQueue: "排队", enabled: "启用", actTrigger: "动作触发（游戏动作面板）", actHint: "点击角色身体部位的动作面板里会显示这个自定义动作，点它即触发本剧本（等同于手动触发）", actName: "动作名", actNamePh: "例如：即兴演出", actGroup: "出现部位", actPick: "选择部位…", actWinTitle: "选择动作出现部位", actCurrent: "当前：{0}", actAny: "任意部位", actDone: "完成", actTarget: "作用目标", actTargetSelf: "只能对自己（点自己的面板可见）", actTargetOthers: "只能对别人（点别人的面板可见）", otherTrigger: "其他触发", otherHint: "进房自动演出；见到指定玩家（对方进房 / 你进房时对方已在）也会触发（<target>=对方昵称）", roomTrigger: "进入房间触发", roomTriggerOnReconnect: "重连回房间时也触发", timeTrigger: "根据时间触发", timeTriggerHint: "设置一个或多个时间段+时区，当前时间命中即自动触发（只在聊天室内检查，每 15 秒一次）", playerTrigger: "见到玩家触发", playerTargetId: "玩家数字ID（可多个）", orgasmTrigger: "高潮触发", orgasmDenyTrigger: "拒绝高潮触发", orgasmHint: "自己高潮完成时触发「高潮触发」；被拒绝高潮（=毁坏高潮，如高潮锁「拒绝」模式）时触发「拒绝高潮触发」", interactTrigger: "别人对我互动触发", interactHint: "别的玩家对你做的互动动作命中时自动触发：先选身体部位，再选该部位上哪些动作会触发（绿格=该部位已有选中动作）", interactPick: "选择部位与动作…", interactGroupsN: "已选 {0} 个部位 / {1} 个动作", interactWinTitle: "选择互动触发部位", interactActWinTitle: "选择 {0} 的互动动作", interactActHint: "点动作切换选中（绿底=会触发），可多选", interactEmpty: "这个部位没有可选互动动作", interactBack: "返回部位", interactCurrent: "已选 {0} 个动作", dupScript: "复制剧本", delScript: "删除剧本", delScriptConfirm: "确认删除", nodeEdit: "台词节点", nodeType: "消息类型", typeChat: "普通消息", typeRp: "RP 动作 (*)", typeNarr: "旁白 (**)", typeAction: "Action 消息", typeLeave: "离开房间", leaveHint: "这个节点会让你离开当前房间（不发送任何台词）。通常放在剧本最后，之后的台词不会发出。", leavePreview: "离开当前房间（不发送台词）", forceLeave: "强行离开", forceLeaveHint: "勾选后跳过游戏的离房资格检查（被牵引绳拴住/无法行走/监狱/锁房等受限状态）与缓慢离房，立即离开并切回找房界面", typeOutfit: "服装道具变更", typeJudge: "判定", judgeEdit: "判定设置", judgeType: "判定类型", judgeCoin: "硬币（正/反）", judgeDice: "骰子", judgeCoinYesIs: "走「是」的一面", judgeCoinHeads: "正面 → 是", judgeCoinTails: "反面 → 是", judgeDiceExpr: "骰子指令", judgeDiceRule: "判定方式", judgeDiceThreshold: "阈值", judgeGte: "≥ 阈值 → 是", judgeLte: "≤ 阈值 → 是", judgePlayer: "玩家ID判定", judgePlayerHint: "判定当前演出的对方数字ID，按 ID 走不同分支；都不匹配走「其他玩家」。多个ID用逗号/空格分隔", judgePlayerIdsPh: "如 12345、67890", judgePlayerElse: "其他玩家", judgePlayerBranchPh: "分支{0}", judgeRelation: "玩家关系判定", judgeRelationHint: "判定对方与我的关系，按 主人→恋人→白名单→好友→无关系 的固定顺序走对应分支", relOwner: "主人", relLover: "恋人", relWhite: "白名单", relFriend: "好友", relNone: "无关系", judgeShowResult: "显示判定结果", judgeLineType: "结果台词类型", judgeDefaultLine: "判定结果：<roll>", judgeYesPh: "（是分支，点此编辑）", judgeNoPh: "（否分支，点此编辑）", judgeDiceBranches: "结果分支（最多 5 个）", judgeDiceBranchesHint: "按点数范围走不同分支：1~10 走分支1、11~20 走分支2……添加分支会自动创建并连接一个台词节点", judgeBranchAdd: "添加分支", judgeBranchMax: "最多只能有 5 个结果分支", judgeBranchPh: "（分支{0}，点此编辑）", insertRoll: "插入判定结果", addJudge: "+ 添加判定", connectBtn: "连接…", connectDisconnect: "断开", connectHint: "连到某个节点作为下一句", connectWinTitle: "连接到：{0}", connectNone: "未连接", connectYes: "是", connectNo: "否", connectNext: "下一句", connectNextLabel: "连接下一句", connectWouldLoop: "这样连接会循环演出刷屏，已取消本次连接", outfitHint: "粘贴 BCX 服装代码（在 BCX 衣柜里导出的服装/物品代码，LZString 或 JSON 格式，长代码也支持）；演出时应用到目标角色；应用走游戏权限校验", outfitTargetLabel: "应用目标", outfitTargetSelf: "对自己", outfitTargetTarget: "对目标角色（无目标时=自己）", outfitPreview: "服装变更", outfitBad: "服装代码无效：解析失败，这句已跳过（请确认粘贴了完整代码、无多余换行）", outfitBlocked: "服装应用失败：未成功应用任何内容，这句已跳过", outfitBlockedList: "服装应用失败：未成功应用任何内容（{0}），这句已跳过", outfitPartial: "服装部分被拦截（{0}）：其余已应用并同步", outfitApplied: "已对 {0} 应用服装", outfitAppliedSkip: "已对 {0} 应用服装（跳过 {1} 件不支持部位：{2}）", outfitAppliedClear: "已对 {0} 应用服装（清空 {1} 个代码未提及的部位）", outfitItemsModeLabel: "道具模式", outfitItemsModeAdd: "添加（只补上缺失的道具，不碰已有）", outfitItemsModeOverride: "覆盖（替换并清掉代码里没有的道具）", outfitTargetGone: "目标角色已不在房间，服装未应用", outfitModeLabel: "应用范围", outfitBodyLabel: "身体", outfitClothesLabel: "服装", outfitItemsLabel: "道具", outfitCosLabel: "cos道具", outfitLocksLabel: "应用锁", outfitLocksHint: "勾选后代码里的锁/束缚属性会应用（对别人时走游戏权限校验，锁归属记为你）；不勾选则只换物品不带锁", outfitPickLabel: "道具部位", outfitPickBtn: "选择部位…", outfitGroupsAll: "全部组", outfitGroupsN: "已选 {0} 组", outfitWinTitle: "选择要应用的道具部位", outfitWinHint: "橙色 = 代码里有道具的部位，绿色 = 要应用的部位，点击切换；添加模式只有代码里有的部位可点，覆盖模式所有格子可点（选中但代码里没有道具的格子会被清空）", outfitNone: "按当前设置没有可应用的内容，这句已跳过", outfitNeedNode: "请先选中一个服装道具变更节点", outfitAll: "全选", outfitNoneBtn: "清空", nodeDelay: "延时（秒，0=立刻）", nodeText: "台词文本", nodeEnabled: "启用该句", insertNick: "插入我的昵称的占位符", insertTarget: "插入对方的昵称的占位符", insertTime: "插入时间判定", nickHint: "演出时 <nick> 换成我的昵称、<target> 换成对方的昵称（没有对方时=我自己）", timeRuleSection: "时间判定台词（<time>）", timeRuleHint: "从上到下匹配，<time>将会替换成第一个满足条件的备选演出句；没有命中 → 替换为空。时间用 24 小时制；时区可填 local、UTC+8、UTC-5:30，或 Asia/Shanghai、America/New_York 等；支持跨午夜（如 22:00~02:00）", timeRuleFrom: "开始时间", timeRuleTo: "结束时间", timeRuleTz: "时区", timeRuleText: "替换台词", timeRuleTextPh: "时间判定的备选演出句", timeRuleAdd: "添加时间段", insAfter: "在下方插入", insBefore: "在上方插入", moveUp: "上移", moveDown: "下移", delNode: "删除", delNodeConfirm: "确认删除", nodeHint: "点流程里的节点卡片编辑台词；没有台词时点「在下方插入」添加。", triggerLabel: "触发", delayNow: "立刻", delayChip: "{0}秒", addNode: "+ 添加台词", nodeEmpty: "还没有台词：点「+ 添加台词」写第一句", noScript: "还没有剧本：点工具栏「新建剧本」", selectNodeFirst: "先在中间流程里点一个节点", statusIdle: "待机中：在聊天里说出触发关键词，演出自动开始", statusRunning: "▶ 演出中：「{0}」 第 {1}/{2} 句 · 下一句 {3}s", statusQueued: "排队中：还有 {0} 个剧本待演", importTitle: "导入剧本", importHint: "粘贴导出的 JSON 文本，或选择 .txt 文件；与现有剧本合并（同 id 覆盖）", importFromText: "从文本导入", importFile: "选择文件", importClose: "关闭", toastStarted: "▶ 演出开始：「{0}」共 {1} 句", toastFinished: "演出结束：「{0}」", saveQuota: "数据保存失败：浏览器存储空间已满（localStorage 配额），剧本只存在内存、刷新/重登会丢失！请清理其它插件的大数据（如 LSCG 备份），控制台 PlayScript.StorageInfo() 可查占用、PlayScript.CleanLSCGBackups() 可清 LSCG 备份", cloudTooBig: "剧本数据较大（约 {0}KB 压缩后，上限 {1}KB）未上传云端：只保存在本机，换电脑需用「导出/导入」迁移", bioTooBig: "BIO 备份放不下（玩家描述上限 10000 字符）：勾选的剧本太多，请减少勾选「在线备份存储」的剧本", cloudCapLabel: "在线备份上限（KB，0=不限）", cloudCheckBtn: "在线存储空间", cloudInfo: "在线存储：账号数据共 {0}K / 上限 {5}K，可用 {1}K；本插件占用 {2}K（上限 {3}K）；ExtensionSettings {4}K", cloudInfoNone: "在线存储：暂未收到登录账号数据（登录后重试）；本插件上限 {0}K", cloudInfoPartial: "在线存储：账号数据总量会在进行一次重连后补全，或请在下次登录前加载本插件即可登录后补全；本插件占用 {0}K（上限 {1}K）；ExtensionSettings {2}K；服务器上限 {3}K", cloudBioLabel: "在线备份存储（编码存 BIO，不进默认在线存储）", cloudBioHint: "勾选后该剧本会以编码形式存进玩家描述（BIO，上限 10000 字符），不再占用默认在线存储空间；和存进在线存储空间里的剧本一样不会因为换设备登录而丢失。取消勾选时，剧本确认存进在线存储空间后才会从 BIO 里删掉", bioUncheckHold: "云端未确认（未登录或超上限）：刚取消勾选的剧本暂留在 BIO，确认存进在线存储空间后会自动移出", toastAborted: "⏹ 演出已中止：「{0}」", toastAbortRoom: "已离开聊天室，演出中止", toastBusy: "演出进行中，本次触发已忽略（剧本设置里可改成排队/重新开始）", toastQueued: "已排队：「{0}」（第 {1} 位）", toastQueueFull: "排队已满（8 个），本次触发丢弃", toastCooldown: "冷却中：约 {0} 秒后可再次触发", toastNeedRoom: "需要先进入聊天室", toastNothing: "当前没有正在进行的演出", toastExported: "已导出：复制到剪贴板并下载文件", toastExportFail: "导出失败（剪贴板不可用）", toastImported: "导入完成：新增 {0} 个，覆盖 {1} 个", toastImportBad: "导入失败：不是有效的 PlayScript 数据", toastCleared: "已清空全部剧本", toastNoSelScript: "请先在左侧选择一个剧本", toastMaxScripts: "剧本数量已达上限（{0} 个）", toastMaxNodes: "台词数量已达上限（{0} 句）", toastEmptyText: "这句台词是空的，不会被发送", toastEmptyNodes: "「{0}」没有可发送的台词（空台词/已停用）", toastKwEmpty: "请先填写触发关键词", toastNodeAdded: "已添加台词", toastScriptAdded: "已新建剧本", toastScriptDeleted: "剧本已删除", toastDup: "已复制剧本", toastDataLoaded: "已加载账号剧本数据（{0} 个剧本）", }, en: { title: "PlayScript Stage", tips: "Drag title to move · drag corner to resize · click a node card to edit · say the keyword in chat to play", cmd: "Type /ps in chat to toggle", scriptsHeader: "Scripts", scriptsHint: "Select a script on the left", newScript: "New script", testRun: "Manual trigger", stop: "Stop show", exportBtn: "Export", exportOneBtn: "Export this script", importBtn: "Import", clearAll: "Clear all", clearAllConfirm: "Confirm clear", langBtn: "中文", minimizeTitle: "Minimize (click title bar to restore)", closeTitle: "Close window", resizeTitle: "Drag to resize", editorResizeTitle: "Drag to resize the settings panel", scriptSettings: "Script settings", scriptName: "Script name", keyword: "Trigger keyword", keywordPh: "e.g. summon", triggerSettings: "Trigger settings", chipOther: "Other", editorHint: "Click the ▶ trigger block at the top of the canvas to edit triggers; click a line card to edit it; click the script row on the left to come back here", triggerTargetHint: "Target a player: say \"keyword, name-or-ID\" or the reverse (e.g. \"午时已到，瓦提娜\", \"天降正义 安缇妮\", \"255225，英雄不朽\", \"Vatina 在这停顿\") and <target> points to that player (names are case-insensitive). If the script uses <target> or an outfit node targeting others and no such player exists, the script won't run and a notice is shown; scripts without targets trigger normally. Anti-misfire: the keyword must be surrounded by spaces or punctuation — touching other characters won't trigger", triggerNoTarget: "Target not found: no player \"{0}\" in the room — trigger skipped", matchMode: "Match mode", matchContains: "Contains", matchExact: "Exact", caseSensitive: "Case sensitive", triggerWho: "Triggered by", whoSelf: "Only me", whoAnyone: "Anyone", whoOthers: "Others", cooldown: "Cooldown (sec, 0=off)", onRetrigger: "Re-trigger while playing", retryIgnore: "Ignore", retryRestart: "Restart", retryQueue: "Queue", enabled: "Enabled", actTrigger: "Action trigger (game action panel)", actHint: "This custom action appears in the action panel when clicking a body part; clicking it triggers this script (same as manual trigger)", actName: "Action name", actNamePh: "e.g. Improv show", actGroup: "Body part", actPick: "Pick part…", actWinTitle: "Pick body part", actCurrent: "Current: {0}", actAny: "Any part", actDone: "Done", actTarget: "Target", actTargetSelf: "Self only (visible on own panel)", actTargetOthers: "Others only (visible on others' panels)", otherTrigger: "Other triggers", otherHint: "Auto-play on room enter; also triggers when the target player enters the room or is already in a room you enter (<target> = their nickname)", roomTrigger: "Trigger on room enter", roomTriggerOnReconnect: "Also trigger when reconnecting back into the room", timeTrigger: "Trigger by time", timeTriggerHint: "Add one or more time ranges + timezones; the script fires when the current time matches (checked every 15s, only while in a chat room)", playerTrigger: "Trigger on seeing player", playerTargetId: "Player IDs (multiple ok)", orgasmTrigger: "Trigger on orgasm", orgasmDenyTrigger: "Trigger on denied orgasm", orgasmHint: "Fires when you orgasm; the deny trigger fires only on a ruined orgasm (e.g. orgasm lock deny mode).", interactTrigger: "Trigger on others' interactions", interactHint: "Fires when another player performs a selected activity on you: pick body parts first, then pick which activities trigger (green cell = part has selections)", interactPick: "Pick parts & actions…", interactGroupsN: "{0} parts / {1} actions selected", interactWinTitle: "Pick interaction trigger parts", interactActWinTitle: "Pick activities for {0}", interactActHint: "Click an activity to toggle it (green = will trigger); multi-select", interactEmpty: "No selectable activities for this part", interactBack: "Back to parts", interactCurrent: "{0} actions selected", dupScript: "Duplicate", delScript: "Delete script", delScriptConfirm: "Confirm delete", nodeEdit: "Line node", nodeType: "Message type", typeChat: "Chat", typeRp: "RP action (*)", typeNarr: "Narration (**)", typeAction: "Action", typeLeave: "Leave room", leaveHint: "This node makes you leave the current room (no message is sent). Usually placed last; following lines will not be sent.", leavePreview: "Leaves the current room (no message)", forceLeave: "Force leave", forceLeaveHint: "Bypasses the game's leave restrictions (leashed / can't walk / prison / locked room) and slow-leave, leaving immediately", typeOutfit: "Outfit change", typeJudge: "Judge", judgeEdit: "Judge settings", judgeType: "Judge type", judgeCoin: "Coin (heads/tails)", judgeDice: "Dice", judgeCoinYesIs: "Side that means yes", judgeCoinHeads: "Heads → yes", judgeCoinTails: "Tails → yes", judgeDiceExpr: "Dice roll", judgeDiceRule: "Rule", judgeDiceThreshold: "Threshold", judgeGte: "≥ threshold → yes", judgeLte: "≤ threshold → yes", judgePlayer: "Player ID", judgePlayerHint: "Judge the other player's numeric ID and route to matching branch; non-matches go to Other players. Separate multiple IDs with commas/spaces", judgePlayerIdsPh: "e.g. 12345, 67890", judgePlayerElse: "Other players", judgePlayerBranchPh: "Branch {0}", judgeRelation: "Relationship", judgeRelationHint: "Judge the other player's relationship to you, routed in fixed order: Owner → Lover → Whitelist → Friend → No relationship", relOwner: "Owner", relLover: "Lover", relWhite: "Whitelist", relFriend: "Friend", relNone: "None", judgeShowResult: "Show the result", judgeLineType: "Result line type", judgeDefaultLine: "Result: <roll>", judgeYesPh: "(Yes branch — click to edit)", judgeNoPh: "(No branch — click to edit)", judgeDiceBranches: "Result branches (max 5)", judgeDiceBranchesHint: "Route by roll result: 1~10 → branch 1, 11~20 → branch 2, etc. Adding a branch auto-creates and connects a line node", judgeBranchAdd: "Add branch", judgeBranchMax: "At most 5 result branches", judgeBranchPh: "(Branch {0} — click to edit)", insertRoll: "Insert result tag", addJudge: "+ Add judge", connectBtn: "Connect…", connectDisconnect: "Disconnect", connectHint: "Connect to a node as the next line", connectWinTitle: "Connect to: {0}", connectNone: "Not connected", connectYes: "Yes", connectNo: "No", connectNext: "Next", connectNextLabel: "Connect next", connectWouldLoop: "This connection would loop the show forever; it was cancelled", outfitHint: "Paste a BCX clothing code (exported from the BCX wardrobe, LZString or JSON format); applied to the target at play time with game permission checks", outfitTargetLabel: "Apply to", outfitTargetSelf: "Self", outfitTargetTarget: "Target (falls back to self)", outfitPreview: "Outfit", outfitBad: "Invalid outfit code: parse failed, line skipped (make sure the full code was pasted, no stray line breaks)", outfitBlocked: "Outfit apply failed: nothing was applied, line skipped", outfitBlockedList: "Outfit not applied: nothing could be applied ({0}), line skipped", outfitPartial: "Outfit partially blocked ({0}): the rest applied and synced", outfitApplied: "Outfit applied to {0}", outfitAppliedSkip: "Outfit applied to {0} ({1} unsupported part(s) skipped: {2})", outfitAppliedClear: "Outfit applied to {0} ({1} slot(s) not in code cleared)", outfitItemsModeLabel: "Items mode", outfitItemsModeAdd: "Add (only fill empty slots, leave existing)", outfitItemsModeOverride: "Override (replace and remove items not in code)", outfitTargetGone: "Target has left the room, outfit not applied", outfitModeLabel: "Apply scope", outfitBodyLabel: "Body", outfitClothesLabel: "Clothes", outfitItemsLabel: "Items", outfitCosLabel: "Cosplay items", outfitLocksLabel: "Apply locks", outfitLocksHint: "When checked, lock/restraint properties from the code are applied (game permission checks apply for others; lock assigned to you); unchecked = items only, no locks", outfitPickLabel: "Item parts", outfitPickBtn: "Pick parts…", outfitGroupsAll: "All groups", outfitGroupsN: "{0} selected", outfitWinTitle: "Pick item parts to apply", outfitWinHint: "Orange = parts with items in the code, green = parts to apply; click to toggle. Add mode: only code parts are clickable. Override mode: every slot is clickable (a selected slot without a code item will be cleared)", outfitNone: "Nothing to apply with the current settings, line skipped", outfitNeedNode: "Select an outfit node first", outfitAll: "All", outfitNoneBtn: "None", nodeDelay: "Delay (sec, 0=immediate)", nodeText: "Line text", nodeEnabled: "Enable this line", insertNick: "Insert my nickname tag", insertTarget: "Insert their nickname tag", insertTime: "Insert time tag", nickHint: "<nick> = my nickname, <target> = the other player's nickname (fallback = me)", timeRuleSection: "Time-based line (<time>)", timeRuleHint: "Matched top to bottom: <time> is replaced with the first matching alternative line; no match → empty. 24-hour times. Timezone can be local, UTC+8, UTC-5:30, or Asia/Shanghai, America/New_York, etc. Cross-midnight ranges (e.g. 22:00~02:00) are supported", timeRuleFrom: "From", timeRuleTo: "To", timeRuleTz: "Timezone", timeRuleText: "Replacement text", timeRuleTextPh: "Alternative line for this time slot", timeRuleAdd: "Add time range", insAfter: "Insert below", insBefore: "Insert above", moveUp: "Move up", moveDown: "Move down", delNode: "Delete", delNodeConfirm: "Confirm delete", nodeHint: "Click a node card in the flow to edit its line. If empty, use Insert below.", triggerLabel: "Trigger", delayNow: "now", delayChip: "{0}s", addNode: "+ Add line", nodeEmpty: "No lines yet. Click + Add line to write the first one.", noScript: "No scripts yet. Click New script in the toolbar.", selectNodeFirst: "Select a node in the flow first", statusIdle: "Idle: say a trigger keyword in chat to start the show", statusRunning: "▶ Playing: {0} — line {1}/{2}, next in {3}s", statusQueued: "Queued: {0} script(s) waiting", importTitle: "Import scripts", importHint: "Paste exported JSON or choose a .txt file; merges with existing scripts (same id = replace)", importFromText: "Import from text", importFile: "Choose file", importClose: "Close", toastStarted: "▶ Started: {0} ({1} lines)", toastFinished: "Finished: {0}", saveQuota: "Save failed: browser storage quota exceeded — scripts exist only in memory and will be lost on reload! Clean up large data from other plugins (e.g. LSCG backups); console: PlayScript.StorageInfo() shows usage, PlayScript.CleanLSCGBackups() removes LSCG backups", cloudTooBig: "Script data is large (~{0}KB compressed, cap {1}KB) and was not uploaded to the account: stored locally only — use Export/Import to move it", bioTooBig: "BIO backup doesn't fit (profile description limit 10000 chars): too many scripts with \"online backup\" checked — uncheck some", cloudCapLabel: "Online backup cap (KB, 0=unlimited)", cloudCheckBtn: "Online storage", cloudInfo: "Online storage: account data {0}K / {5}K limit, {1}K available; this plugin uses {2}K (cap {3}K); ExtensionSettings {4}K", cloudInfoNone: "Online storage: no account data received yet (try after login); this plugin's cap is {0}K", cloudInfoPartial: "Online storage: account total will be filled in after a reconnect, or load this plugin before your next login so it can be captured after login; this plugin uses {0}K (cap {1}K); ExtensionSettings {2}K; server limit {3}K", cloudBioLabel: "Online backup (encoded into BIO, not in default online storage)", cloudBioHint: "When checked, this script is stored encoded in your profile description (BIO, 10000 char limit) instead of the default online storage; like scripts in the online storage, it won't be lost when logging in on another device. When unchecked, it is removed from the BIO only after it is confirmed saved to the online storage", bioUncheckHold: "Cloud not confirmed (not logged in or over the cap): the just-unchecked scripts stay in the BIO and will be removed automatically once confirmed in the online storage", toastAborted: "⏹ Aborted: {0}", toastAbortRoom: "Left the chat room, show aborted", toastBusy: "A show is running, trigger ignored (set Queue/Restart in script settings)", toastQueued: "Queued: {0} (#{1})", toastQueueFull: "Queue full (8), trigger dropped", toastCooldown: "Cooldown: about {0}s left", toastNeedRoom: "Enter a chat room first", toastNothing: "No show is running", toastExported: "Exported: copied to clipboard and downloaded", toastExportFail: "Export failed (clipboard unavailable)", toastImported: "Imported: {0} added, {1} replaced", toastImportBad: "Import failed: not valid PlayScript data", toastCleared: "All scripts cleared", toastNoSelScript: "Select a script on the left first", toastMaxScripts: "Script limit reached ({0})", toastMaxNodes: "Line limit reached ({0})", toastEmptyText: "This line is empty and will be skipped", toastEmptyNodes: "{0} has no sendable lines (empty/disabled)", toastKwEmpty: "Fill in the trigger keyword first", toastNodeAdded: "Line added", toastScriptAdded: "Script created", toastScriptDeleted: "Script deleted", toastDup: "Script duplicated", toastDataLoaded: "Account data loaded ({0} scripts)", }, }; function PST(key, ...args) { let t = (typeof PSUI !== "undefined" && PSUI && PSUI.lang === "en" && PSText.en && PSText.en[key]) ? PSText.en[key] : PSText.zh[key]; if (t === undefined) t = String(key); if (args.length) { for (let i = 0; i < args.length; i++) t = t.replace("{" + i + "}", String(args[i])); } return t; } const PSUI = { lang: "zh", open: false, el: null, titleEl: null, toolbarEl: null, bodyEl: null, btnCloud: null, cloudCapInp: null, listEl: null, flowEl: null, editorEl: null, statusEl: null, cornerEl: null, importEl: null, importTextEl: null, importFileEl: null, textareaEl: null, flowCards: [], flowToolbarEl: null, actWin: null, actOpen: false, actScriptId: null, actCanvas: null, actHover: null, actTitleEl: null, actChipEl: null, outfitWin: null, outfitOpen: false, outfitScriptId: null, outfitNodeId: null, outfitCodeGroups: [], outfitPickable: [], outfitBundle: [], outfitSel: [], outfitHover: null, outfitTitleEl: null, outfitChipEl: null, outfitCanvas: null, interactWin: null, interactOpen: false, interactScriptId: null, interactCanvas: null, interactHover: null, interactTitleEl: null, interactChipEl: null, interactActWin: null, interactActOpen: false, interactActGroup: null, interactActList: [], interactActTitleEl: null, interactActChipEl: null, interactActListEl: null, connectWin: null, connectTitleEl: null, connectListEl: null, connectScriptId: null, connectNodeId: null, connectPort: "next", dot: null, toastEl: null, toastTimer: null, win: { x: null, y: null, w: 1000, h: 640, minimized: false, editorW: 340 }, dotPos: null, selScriptId: null, selNodeId: null, selTrigger: false, drag: null, resize: null, dotDrag: null, nodeDrag: null, armed: {}, tickId: null, focusRequest: false, keyBound: false, }; function PSStyle(el, obj) { for (const k in obj) el.style[k] = obj[k]; } function PSEl(tag, style, html) { const e = document.createElement(tag); if (style) PSStyle(e, style); if (html !== undefined && html !== null) e.innerHTML = html; return e; } const PS_ACCENT = "#2ec4b6"; const PS_BG = "#1b1e2b"; const PS_PANEL = "#232838"; const PS_BORDER = "#3a4160"; const PS_TEXT = "#e8e8f0"; const PS_TEXT_DIM = "#9aa0b5"; function PSBigBtn(label, onClick, opts) { opts = opts || {}; const b = PSEl("button", { padding: "10px 16px", fontSize: "15px", fontWeight: "600", fontFamily: "sans-serif", background: opts.bg || "#3a4160", color: "#ffffff", border: "1px solid #ffffff", borderRadius: "8px", cursor: "pointer", }, PSEsc(label)); b.title = opts.title || ""; b.addEventListener("click", (ev) => { if (ev && ev.stopPropagation) ev.stopPropagation(); onClick(); }); return b; } function PSSmallBtn(label, onClick, opts) { opts = opts || {}; const b = PSEl("button", { padding: "4px 8px", fontSize: "12px", fontFamily: "sans-serif", background: opts.bg || "#2c3350", color: "#ffffff", border: "1px solid #6a7290", borderRadius: "6px", cursor: "pointer", }, PSEsc(label)); b.title = opts.title || ""; b.addEventListener("click", (ev) => { if (ev && ev.stopPropagation) ev.stopPropagation(); onClick(); }); return b; } function PSArm(key, el, confirmLabel, action) { if (PSUI.armed[key]) { const a = PSUI.armed[key]; clearTimeout(a.t); delete PSUI.armed[key]; action(); return; } const origText = el.textContent; const origBg = el.style.background || ""; const origBorder = el.style.borderColor || ""; el.textContent = confirmLabel; PSStyle(el, { background: "#e74c3c", borderColor: "#ffb3b3" }); const t = setTimeout(() => { delete PSUI.armed[key]; el.textContent = origText; PSStyle(el, { background: origBg, borderColor: origBorder }); }, 3000); PSUI.armed[key] = { t, el, origText, origBg, origBorder }; } function PSToast(msg) { if (typeof document === "undefined" || !document.body) { console.log("[PlayScript]", msg); return; } try { if (!PSUI.toastEl) { PSUI.toastEl = document.createElement("div"); PSUI.toastEl.id = "ps-toast"; PSStyle(PSUI.toastEl, { position: "fixed", left: "16px", top: "16px", zIndex: "2147483600", background: "#22283a", border: "1px solid " + PS_ACCENT, color: "#d7f7f2", borderRadius: "8px", padding: "10px 16px", fontSize: "14px", fontFamily: "sans-serif", transition: "opacity .4s", display: "none", maxWidth: "420px", pointerEvents: "none", }); document.body.appendChild(PSUI.toastEl); } PSUI.toastEl.textContent = msg; PSUI.toastEl.style.display = "block"; PSUI.toastEl.style.opacity = "1"; if (PSUI.toastTimer) clearTimeout(PSUI.toastTimer); PSUI.toastTimer = setTimeout(() => { PSUI.toastTimer = null; if (PSUI.toastEl) PSUI.toastEl.style.display = "none"; }, 2600); } catch (e) { } } function PSUIDotClamp(x, y) { const vw = window.innerWidth || 1000, vh = window.innerHeight || 700; const size = 46; return { x: Math.min(Math.max(x, 4), Math.max(4, vw - size - 4)), y: Math.min(Math.max(y, 4), Math.max(4, vh - size - 4)), }; } function PSUIDotSave() { if (!PSStore.state) return; PSStore.state.ui.dotPos = PSUI.dotPos; PSStore.requestSave(); } function PSUIDotBuild() { if (PSUI.dot || typeof document === "undefined" || !document.body) return; let pos = (PSStore.state && PSStore.state.ui && PSStore.state.ui.dotPos) || null; if (!pos || typeof pos.x !== "number" || typeof pos.y !== "number") { pos = { x: (window.innerWidth || 1000) - 60, y: (window.innerHeight || 700) - 60 }; } const clamped = PSUIDotClamp(pos.x, pos.y); PSUI.dotPos = clamped; const dot = document.createElement("div"); dot.id = "ps-dot"; PSStyle(dot, { position: "fixed", left: clamped.x + "px", top: clamped.y + "px", width: "46px", height: "46px", borderRadius: "50%", background: PS_ACCENT, color: "#0b2623", border: "2px solid #ffffff", boxShadow: "0 4px 14px rgba(0,0,0,.5)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "20px", fontWeight: "bold", cursor: "pointer", zIndex: "2147483400", userSelect: "none", fontFamily: "sans-serif", }); dot.textContent = "▶"; dot.title = "PlayScript（点击开关浮窗，按住拖动）"; dot.addEventListener("mousedown", (ev) => { ev.preventDefault && ev.preventDefault(); PSUI.dotDrag = { sx: ev.clientX, sy: ev.clientY, ox: PSUI.dotPos.x, oy: PSUI.dotPos.y, moved: false }; const move = (e) => { if (!PSUI.dotDrag) return; const dx = e.clientX - PSUI.dotDrag.sx, dy = e.clientY - PSUI.dotDrag.sy; if (Math.abs(dx) + Math.abs(dy) > 5) PSUI.dotDrag.moved = true; const c = PSUIDotClamp(PSUI.dotDrag.ox + dx, PSUI.dotDrag.oy + dy); PSUI.dotPos = c; PSStyle(dot, { left: c.x + "px", top: c.y + "px" }); }; const up = () => { document.removeEventListener("mousemove", move); document.removeEventListener("mouseup", up); if (!PSUI.dotDrag) return; const moved = PSUI.dotDrag.moved; PSUI.dotDrag = null; if (moved) PSUIDotSave(); else PlayScriptToggle(); }; document.addEventListener("mousemove", move); document.addEventListener("mouseup", up); }); document.body.appendChild(dot); PSUI.dot = dot; } function PSUIWinClamp() { const vw = window.innerWidth || 1000, vh = window.innerHeight || 700; PSUI.win.w = PSClamp(PSUI.win.w, PS_MIN_WIN_W, Math.max(PS_MIN_WIN_W, vw - 8)); PSUI.win.h = PSClamp(PSUI.win.h, PS_MIN_WIN_H, Math.max(PS_MIN_WIN_H, vh - 8)); PSUI.win.x = PSClamp(PSUI.win.x ?? (vw - PSUI.win.w) / 2, 4, Math.max(4, vw - PSUI.win.w - 4)); PSUI.win.y = PSClamp(PSUI.win.y ?? (vh - PSUI.win.h) / 2, 4, Math.max(4, vh - PSUI.win.h - 4)); } function PSUIWinSave() { if (!PSStore.state) return; PSStore.state.ui.win = { x: PSUI.win.x, y: PSUI.win.y, w: PSUI.win.w, h: PSUI.win.h, minimized: PSUI.win.minimized, editorW: PSUI.win.editorW }; PSStore.requestSave(); } function PSUIRoot() { if (PSUI.el || typeof document === "undefined" || !document.body) return PSUI.el; if (PSStore.state && PSStore.state.ui && PSStore.state.ui.win) { const g = PSStore.state.ui.win; if (typeof g.x === "number" && typeof g.y === "number") PSUI.win.x = g.x; if (typeof g.y === "number") PSUI.win.y = g.y; if (typeof g.w === "number") PSUI.win.w = g.w; if (typeof g.h === "number") PSUI.win.h = g.h; PSUI.win.minimized = !!g.minimized; if (typeof g.editorW === "number") PSUI.win.editorW = g.editorW; } PSUIWinClamp(); const el = document.createElement("div"); el.id = "ps-win"; PSStyle(el, { position: "fixed", left: PSUI.win.x + "px", top: PSUI.win.y + "px", width: PSUI.win.w + "px", height: PSUI.win.h + "px", zIndex: "2147483500", background: PS_BG, border: "2px solid #ffffff", borderRadius: "10px", boxShadow: "0 8px 40px rgba(0,0,0,.6)", display: "none", color: PS_TEXT, fontFamily: "sans-serif", fontSize: "14px", overflow: "hidden", }); PSUI.el = el; const title = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "8px 12px", background: "#141826", borderBottom: "1px solid " + PS_BORDER, cursor: "move", userSelect: "none", }); title.appendChild(PSEl("span", { color: PS_ACCENT, fontSize: "18px" }, "▶")); PSUI.titleText = PSEl("span", { fontWeight: "700", fontSize: "15px", flex: "1" }, PSEsc(PST("title"))); title.appendChild(PSUI.titleText); const minBtn = PSSmallBtn("—", () => PSUIMinimizeToggle(), { title: PST("minimizeTitle") }); const closeBtn = PSSmallBtn("✕", PlayScriptClose, { title: PST("closeTitle") }); title.appendChild(minBtn); title.appendChild(closeBtn); title.addEventListener("mousedown", (ev) => { if (ev.target === minBtn || ev.target === closeBtn) return; PSUI.drag = { sx: ev.clientX, sy: ev.clientY, ox: PSUI.win.x, oy: PSUI.win.y }; const move = (e) => { if (!PSUI.drag) return; PSUI.win.x = PSClamp(PSUI.drag.ox + e.clientX - PSUI.drag.sx, 4, Math.max(4, (window.innerWidth || 1000) - PSUI.win.w - 4)); PSUI.win.y = PSClamp(PSUI.drag.oy + e.clientY - PSUI.drag.sy, 4, Math.max(4, (window.innerHeight || 700) - PSUI.win.h - 4)); PSStyle(el, { left: PSUI.win.x + "px", top: PSUI.win.y + "px" }); }; const up = () => { document.removeEventListener("mousemove", move); document.removeEventListener("mouseup", up); PSUI.drag = null; PSUIWinSave(); }; document.addEventListener("mousemove", move); document.addEventListener("mouseup", up); }); el.appendChild(title); const bar = PSEl("div", { display: "flex", flexWrap: "wrap", gap: "8px", alignItems: "center", padding: "8px 12px", background: "#20243a", borderBottom: "1px solid " + PS_BORDER, }); PSUI.langBtnEl = PSBigBtn(PST("langBtn"), PSUILangToggle); PSUI.btnNew = PSBigBtn(PST("newScript"), PSUINewScript); PSUI.btnTest = PSBigBtn(PST("testRun"), PSUITestRun, { bg: "#2c7a70" }); PSUI.btnStop = PSBigBtn(PST("stop"), PSStop); PSUI.btnExport = PSBigBtn(PST("exportBtn"), PSUIExport); PSUI.btnImport = PSBigBtn(PST("importBtn"), PSUIImportToggle); const clearBtn = PSBigBtn(PST("clearAll"), () => PSArm("clearall", clearBtn, PST("clearAllConfirm"), () => { PSClearAll(); PSUIRenderAll(); }), { bg: "#7a2c3a" }); PSUI.btnClear = clearBtn; bar.appendChild(PSUI.btnNew); bar.appendChild(PSUI.btnTest); bar.appendChild(PSUI.btnStop); bar.appendChild(PSUI.btnExport); bar.appendChild(PSUI.btnImport); bar.appendChild(clearBtn); bar.appendChild(PSUI.langBtnEl); PSUI.btnCloud = PSBigBtn(PST("cloudCheckBtn"), () => { const info = PSCloudInfo(); const loggedIn = typeof Player !== "undefined" && Player && typeof Player.CharacterID === "string" && Player.CharacterID !== ""; if (info.used == null) { if (!loggedIn) { PSToast(PST("cloudInfoNone", info.capKB)); return; } PSToast(PST("cloudInfoPartial", PSByteToKB(info.self), info.capKB, PSByteToKB(info.extensionBytes), PSByteToKB(info.limit))); try { console.log("[PlayScript] 在线存储（部分）:", JSON.stringify(info)); } catch (e) { } return; } PSToast(PST("cloudInfo", PSByteToKB(info.used), PSByteToKB(info.available), PSByteToKB(info.self), info.capKB, PSByteToKB(info.extensionBytes), PSByteToKB(info.limit))); try { console.log("[PlayScript] 在线存储:", JSON.stringify(info)); } catch (e) { } }); bar.appendChild(PSUI.btnCloud); const capWrap = PSEl("span", { display: "inline-flex", alignItems: "center", gap: "4px", fontSize: "12px", color: PS_TEXT_DIM }); capWrap.appendChild(PSEl("span", {}, PSEsc(PST("cloudCapLabel")))); PSUI.cloudCapInp = document.createElement("input"); PSUI.cloudCapInp.type = "number"; PSUI.cloudCapInp.min = "0"; PSUI.cloudCapInp.step = "1"; PSUI.cloudCapInp.value = String(PSStore.cloudCapKB()); PSStyle(PSUI.cloudCapInp, { width: "56px", padding: "6px 6px", fontSize: "12px", borderRadius: "6px", background: "#10141f", color: PS_TEXT, border: "1px solid " + PS_BORDER, fontFamily: "sans-serif", }); PSUI.cloudCapInp.addEventListener("change", () => { const n = Number(PSUI.cloudCapInp.value); PSStore.state.cloudCapKB = (isFinite(n) && n >= 0) ? n : PS_CLOUD_DEFAULT_CAP_KB; PSStore.requestSave(); PSUIRenderAll(); }); capWrap.appendChild(PSUI.cloudCapInp); bar.appendChild(capWrap); PSUI.toolbarEl = bar; el.appendChild(bar); const body = PSEl("div", { display: "flex", flex: "1", minHeight: "0", position: "absolute", left: "0", right: "0", top: "104px", bottom: "40px", }); PSUI.listEl = PSEl("div", { width: "224px", minWidth: "224px", overflowY: "auto", background: "#181c2a", borderRight: "1px solid " + PS_BORDER, padding: "8px" }); PSUI.flowEl = PSEl("div", { flex: "1", minWidth: "0", overflowY: "auto", overflowX: "auto", padding: "10px 14px" }); const editorW = PSClamp(Number(PSUI.win.editorW) || 340, 300, 900); PSUI.win.editorW = editorW; PSUI.editorEl = PSEl("div", { width: editorW + "px", minWidth: editorW + "px", overflowY: "auto", background: "#181c2a", borderLeft: "1px solid " + PS_BORDER, padding: "10px" }); const flowCol = PSEl("div", { flex: "1", minWidth: "0", display: "flex", flexDirection: "column", minHeight: "0" }); PSUI.flowToolbarEl = PSEl("div", { display: "flex", gap: "8px", padding: "8px 14px", borderBottom: "1px solid " + PS_BORDER, background: "#141826", flexShrink: "0" }); flowCol.appendChild(PSUI.flowToolbarEl); flowCol.appendChild(PSUI.flowEl); body.appendChild(PSUI.listEl); body.appendChild(flowCol); const editHandle = PSEl("div", { width: "6px", minWidth: "6px", cursor: "ew-resize", background: "#0f1320", borderLeft: "1px solid " + PS_BORDER, borderRight: "1px solid " + PS_BORDER }); editHandle.title = PST("editorResizeTitle"); editHandle.addEventListener("mousedown", (ev) => { ev.preventDefault && ev.preventDefault(); ev.stopPropagation && ev.stopPropagation(); const sx = ev.clientX, sw = PSUI.win.editorW; const move = (e) => { const w = PSClamp(sw + (sx - e.clientX), 300, 900); PSUI.win.editorW = w; PSStyle(PSUI.editorEl, { width: w + "px", minWidth: w + "px" }); }; const up = () => { document.removeEventListener("mousemove", move); document.removeEventListener("mouseup", up); PSUIWinSave(); }; document.addEventListener("mousemove", move); document.addEventListener("mouseup", up); }); body.appendChild(editHandle); body.appendChild(PSUI.editorEl); PSUI.bodyEl = body; el.appendChild(body); const corner = PSEl("div", { position: "absolute", right: "2px", bottom: "2px", width: "18px", height: "18px", cursor: "nwse-resize", zIndex: "6", borderRight: "2px solid #8891b8", borderBottom: "2px solid #8891b8", }); corner.title = PST("resizeTitle"); corner.addEventListener("mousedown", (ev) => { ev.preventDefault && ev.preventDefault(); PSUI.resize = { sx: ev.clientX, sy: ev.clientY, ow: PSUI.win.w, oh: PSUI.win.h }; const move = (e) => { if (!PSUI.resize) return; PSUI.win.w = PSClamp(PSUI.resize.ow + e.clientX - PSUI.resize.sx, PS_MIN_WIN_W, Math.max(PS_MIN_WIN_W, (window.innerWidth || 1000) - 8)); PSUI.win.h = PSClamp(PSUI.resize.oh + e.clientY - PSUI.resize.sy, PS_MIN_WIN_H, Math.max(PS_MIN_WIN_H, (window.innerHeight || 700) - 8)); PSStyle(el, { width: PSUI.win.w + "px", height: PSUI.win.h + "px" }); }; const up = () => { document.removeEventListener("mousemove", move); document.removeEventListener("mouseup", up); PSUI.resize = null; PSUIWinSave(); }; document.addEventListener("mousemove", move); document.addEventListener("mouseup", up); }); el.appendChild(corner); PSUI.cornerEl = corner; PSUI.statusEl = PSEl("div", { position: "absolute", left: "0", right: "0", bottom: "0", padding: "8px 12px", background: "#141826", borderTop: "1px solid " + PS_BORDER, fontSize: "13px", color: "#9fd8d0", }); el.appendChild(PSUI.statusEl); PSUIImportBuild(el); if (!PSUI.keyBound && typeof document.addEventListener === "function") { PSUI.keyBound = true; document.addEventListener("keydown", (e) => { if (e.key === "Escape") { if (PSUI.interactActOpen) PSInteractActClose(); else if (PSUI.interactOpen) PSInteractWinClose(); else if (PSUI.outfitOpen) PSUIOutfitWinClose(); else if (PSUI.actOpen) PSUIActWinClose(); else if (PSUI.open) PlayScriptClose(); } }); } document.body.appendChild(el); return el; } function PSUIMinimizeToggle() { PSUI.win.minimized = !PSUI.win.minimized; applyMin(); PSUIWinSave(); function applyMin() { if (!PSUI.bodyEl) return; PSUI.bodyEl.style.display = PSUI.win.minimized ? "none" : "flex"; PSUI.toolbarEl.style.display = PSUI.win.minimized ? "none" : "flex"; PSUI.statusEl.style.display = PSUI.win.minimized ? "none" : "block"; PSUI.cornerEl.style.display = PSUI.win.minimized ? "none" : "block"; if (PSUI.el) PSUI.el.style.height = PSUI.win.minimized ? "auto" : PSUI.win.h + "px"; } } function PSUIListBuild() { const box = PSUI.listEl; if (!box) return; box.innerHTML = ""; const head = PSEl("div", { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "4px 4px 8px", borderBottom: "1px solid " + PS_BORDER, marginBottom: "8px" }); head.appendChild(PSEl("span", { fontWeight: "700", fontSize: "15px" }, PSEsc(PST("scriptsHeader")))); head.appendChild(PSEl("span", { color: PS_TEXT_DIM, fontSize: "12px" }, String(PSStore.state.scripts.length))); box.appendChild(head); if (!PSStore.state.scripts.length) { box.appendChild(PSEl("div", { color: PS_TEXT_DIM, padding: "12px 6px" }, PSEsc(PST("noScript")))); return; } for (const sc of PSStore.state.scripts) { const selected = sc.id === PSUI.selScriptId; const running = PSActive && PSActive.scriptId === sc.id; const row = PSEl("div", { display: "flex", alignItems: "center", gap: "6px", padding: "8px 8px", marginBottom: "6px", borderRadius: "8px", background: selected ? "#2c3452" : "#20263a", border: selected ? "1px solid " + PS_ACCENT : "1px solid " + PS_BORDER, cursor: "pointer", }); row.title = PSEsc(sc.name); const cb = document.createElement("input"); cb.type = "checkbox"; cb.checked = sc.enabled !== false; cb.addEventListener("change", () => { PSUpdateScript(sc.id, { enabled: cb.checked }); PSUIRenderAll(); }); cb.addEventListener("click", (ev) => { if (ev && ev.stopPropagation) ev.stopPropagation(); }); row.appendChild(cb); if (running) row.appendChild(PSEl("span", { color: PS_ACCENT, fontSize: "12px" }, "▶")); const mid = PSEl("div", { flex: "1", minWidth: "0" }); const nameEl = PSEl("div", { fontWeight: "600", fontSize: "13px", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, PSEsc(sc.name)); const kwEl = PSEl("div", { color: PS_TEXT_DIM, fontSize: "12px", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, PSEsc((sc.keyword || ""))); mid.appendChild(nameEl); mid.appendChild(kwEl); row.appendChild(mid); row.appendChild(PSEl("span", { color: PS_TEXT_DIM, fontSize: "11px" }, String(sc.nodes.length))); row.addEventListener("click", () => { PSUI.selScriptId = sc.id; PSUI.selNodeId = null; PSUI.selTrigger = false; PSUIRenderAll(); }); box.appendChild(row); } } function PSUINodeIcon(type) { const m = PSTypeMeta[type] || PSTypeMeta.chat; return m.icon; } function PSUINodeColor(type) { const m = PSTypeMeta[type] || PSTypeMeta.chat; return m.color; } function PSUITypeLabel(type) { return PST({ chat: "typeChat", rp: "typeRp", narr: "typeNarr", action: "typeAction", leave: "typeLeave", outfit: "typeOutfit", judge: "typeJudge" }[type] || "typeChat"); } function PSUIDelayText(delay) { const d = Number(delay) || 0; if (d <= 0) return PST("delayNow"); return PST("delayChip", (Math.round(d * 100) / 100)); } function PSUIOutgoingLine(sc, n, port) { const el = PSEl("div", { textAlign: "center", padding: "1px 0 4px", lineHeight: "1.4" }); const id = port === "yes" ? n.yesId : (port === "no" ? n.noId : n.nextId); const target = id ? PSUINodeTargetLabel(sc, id) : null; const head = port === "yes" ? PST("connectYes") : (port === "no" ? PST("connectNo") : "↓"); el.appendChild(PSEl("span", { display: "inline-block", padding: "2px 10px", fontSize: "12px", borderRadius: "10px", background: target ? "#2a2140" : "#1c1e2a", color: target ? "#d9c2ff" : "#6a7290", border: "1px solid " + (target ? "#9f7fd0" : PS_BORDER), maxWidth: "100%", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", }, PSEsc(head + " → " + (target ? target : PST("connectNone"))))); return el; } function PSUIFlowBuild() { const box = PSUI.flowEl; if (!box) return; box.innerHTML = ""; PSUI.flowCards = []; const sc = PSFindScript(PSUI.selScriptId); if (!sc) { box.appendChild(PSEl("div", { color: PS_TEXT_DIM, padding: "24px 10px", textAlign: "center" }, PSEsc(PST("noScript") + " / " + PST("scriptsHint")))); return; } const trigSelected = !PSUI.selNodeId && PSUI.selTrigger; const trig = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "10px 12px", borderRadius: "10px", marginBottom: "4px", background: trigSelected ? "#2c3452" : "#17302e", border: "1px solid " + (trigSelected ? PS_ACCENT : "#3f8f86"), cursor: "pointer", }); trig.addEventListener("click", () => { PSUI.selNodeId = null; PSUI.selTrigger = true; PSUIRenderAll(); }); trig.appendChild(PSEl("span", { fontSize: "18px", color: PS_ACCENT }, "▶")); const trigMid = PSEl("div", { flex: "1", minWidth: "0" }); trigMid.appendChild(PSEl("div", { fontSize: "11px", color: PS_ACCENT, fontWeight: "700" }, PSEsc(PST("triggerLabel")))); const kwPreview = PSEl("div", { fontSize: "13px", color: PS_TEXT, marginTop: "3px", whiteSpace: "pre-wrap", maxHeight: "38px", overflow: "hidden", wordBreak: "break-word", }, PSEsc(sc.keyword ? PSUINodePreview(sc.keyword) : PST("keywordPh"))); trigMid.appendChild(kwPreview); trig.appendChild(trigMid); const chip = (txt, bg, fg) => PSEl("span", { display: "inline-block", padding: "2px 8px", marginLeft: "4px", fontSize: "11px", borderRadius: "8px", background: bg, color: fg, border: "1px solid " + PS_BORDER, }, PSEsc(txt)); const chipRow = PSEl("div", { display: "flex", gap: "4px", flexWrap: "wrap", justifyContent: "flex-end" }); chipRow.appendChild(chip(sc.matchMode === "exact" ? PST("matchExact") : PST("matchContains"), "#2a3150", "#c9d2f5")); chipRow.appendChild(chip(sc.triggerWho === "others" ? PST("whoOthers") : (sc.triggerWho === "anyone" ? PST("whoAnyone") : PST("whoSelf")), "#2a3150", "#c9d2f5")); if (sc.caseSensitive) chipRow.appendChild(chip("Aa", "#2a3150", "#c9d2f5")); if ((Number(sc.cooldown) || 0) > 0) chipRow.appendChild(chip(PST("delayChip", Number(sc.cooldown) || 0), "#2a3150", "#c9d2f5")); if (sc.actionEnabled) chipRow.appendChild(chip("▶", "#1d4d47", "#2ec4b6")); if (sc.roomTrigger || sc.playerTrigger || sc.orgasmTrigger || sc.orgasmDenyTrigger || sc.interactTrigger) chipRow.appendChild(chip(PST("chipOther"), "#2a3150", "#c9d2f5")); trig.appendChild(chipRow); box.appendChild(trig); if (!sc.nodes.length) { box.appendChild(PSEl("div", { color: PS_TEXT_DIM, padding: "18px 10px", textAlign: "center" }, PSEsc(PST("nodeEmpty")))); } const byId = new Map(sc.nodes.map((n) => [n.id, n])); const branchSet = PSBranchNodeSet(sc.nodes); const visited = new Set(); const idxOf = (n) => sc.nodes.indexOf(n); const connector = (n) => { const conn = PSEl("div", { display: "flex", alignItems: "center", gap: "6px", padding: "2px 0", lineHeight: "1" }); conn.appendChild(PSEl("span", { width: "22px", textAlign: "center", color: "#6a7290", fontSize: "14px" }, "↓")); conn.appendChild(PSEl("span", { display: "inline-block", padding: "2px 10px", fontSize: "12px", background: n.delay > 0 ? "#2a3150" : "#22301f", color: n.delay > 0 ? "#c9d2f5" : "#9fdc9a", border: "1px solid " + PS_BORDER, borderRadius: "10px", }, PSEsc(PSUIDelayText(n.delay)))); return conn; }; const makeCard = (n) => { const selected = n.id === PSUI.selNodeId; const card = PSEl("div", { display: "flex", alignItems: "flex-start", gap: "8px", padding: "10px 12px", borderRadius: "10px", marginBottom: "2px", maxWidth: "480px", background: selected ? "#2c3452" : "#20263a", border: "1px solid " + (selected ? PS_ACCENT : PSUINodeColor(n.type)), cursor: "pointer", opacity: n.enabled === false ? "0.55" : "1", }); card.setAttribute("data-node-id", n.id); card.addEventListener("click", () => { PSUI.selNodeId = n.id; PSUI.selTrigger = false; PSUIRenderAll(); }); const handle = PSEl("span", { cursor: "grab", color: "#6a7290", fontSize: "14px", padding: "2px 0", userSelect: "none" }, "⋮⋮"); handle.title = "拖动排序"; handle.addEventListener("mousedown", (ev) => { ev.preventDefault && ev.preventDefault(); ev.stopPropagation && ev.stopPropagation(); PSUINodeDragStart(sc.id, n.id, idxOf(n)); }); card.appendChild(handle); const mid = PSEl("div", { flex: "1", minWidth: "0" }); const label = PSEl("div", { fontSize: "11px", color: PSUINodeColor(n.type), fontWeight: "700" }, PSEsc(PSUITypeLabel(n.type))); mid.appendChild(label); const preview = PSEl("div", { fontSize: "13px", color: PS_TEXT, marginTop: "3px", whiteSpace: "pre-wrap", maxHeight: "42px", overflow: "hidden", wordBreak: "break-word", }, PSEsc(n.type === "leave" ? PST("leavePreview") : (n.type === "outfit" ? (PST("outfitPreview") + "：" + PSUINodePreview(n.text)) : (n.type === "judge" ? PSUIJudgePreview(n) : PSUINodePreview(n.text))))); mid.appendChild(preview); card.appendChild(mid); const cb = document.createElement("input"); cb.type = "checkbox"; cb.checked = n.enabled !== false; cb.title = PST("nodeEnabled"); cb.addEventListener("click", (ev) => { if (ev && ev.stopPropagation) ev.stopPropagation(); }); cb.addEventListener("change", () => { PSUpdateNode(sc.id, n.id, { enabled: cb.checked }); PSUIRenderAll(); }); card.appendChild(cb); card.appendChild(PSSmallBtn("↑", () => { PSMoveNode(sc.id, n.id, -1); PSUIRenderAll(); }, { title: PST("moveUp") })); card.appendChild(PSSmallBtn("↓", () => { PSMoveNode(sc.id, n.id, +1); PSUIRenderAll(); }, { title: PST("moveDown") })); PSUI.flowCards.push({ id: n.id, el: card, previewEl: preview }); return card; }; const makeLane = (label, color) => { const lane = PSEl("div", { flex: "0 0 auto", width: "max-content", minWidth: "240px", borderLeft: "2px solid " + color, paddingLeft: "6px" }); lane.appendChild(PSEl("div", { fontSize: "11px", color: "#d9c2ff", fontWeight: "700", marginBottom: "2px" }, PSEsc(label))); return lane; }; const renderPath = (n, lane) => { if (!n || visited.has(n.id)) return; visited.add(n.id); if (n.type === "judge") { const jBox = PSEl("div", { display: "inline-flex", flexDirection: "column", alignItems: "stretch", verticalAlign: "top" }); const card = makeCard(n); card.style.maxWidth = "none"; jBox.appendChild(card); const cols = PSEl("div", { display: "flex", gap: "8px", alignItems: "flex-start", flexWrap: "nowrap" }); let branchDefs; if (n.judgeType === "dice" && Array.isArray(n.diceBranches) && n.diceBranches.length) { branchDefs = n.diceBranches.map((b) => ({ label: b.from + "~" + b.to, nodeId: b.nodeId, color: "#4e7fb0" })); } else if (n.judgeType === "player") { const pbs = Array.isArray(n.playerBranches) ? n.playerBranches : []; branchDefs = pbs.map((b, i) => ({ label: (b.ids && b.ids.length) ? b.ids.map((x) => "#" + x).join("/") : PST("judgePlayerBranchPh", i + 1), nodeId: b.nodeId, color: "#b07f2c" })); branchDefs.push({ label: PST("judgePlayerElse"), nodeId: n.elseId, color: "#7a2c3a" }); } else if (n.judgeType === "relation") { const rbs = Array.isArray(n.relationBranches) ? n.relationBranches : []; branchDefs = rbs.map((b) => ({ label: PSRelationLabel(b.rel), nodeId: b.nodeId, color: "#2c7a9f" })); } else { branchDefs = [ { label: PST("connectYes"), nodeId: n.yesId, color: "#2c7a70" }, { label: PST("connectNo"), nodeId: n.noId, color: "#7a2c3a" }, ]; } branchDefs.forEach((bd) => { const lane = makeLane(bd.label, bd.color); cols.appendChild(lane); renderPath(byId.get(bd.nodeId), lane); }); jBox.appendChild(cols); lane.appendChild(connector(n)); lane.appendChild(jBox); } else { lane.appendChild(connector(n)); lane.appendChild(makeCard(n)); let next = n.nextId ? byId.get(n.nextId) : null; if (!next && !branchSet.has(n.id)) { const idx = idxOf(n); next = (idx >= 0 && idx < sc.nodes.length - 1) ? sc.nodes[idx + 1] : null; } renderPath(next, lane); } }; if (sc.nodes.length) renderPath(sc.nodes[0], box); sc.nodes.forEach((n) => { if (!visited.has(n.id)) renderPath(n, box); }); const addRow = PSEl("div", { display: "flex", gap: "8px", margin: "0" }); const addBtn = PSBigBtn(PST("addNode"), () => { const sc2 = PSFindScript(PSUI.selScriptId); if (!sc2) { PSToast(PST("toastNoSelScript")); return; } const sel = PSUI.selNodeId ? sc2.nodes.find((x) => x.id === PSUI.selNodeId) : null; let n = null; if (sel && sel.type !== "judge") { n = PSAddNodeAfter(sc2.id, sel.id, { type: "chat", text: "", delay: 0, enabled: true }); } else { const prev = sc2.nodes.length ? sc2.nodes[sc2.nodes.length - 1] : null; n = PSAddNode(sc2.id, { type: "chat", text: "", delay: 0, enabled: true }); if (n && prev && prev.type !== "judge") PSUpdateNode(sc2.id, prev.id, { nextId: n.id }); } if (n) { PSUI.selNodeId = n.id; PSUI.selTrigger = false; PSUI.focusRequest = true; PSUIRenderAll(); } }, { bg: "#2c7a70" }); addBtn.style.flex = "1"; addRow.appendChild(addBtn); const addJudgeBtn = PSBigBtn(PST("addJudge"), () => { const sc2 = PSFindScript(PSUI.selScriptId); if (!sc2) { PSToast(PST("toastNoSelScript")); return; } const sel = PSUI.selNodeId ? sc2.nodes.find((x) => x.id === PSUI.selNodeId) : null; const prev = sc2.nodes.length ? sc2.nodes[sc2.nodes.length - 1] : null; const j = PSAddNode(sc2.id, { type: "judge", text: PST("judgeDefaultLine"), delay: 0, enabled: true, showResult: true }); const yesN = PSAddNode(sc2.id, { type: "chat", text: PST("judgeYesPh"), delay: 0, enabled: true }); const noN = PSAddNode(sc2.id, { type: "chat", text: PST("judgeNoPh"), delay: 0, enabled: true }); PSUpdateNode(sc2.id, j.id, { yesId: yesN.id, noId: noN.id }); const anchor = (sel && sel.type !== "judge") ? sel : ((prev && prev.type !== "judge") ? prev : null); if (anchor) PSUpdateNode(sc2.id, anchor.id, { nextId: j.id }); if (j) { PSUI.selNodeId = j.id; PSUI.selTrigger = false; PSUI.focusRequest = true; PSUIRenderAll(); } }, { bg: "#5a3d7a" }); addJudgeBtn.style.flex = "1"; addRow.appendChild(addJudgeBtn); if (PSUI.flowToolbarEl) { PSUI.flowToolbarEl.innerHTML = ""; PSUI.flowToolbarEl.appendChild(addRow); } else { box.appendChild(addRow); } } function PSUINodePreview(text) { const t = String(text ?? "").replace(/\s+/g, " ").trim(); if (!t) return "(空)"; return t.length > 80 ? t.slice(0, 80) + "…" : t; } function PSUIJudgePreview(n) { if (n && n.judgeType === "relation") { const rbs = PSNormalizeRelationBranches(n.relationBranches); const parts = rbs.map((b) => PSRelationLabel(b.rel)); return "💞 关系：" + (parts.length ? parts.join("、") : "（未设置分支）"); } if (n && n.judgeType === "player") { const pbs = PSNormalizePlayerBranches(n.playerBranches); const parts = pbs.map((b, i) => (b.ids && b.ids.length) ? b.ids.map((x) => "#" + x).join("/") : PST("judgePlayerBranchPh", i + 1)); return "👤 玩家ID：" + (parts.length ? parts.join("、") : "（未设置分支）") + (n.elseId ? " · " + PST("judgePlayerElse") : ""); } if (n && n.judgeType === "dice") { if (Array.isArray(n.diceBranches) && n.diceBranches.length) { return "🎲 " + (n.diceExpr || "1d6") + " · " + n.diceBranches.length + " 个结果分支"; } const rule = n.diceRule === "lte" ? "≤" : "≥"; return "🎲 " + (n.diceExpr || "1d6") + " " + rule + " " + (n.diceThreshold || 4) + " → 是"; } return "🪙 硬币：" + ((n && n.coinYesIs === "tails") ? "反面 → 是" : "正面 → 是"); } function PSUINodeTargetLabel(sc, id) { const t = sc && Array.isArray(sc.nodes) ? sc.nodes.find((n) => n.id === id) : null; if (!t) return null; return PSUITypeLabel(t.type) + "：" + (t.type === "judge" ? PSUIJudgePreview(t) : PSUINodePreview(t.text)); } function PSUINodeDragStart(scriptId, nodeId, fromIdx) { if (typeof document.addEventListener !== "function") return; PSUI.nodeDrag = { scriptId, nodeId, fromIdx, targetId: null }; const cardEl = PSUI.flowCards.find((c) => c.id === nodeId); if (cardEl) cardEl.el.style.opacity = "0.4"; const move = (e) => { if (!PSUI.nodeDrag) return; let overId = null; try { if (typeof document.elementFromPoint === "function") { const el = document.elementFromPoint(e.clientX, e.clientY); const card = el && typeof el.closest === "function" ? el.closest("[data-node-id]") : null; if (card) overId = card.getAttribute("data-node-id"); } } catch (err) { } PSUI.flowCards.forEach((c) => { c.el.style.outline = c.id === overId ? "2px dashed " + PS_ACCENT : ""; }); PSUI.nodeDrag.targetId = overId; }; const up = () => { document.removeEventListener("mousemove", move); document.removeEventListener("mouseup", up); const d = PSUI.nodeDrag; PSUI.nodeDrag = null; PSUI.flowCards.forEach((c) => { c.el.style.outline = ""; c.el.style.opacity = "1"; }); if (d && d.targetId && d.targetId !== d.nodeId) { const sc = PSFindScript(d.scriptId); if (sc) { const toIdx = sc.nodes.findIndex((n) => n.id === d.targetId); if (toIdx >= 0 && PSMoveNodeTo(sc, d.nodeId, toIdx)) PSUIRenderAll(); } } }; document.addEventListener("mousemove", move); document.addEventListener("mouseup", up); } const PS_ACT_ALIASES = { "ItemHandheld": "ItemHands" }; function PSActBaseGroup(name) { const n = String(name || ""); if (PS_ACT_ALIASES[n]) return PS_ACT_ALIASES[n]; return n.replace(/\d+$/, ""); } function PSActZones() { const out = []; const byName = {}; if (typeof AssetGroup === "undefined" || !Array.isArray(AssetGroup)) return out; for (const g of AssetGroup) { try { if (!g || typeof g.IsItem !== "function" || !g.IsItem()) continue; } catch (e) { continue; } if (!g.Name || !Array.isArray(g.Zone) || !g.Zone.length) continue; const base = PSActBaseGroup(g.Name); if (!byName[base]) { byName[base] = { name: base, zones: [], hitZones: [] }; out.push(byName[base]); } if (g.Name === base) { for (const z of g.Zone) byName[base].zones.push(z); } for (const z of g.Zone) byName[base].hitZones.push(z); } return out.filter((e) => e.hitZones.length); } function PSActCanvasMap(cw, ch) { const s = Math.min(cw / 500, ch / 1000); return { s, ox: (cw - 500 * s) / 2, oy: (ch - 1000 * s) / 2 }; } function PSActZoneHitTest(x, y, cw, ch) { const { s, ox, oy } = PSActCanvasMap(cw, ch); const ix = (x - ox) / s, iy = (y - oy) / s; for (const g of PSActZones()) { for (const z of g.hitZones) { if (ix >= z[0] && ix <= z[0] + z[2] && iy >= z[1] && iy <= z[1] + z[3]) return g.name; } } return null; } function PSActZonesAll() { const out = []; if (typeof AssetGroup === "undefined" || !Array.isArray(AssetGroup)) return out; for (const g of AssetGroup) { try { if (!g || typeof g.IsItem !== "function" || !g.IsItem()) continue; } catch (e) { continue; } if (!g.Name || !Array.isArray(g.Zone) || !g.Zone.length) continue; out.push({ name: g.Name, zones: g.Zone.slice() }); } return out; } function PSActZoneHitTestAll(x, y, cw, ch) { const { s, ox, oy } = PSActCanvasMap(cw, ch); const ix = (x - ox) / s, iy = (y - oy) / s; for (const g of PSActZonesAll()) { for (const z of g.zones) { if (ix >= z[0] && ix <= z[0] + z[2] && iy >= z[1] && iy <= z[1] + z[3]) return g.name; } } return null; } function PSPreviewDrawChar(canvas, bundle) { const g = canvas.getContext ? canvas.getContext("2d") : null; if (!g) return false; const cw = canvas.width, ch = canvas.height; const { s, ox, oy } = PSActCanvasMap(cw, ch); g.fillStyle = "#141826"; g.fillRect(0, 0, cw, ch); let drawn = false; let temp = null; try { if (typeof CharacterCreate === "function" && typeof CharacterRefresh === "function" && typeof DrawCharacterSegment === "function") { const type = (typeof CharacterType !== "undefined" && CharacterType.SIMPLE) ? CharacterType.SIMPLE : "simple"; const family = (typeof Player !== "undefined" && Player && typeof Player.AssetFamily === "string" && Player.AssetFamily) ? Player.AssetFamily : "Female3DCG"; temp = CharacterCreate(family, type, "PlayScriptPreview"); const items = []; for (const b of (Array.isArray(bundle) ? bundle : [])) { if (!b || typeof b.Group !== "string" || typeof b.Name !== "string") continue; let it = null; try { if (typeof ServerBundledItemToAppearanceItem === "function") { it = ServerBundledItemToAppearanceItem(family, b); } if (!it && typeof AppearanceItem !== "undefined" && AppearanceItem && typeof AppearanceItem.fromAsset === "function" && typeof AssetGet === "function") { const asset = AssetGet(family, b.Group, b.Name); if (asset) it = AppearanceItem.fromAsset(asset, { difficulty: b.Difficulty, color: b.Color, craft: b.Craft, property: b.Property }); } } catch (e) { it = null; } if (it) items.push(it); } temp.Appearance = items; CharacterRefresh(temp, false, false); const seg = DrawCharacterSegment(temp, 0, 0, 500, 1000); if (seg && seg.width) { g.drawImage(seg, ox, oy, 500 * s, 1000 * s); drawn = true; } } } catch (e) { PSErr("预览绘制失败", e); } finally { if (temp && typeof CharacterDelete === "function") { try { CharacterDelete(temp); } catch (e) { } } } if (!drawn) { g.fillStyle = "#2a3150"; g.fillRect(ox, oy, 500 * s, 1000 * s); g.fillStyle = "#9aa0b5"; g.font = "24px sans-serif"; g.textAlign = "center"; g.fillText("?", cw / 2, ch / 2); } return drawn; } function PSPreviewDrawZones(canvas, colorOf, zonesList) { const g = canvas.getContext ? canvas.getContext("2d") : null; if (!g || typeof colorOf !== "function") return; const cw = canvas.width, ch = canvas.height; const { s, ox, oy } = PSActCanvasMap(cw, ch); for (const grp of (zonesList || PSActZones())) { const c = colorOf(grp.name); if (!c) continue; for (const z of grp.zones) { const rx = ox + z[0] * s, ry = oy + z[1] * s, rw = z[2] * s, rh = z[3] * s; if (c.fill) { g.fillStyle = c.fill; g.fillRect(rx, ry, rw, rh); } g.strokeStyle = c.stroke; g.lineWidth = c.width || 1.5; g.strokeRect(rx, ry, rw, rh); } } } function PSUIActSelectedGroup() { const sc = PSFindScript(PSUI.actScriptId); return (sc && sc.actionGroup) ? sc.actionGroup : "any"; } function PSUIActPreviewDraw() { const canvas = PSUI.actCanvas; if (!canvas || !PSUI.actOpen) return; let bundle = []; try { if (typeof Player !== "undefined" && Player && typeof ServerAppearanceBundle === "function") { bundle = ServerAppearanceBundle(Player) || []; } } catch (e) { } PSPreviewDrawChar(canvas, bundle); const sel = PSUIActSelectedGroup(); PSPreviewDrawZones(canvas, (name) => { if (name === sel) return { fill: "rgba(0,255,255,0.25)", stroke: "cyan", width: 2.5 }; if (name === PSUI.actHover) return { fill: "rgba(255,255,255,0.18)", stroke: "rgba(255,255,255,0.85)", width: 2 }; return { fill: "#80808040", stroke: "#808080", width: 1.5 }; }); } function PSUIActWinBuild() { if (PSUI.actWin || typeof document === "undefined" || !document.body) return; const win = document.createElement("div"); win.id = "ps-actwin"; PSStyle(win, { position: "fixed", left: "50%", top: "50%", transform: "translate(-50%, -50%)", width: "380px", zIndex: "2147483550", background: PS_BG, border: "2px solid #ffffff", borderRadius: "10px", boxShadow: "0 8px 40px rgba(0,0,0,.7)", display: "none", color: PS_TEXT, fontFamily: "sans-serif", fontSize: "14px", overflow: "hidden", }); const title = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "8px 12px", background: "#141826", borderBottom: "1px solid " + PS_BORDER, userSelect: "none", }); title.appendChild(PSEl("span", { color: PS_ACCENT, fontSize: "16px" }, "▶")); PSUI.actTitleEl = PSEl("span", { fontWeight: "700", fontSize: "14px", flex: "1" }); title.appendChild(PSUI.actTitleEl); const closeBtn = PSSmallBtn("✕", PSUIActWinClose, { title: PST("closeTitle") }); title.appendChild(closeBtn); win.appendChild(title); const canvas = document.createElement("canvas"); canvas.width = 300; canvas.height = 500; PSStyle(canvas, { display: "block", position: "relative", width: "300px", height: "500px", margin: "8px auto 0", cursor: "crosshair", }); canvas.addEventListener("click", (ev) => { try { const rect = canvas.getBoundingClientRect(); const x = (ev.clientX - rect.left) * (canvas.width / Math.max(1, rect.width)); const y = (ev.clientY - rect.top) * (canvas.height / Math.max(1, rect.height)); const hit = PSActZoneHitTest(x, y, canvas.width, canvas.height); if (hit) PSUIActWinSelect(hit); } catch (e) { PSErr("部位选择点击失败", e); } }); canvas.addEventListener("mousemove", (ev) => { try { const rect = canvas.getBoundingClientRect(); const x = (ev.clientX - rect.left) * (canvas.width / Math.max(1, rect.width)); const y = (ev.clientY - rect.top) * (canvas.height / Math.max(1, rect.height)); const hit = PSActZoneHitTest(x, y, canvas.width, canvas.height); if (hit !== PSUI.actHover) { PSUI.actHover = hit; PSUIActPreviewDraw(); } } catch (e) { } }); canvas.addEventListener("mouseleave", () => { if (PSUI.actHover !== null) { PSUI.actHover = null; PSUIActPreviewDraw(); } }); win.appendChild(canvas); const foot = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "10px 12px" }); const anyBtn = PSBigBtn(PST("actAny"), () => PSUIActWinSelect("any")); foot.appendChild(anyBtn); PSUI.actChipEl = PSEl("span", { flex: "1", textAlign: "center", padding: "8px 10px", borderRadius: "6px", background: "#10141f", border: "1px solid " + PS_BORDER, color: PS_ACCENT, fontSize: "13px", }); foot.appendChild(PSUI.actChipEl); const doneBtn = PSBigBtn(PST("actDone"), PSUIActWinClose, { bg: "#2c7a70" }); foot.appendChild(doneBtn); win.appendChild(foot); document.body.appendChild(win); PSUI.actWin = win; PSUI.actCanvas = canvas; } function PSUIActWinOpen() { const sc = PSFindScript(PSUI.selScriptId); if (!sc) { PSToast(PST("toastNoSelScript")); return; } PSUI.actScriptId = sc.id; PSUI.actHover = null; PSUIActWinBuild(); if (!PSUI.actWin) return; PSUI.actOpen = true; PSUI.actWin.style.display = "block"; PSUIActWinRender(); } function PSUIActWinClose() { PSUI.actOpen = false; PSUI.actHover = null; if (PSUI.actWin) PSUI.actWin.style.display = "none"; } function PSUIActWinSelect(group) { const sc = PSFindScript(PSUI.actScriptId); if (sc) PSUpdateScript(sc.id, { actionGroup: group }); PSUIActWinRender(); PSUIRenderAll(); } function PSUIActWinRender() { if (!PSUI.actWin) return; const sc = PSFindScript(PSUI.actScriptId); if (PSUI.actTitleEl) PSUI.actTitleEl.textContent = PST("actWinTitle") + " · " + (sc ? sc.name : ""); if (PSUI.actChipEl) PSUI.actChipEl.textContent = PST("actCurrent", PSActGroupLabel(PSUIActSelectedGroup())); PSUIActPreviewDraw(); } function PSInteractWinScript() { return PSFindScript(PSUI.interactScriptId); } function PSInteractBundle() { try { if (typeof Player !== "undefined" && Player && typeof ServerAppearanceBundle === "function") { return ServerAppearanceBundle(Player) || []; } } catch (e) { } return []; } function PSInteractWinRender() { if (!PSUI.interactWin || !PSUI.interactCanvas) return; const sc = PSInteractWinScript(); if (PSUI.interactTitleEl) PSUI.interactTitleEl.textContent = PST("interactWinTitle") + " · " + (sc ? sc.name : ""); if (PSUI.interactChipEl) PSUI.interactChipEl.textContent = PST("interactGroupsN", PSInteractGroups(sc).length, (sc && Array.isArray(sc.interactActions)) ? sc.interactActions.length : 0); PSPreviewDrawChar(PSUI.interactCanvas, PSInteractBundle()); const sel = new Set(PSInteractGroups(sc)); PSPreviewDrawZones(PSUI.interactCanvas, (name) => { if (sel.has(name)) return { fill: "rgba(76,175,80,0.35)", stroke: "#66bb6a", width: 2.5 }; if (name === PSUI.interactHover) return { fill: "rgba(255,255,255,0.18)", stroke: "rgba(255,255,255,0.85)", width: 2 }; return { fill: "#80808040", stroke: "#808080", width: 1.5 }; }, PSActZones()); } function PSInteractWinBuild() { if (PSUI.interactWin || typeof document === "undefined" || !document.body) return; const win = document.createElement("div"); win.id = "ps-interactwin"; PSStyle(win, { position: "fixed", left: "50%", top: "50%", transform: "translate(-50%, -50%)", width: "380px", zIndex: "2147483570", background: PS_BG, border: "2px solid #ffffff", borderRadius: "10px", boxShadow: "0 8px 40px rgba(0,0,0,.7)", display: "none", color: PS_TEXT, fontFamily: "sans-serif", fontSize: "14px", overflow: "hidden", }); const title = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "8px 12px", background: "#141826", borderBottom: "1px solid " + PS_BORDER, userSelect: "none" }); title.appendChild(PSEl("span", { color: "#9fd8a0", fontSize: "16px" }, "▶")); PSUI.interactTitleEl = PSEl("span", { fontWeight: "700", fontSize: "14px", flex: "1" }); title.appendChild(PSUI.interactTitleEl); const closeBtn = PSSmallBtn("✕", PSInteractWinClose, { title: PST("closeTitle") }); title.appendChild(closeBtn); win.appendChild(title); const hint = PSEl("div", { padding: "6px 12px", fontSize: "12px", color: PS_TEXT_DIM, borderBottom: "1px solid " + PS_BORDER }); hint.textContent = PST("interactHint"); win.appendChild(hint); const canvas = document.createElement("canvas"); canvas.width = 300; canvas.height = 500; PSStyle(canvas, { display: "block", position: "relative", width: "300px", height: "500px", margin: "8px auto 0", cursor: "crosshair" }); canvas.addEventListener("click", (ev) => { try { const rect = canvas.getBoundingClientRect(); const x = (ev.clientX - rect.left) * (canvas.width / Math.max(1, rect.width)); const y = (ev.clientY - rect.top) * (canvas.height / Math.max(1, rect.height)); const hit = PSActZoneHitTest(x, y, canvas.width, canvas.height); if (hit) PSInteractWinPick(hit); } catch (e) { PSErr("互动部位选择点击失败", e); } }); canvas.addEventListener("mousemove", (ev) => { try { const rect = canvas.getBoundingClientRect(); const x = (ev.clientX - rect.left) * (canvas.width / Math.max(1, rect.width)); const y = (ev.clientY - rect.top) * (canvas.height / Math.max(1, rect.height)); const hit = PSActZoneHitTest(x, y, canvas.width, canvas.height); if (hit !== PSUI.interactHover) { PSUI.interactHover = hit; PSInteractWinRender(); } } catch (e) { } }); canvas.addEventListener("mouseleave", () => { if (PSUI.interactHover !== null) { PSUI.interactHover = null; PSInteractWinRender(); } }); win.appendChild(canvas); const foot = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "10px 12px" }); PSUI.interactChipEl = PSEl("span", { flex: "1", textAlign: "center", padding: "8px 10px", borderRadius: "6px", background: "#10141f", border: "1px solid " + PS_BORDER, color: "#9fd8a0", fontSize: "13px" }); foot.appendChild(PSUI.interactChipEl); foot.appendChild(PSBigBtn(PST("actDone"), PSInteractWinClose, { bg: "#2c7a70" })); win.appendChild(foot); document.body.appendChild(win); PSUI.interactWin = win; PSUI.interactCanvas = canvas; } function PSInteractWinOpen() { const sc = PSFindScript(PSUI.selScriptId); if (!sc) { PSToast(PST("toastNoSelScript")); return; } PSUI.interactScriptId = sc.id; PSUI.interactHover = null; PSInteractWinBuild(); if (!PSUI.interactWin) return; PSUI.interactOpen = true; PSUI.interactWin.style.display = "block"; PSInteractWinRender(); } function PSInteractWinClose() { PSUI.interactOpen = false; PSUI.interactHover = null; if (PSUI.interactWin) PSUI.interactWin.style.display = "none"; } function PSInteractWinPick(group) { PSUI.interactActGroup = group; PSUI.interactActList = PSInteractActivityNames(group); PSInteractActBuild(); if (!PSUI.interactActWin) return; PSUI.interactActOpen = true; PSUI.interactActWin.style.display = "flex"; PSInteractActRender(); } function PSInteractActBuild() { if (PSUI.interactActWin || typeof document === "undefined" || !document.body) return; const win = document.createElement("div"); win.id = "ps-interactactwin"; PSStyle(win, { position: "fixed", left: "50%", top: "50%", transform: "translate(-50%, -50%)", width: "420px", maxHeight: "80vh", zIndex: "2147483580", background: PS_BG, border: "2px solid #ffffff", borderRadius: "10px", boxShadow: "0 8px 40px rgba(0,0,0,.7)", display: "none", flexDirection: "column", color: PS_TEXT, fontFamily: "sans-serif", fontSize: "14px", overflow: "hidden", }); const title = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "8px 12px", background: "#141826", borderBottom: "1px solid " + PS_BORDER, userSelect: "none" }); title.appendChild(PSEl("span", { color: "#9fd8a0", fontSize: "16px" }, "▶")); PSUI.interactActTitleEl = PSEl("span", { fontWeight: "700", fontSize: "14px", flex: "1" }); title.appendChild(PSUI.interactActTitleEl); const closeBtn = PSSmallBtn("✕", PSInteractActClose, { title: PST("closeTitle") }); title.appendChild(closeBtn); win.appendChild(title); const hint = PSEl("div", { padding: "6px 12px", fontSize: "12px", color: PS_TEXT_DIM, borderBottom: "1px solid " + PS_BORDER }); hint.textContent = PST("interactActHint"); win.appendChild(hint); const listEl = PSEl("div", { overflowY: "auto", padding: "8px 12px", flex: "1", minHeight: "0" }); win.appendChild(listEl); PSUI.interactActListEl = listEl; const foot = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "10px 12px", borderTop: "1px solid " + PS_BORDER }); foot.appendChild(PSBigBtn(PST("interactBack"), PSInteractActClose)); PSUI.interactActChipEl = PSEl("span", { flex: "1", textAlign: "center", padding: "8px 10px", borderRadius: "6px", background: "#10141f", border: "1px solid " + PS_BORDER, color: "#9fd8a0", fontSize: "13px" }); foot.appendChild(PSUI.interactActChipEl); foot.appendChild(PSBigBtn(PST("actDone"), PSInteractActClose, { bg: "#2c7a70" })); win.appendChild(foot); document.body.appendChild(win); PSUI.interactActWin = win; } function PSInteractActClose() { PSUI.interactActOpen = false; if (PSUI.interactActWin) PSUI.interactActWin.style.display = "none"; PSInteractWinRender(); } function PSInteractToggleAction(group, action) { const sc = PSInteractWinScript(); if (!sc) return; const arr = Array.isArray(sc.interactActions) ? sc.interactActions.slice() : []; const i = arr.findIndex((x) => x.g === group && x.a === action); if (i >= 0) arr.splice(i, 1); else arr.push({ g: group, a: action }); PSUpdateScript(sc.id, { interactActions: arr }); PSInteractActRender(); PSInteractWinRender(); PSUIRenderAll(); } function PSInteractActRender() { if (!PSUI.interactActWin) return; const sc = PSInteractWinScript(); const group = PSUI.interactActGroup; if (PSUI.interactActTitleEl) PSUI.interactActTitleEl.textContent = PST("interactActWinTitle", PSActGroupLabel(group)); if (PSUI.interactActChipEl) PSUI.interactActChipEl.textContent = PST("interactCurrent", PSInteractGroupCount(sc, group)); const listEl = PSUI.interactActListEl; if (!listEl) return; listEl.innerHTML = ""; if (!PSUI.interactActList || !PSUI.interactActList.length) { listEl.appendChild(PSEl("div", { color: PS_TEXT_DIM, padding: "14px", textAlign: "center" }, PSEsc(PST("interactEmpty")))); return; } for (const a of PSUI.interactActList) { const on = PSInteractHas(sc, group, a); const b = PSEl("button", { display: "block", width: "100%", textAlign: "left", padding: "8px 10px", margin: "4px 0", background: on ? "#2e7d32" : "#2c3350", color: on ? "#d7ffd9" : "#ffffff", border: "1px solid " + (on ? "#66bb6a" : PS_BORDER), borderRadius: "6px", cursor: "pointer", fontSize: "13px", }, PSEsc(PSInteractActivityLabel(group, a))); b.addEventListener("click", () => PSInteractToggleAction(group, a)); listEl.appendChild(b); } } function PSUIOutfitNode() { const sc = PSFindScript(PSUI.outfitScriptId); return sc ? (sc.nodes.find((n) => n.id === PSUI.outfitNodeId) || null) : null; } function PSUIOutfitWinOpen() { const sc = PSFindScript(PSUI.selScriptId); const node = sc ? sc.nodes.find((n) => n.id === PSUI.selNodeId) : null; if (!node || node.type !== "outfit") { PSToast(PST("outfitNeedNode")); return; } PSUI.outfitScriptId = sc.id; PSUI.outfitNodeId = node.id; const bundle = PSDecodeOutfitCode(PSNormalizeCode(node.text)) || []; const family = (typeof Player !== "undefined" && Player && typeof Player.AssetFamily === "string" && Player.AssetFamily) ? Player.AssetFamily : "Female3DCG"; const codeGroups = []; for (const b of bundle) { const cls = PSOutfitClassOf(b, family); if (cls !== "items" || typeof b.Group !== "string") continue; if (!codeGroups.includes(b.Group)) codeGroups.push(b.Group); } PSUI.outfitCodeGroups = codeGroups; PSUI.outfitBundle = bundle; const override = node && node.outfitItemsMode === "override"; PSUI.outfitPickable = override ? PSActZonesAll().map((z) => z.name) : codeGroups.slice(); const sel = Array.isArray(node.outfitGroups) ? node.outfitGroups.slice() : PSUI.outfitPickable.slice(); PSUI.outfitSel = sel; PSUI.outfitHover = null; PSUIOutfitWinBuild(); if (!PSUI.outfitWin) return; PSUI.outfitOpen = true; PSUI.outfitWin.style.display = "block"; PSUIOutfitWinRender(); } function PSUIOutfitWinClose() { PSUI.outfitOpen = false; PSUI.outfitHover = null; if (PSUI.outfitWin) PSUI.outfitWin.style.display = "none"; } function PSUIOutfitWinSave() { const node = PSUIOutfitNode(); if (node) PSUpdateNode(PSUI.outfitScriptId, node.id, { outfitGroups: PSUI.outfitSel.slice() }); PSUIOutfitWinRender(); PSUIRenderAll(); } function PSUIOutfitToggle(name) { if (!PSUI.outfitPickable.includes(name)) return; const i = PSUI.outfitSel.indexOf(name); if (i >= 0) PSUI.outfitSel.splice(i, 1); else PSUI.outfitSel.push(name); PSUIOutfitWinSave(); } function PSUIOutfitAll() { PSUI.outfitSel = PSUI.outfitPickable.slice(); PSUIOutfitWinSave(); } function PSUIOutfitNone() { PSUI.outfitSel = []; PSUIOutfitWinSave(); } function PSUIOutfitWinRender() { if (!PSUI.outfitWin) return; const node = PSUIOutfitNode(); if (PSUI.outfitTitleEl) PSUI.outfitTitleEl.textContent = PST("outfitWinTitle") + (node ? "" : ""); if (PSUI.outfitChipEl) PSUI.outfitChipEl.textContent = PST("outfitGroupsN", PSUI.outfitSel.length, PSUI.outfitPickable.length); if (!PSUI.outfitCanvas) return; PSPreviewDrawChar(PSUI.outfitCanvas, PSUI.outfitBundle); PSPreviewDrawZones(PSUI.outfitCanvas, (name) => { const hasItem = PSUI.outfitCodeGroups.includes(name); const isSel = PSUI.outfitSel.includes(name); const isHover = name === PSUI.outfitHover; if (isSel) return { fill: "rgba(76,175,80,0.35)", stroke: "#66bb6a", width: 2.5 }; if (isHover) return { fill: "rgba(255,255,255,0.18)", stroke: "rgba(255,255,255,0.85)", width: 2 }; if (hasItem) return { fill: "rgba(255,152,0,0.30)", stroke: "#ff9f43", width: 2 }; return { fill: "#80808040", stroke: "#808080", width: 1.5 }; }, PSActZonesAll()); } function PSUIOutfitWinBuild() { if (PSUI.outfitWin || typeof document === "undefined" || !document.body) return; const win = document.createElement("div"); win.id = "ps-outfitwin"; PSStyle(win, { position: "fixed", left: "50%", top: "50%", transform: "translate(-50%, -50%)", width: "380px", zIndex: "2147483560", background: PS_BG, border: "2px solid #ffffff", borderRadius: "10px", boxShadow: "0 8px 40px rgba(0,0,0,.7)", display: "none", color: PS_TEXT, fontFamily: "sans-serif", fontSize: "14px", overflow: "hidden", }); const title = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "8px 12px", background: "#141826", borderBottom: "1px solid " + PS_BORDER, userSelect: "none", }); title.appendChild(PSEl("span", { color: "#9fd8a0", fontSize: "16px" }, "▶")); PSUI.outfitTitleEl = PSEl("span", { fontWeight: "700", fontSize: "14px", flex: "1" }); title.appendChild(PSUI.outfitTitleEl); const closeBtn = PSSmallBtn("✕", PSUIOutfitWinClose, { title: PST("closeTitle") }); title.appendChild(closeBtn); win.appendChild(title); const hint = PSEl("div", { padding: "6px 12px", fontSize: "12px", color: PS_TEXT_DIM, borderBottom: "1px solid " + PS_BORDER }); hint.textContent = PST("outfitWinHint"); win.appendChild(hint); const canvas = document.createElement("canvas"); canvas.width = 300; canvas.height = 500; PSStyle(canvas, { display: "block", position: "relative", width: "300px", height: "500px", margin: "8px auto 0", cursor: "crosshair", }); canvas.addEventListener("click", (ev) => { try { const rect = canvas.getBoundingClientRect(); const x = (ev.clientX - rect.left) * (canvas.width / Math.max(1, rect.width)); const y = (ev.clientY - rect.top) * (canvas.height / Math.max(1, rect.height)); const hit = PSActZoneHitTestAll(x, y, canvas.width, canvas.height); if (hit) PSUIOutfitToggle(hit); } catch (e) { PSErr("道具部位选择点击失败", e); } }); canvas.addEventListener("mousemove", (ev) => { try { const rect = canvas.getBoundingClientRect(); const x = (ev.clientX - rect.left) * (canvas.width / Math.max(1, rect.width)); const y = (ev.clientY - rect.top) * (canvas.height / Math.max(1, rect.height)); const hit = PSActZoneHitTestAll(x, y, canvas.width, canvas.height); if (hit !== PSUI.outfitHover) { PSUI.outfitHover = hit; PSUIOutfitWinRender(); } } catch (e) { } }); canvas.addEventListener("mouseleave", () => { if (PSUI.outfitHover !== null) { PSUI.outfitHover = null; PSUIOutfitWinRender(); } }); win.appendChild(canvas); const foot = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "10px 12px" }); foot.appendChild(PSBigBtn(PST("outfitAll"), PSUIOutfitAll)); foot.appendChild(PSBigBtn(PST("outfitNoneBtn"), PSUIOutfitNone)); PSUI.outfitChipEl = PSEl("span", { flex: "1", textAlign: "center", padding: "8px 10px", borderRadius: "6px", background: "#10141f", border: "1px solid " + PS_BORDER, color: "#9fd8a0", fontSize: "13px", }); foot.appendChild(PSUI.outfitChipEl); foot.appendChild(PSBigBtn(PST("actDone"), PSUIOutfitWinClose, { bg: "#2c7a70" })); win.appendChild(foot); document.body.appendChild(win); PSUI.outfitWin = win; PSUI.outfitCanvas = canvas; } function PSUIEditorRow(labelText, control) { const row = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const lab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); lab.textContent = labelText; row.appendChild(lab); row.appendChild(control); return row; } function PSUIInput(value, onchange, opts) { opts = opts || {}; const inp = document.createElement("input"); inp.type = opts.type || "text"; inp.value = value; if (opts.placeholder) inp.placeholder = opts.placeholder; if (opts.min !== undefined) { inp.min = opts.min; } if (opts.max !== undefined) { inp.max = opts.max; } if (opts.step !== undefined) { inp.step = opts.step; } PSStyle(inp, { flex: "1", minWidth: "0", padding: "8px 10px", fontSize: "13px", borderRadius: "6px", background: "#10141f", color: PS_TEXT, border: "1px solid " + PS_BORDER, fontFamily: "sans-serif", }); if (onchange) inp.addEventListener("change", () => onchange(inp.value)); return inp; } function PSUISelect(value, optionsHTML, onchange) { const sel = PSEl("select", { flex: "1", minWidth: "0", padding: "8px 6px", fontSize: "13px", borderRadius: "6px", background: "#10141f", color: PS_TEXT, border: "1px solid " + PS_BORDER, fontFamily: "sans-serif", }, optionsHTML); sel.value = value; sel.addEventListener("change", () => onchange(sel.value)); return sel; } function PSUIEditorBuild() { const box = PSUI.editorEl; if (!box) return; box.innerHTML = ""; PSUI.textareaEl = null; const sc = PSFindScript(PSUI.selScriptId); if (!sc) { box.appendChild(PSEl("div", { color: PS_TEXT_DIM, padding: "20px 10px", textAlign: "center" }, PSEsc(PST("scriptsHint")))); return; } const node = sc.nodes.find((n) => n.id === PSUI.selNodeId) || null; if (node) { PSUINodeEditor(box, sc, node); return; } if (PSUI.selTrigger) { PSUITriggerSection(box, sc); return; } const sec1 = PSEl("div", { padding: "10px", borderRadius: "8px", background: PS_PANEL, marginBottom: "10px" }); sec1.appendChild(PSEl("div", { fontWeight: "700", marginBottom: "8px", color: PS_ACCENT }, PSEsc(PST("scriptSettings")))); const nameInp = PSUIInput(sc.name, (v) => { PSUpdateScript(sc.id, { name: v }); PSUIRenderAll(); }); sec1.appendChild(PSUIEditorRow(PST("scriptName"), nameInp)); const bioRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const bioLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); bioLab.textContent = PST("cloudBioLabel"); bioRow.appendChild(bioLab); const bioCb = document.createElement("input"); bioCb.type = "checkbox"; bioCb.checked = !!sc.cloudBio; bioCb.addEventListener("change", () => { PSUpdateScript(sc.id, { cloudBio: bioCb.checked }); PSUIRenderAll(); }); bioRow.appendChild(bioCb); sec1.appendChild(bioRow); const bioHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "8px" }); bioHint.textContent = PST("cloudBioHint"); sec1.appendChild(bioHint); const btnRow = PSEl("div", { display: "flex", gap: "8px", flexWrap: "wrap" }); const expOneBtn = PSSmallBtn(PST("exportOneBtn"), () => PSUIExportScript(sc.id)); const dupBtn = PSSmallBtn(PST("dupScript"), () => { const c = PSDuplicateScript(sc.id); if (c) { PSUI.selScriptId = c.id; PSUI.selNodeId = null; PSUI.selTrigger = false; PSUIRenderAll(); } }); const delBtn = PSSmallBtn(PST("delScript"), () => PSArm("delscript:" + sc.id, delBtn, PST("delScriptConfirm"), () => { PSDeleteScript(sc.id); PSUIRenderAll(); }), { bg: "#7a2c3a" }); btnRow.appendChild(expOneBtn); btnRow.appendChild(dupBtn); btnRow.appendChild(delBtn); sec1.appendChild(btnRow); box.appendChild(sec1); box.appendChild(PSEl("div", { color: PS_TEXT_DIM, padding: "14px 10px", textAlign: "center" }, PSEsc(PST("editorHint")))); } function PSUITriggerSection(box, sc) { const sec1 = PSEl("div", { padding: "10px", borderRadius: "8px", background: PS_PANEL, marginBottom: "10px" }); sec1.appendChild(PSEl("div", { fontWeight: "700", marginBottom: "8px", color: PS_ACCENT }, PSEsc(PST("triggerLabel")) + " · " + PSEsc(PST("triggerSettings")))); const kwInp = PSUIInput(sc.keyword, (v) => { PSUpdateScript(sc.id, { keyword: v }); PSUIRenderAll(); }, { placeholder: PST("keywordPh") }); sec1.appendChild(PSUIEditorRow(PST("keyword"), kwInp)); const matchSel = PSUISelect(sc.matchMode, [ '<option value="contains">' + PSEsc(PST("matchContains")) + "</option>", '<option value="exact">' + PSEsc(PST("matchExact")) + "</option>", ].join(""), (v) => { PSUpdateScript(sc.id, { matchMode: v }); PSUIRenderAll(); }); sec1.appendChild(PSUIEditorRow(PST("matchMode"), matchSel)); const whoSel = PSUISelect(sc.triggerWho, [ '<option value="self">' + PSEsc(PST("whoSelf")) + "</option>", '<option value="anyone">' + PSEsc(PST("whoAnyone")) + "</option>", '<option value="others">' + PSEsc(PST("whoOthers")) + "</option>", ].join(""), (v) => { PSUpdateScript(sc.id, { triggerWho: v }); PSUIRenderAll(); }); sec1.appendChild(PSUIEditorRow(PST("triggerWho"), whoSel)); const caseCb = document.createElement("input"); caseCb.type = "checkbox"; caseCb.checked = !!sc.caseSensitive; caseCb.addEventListener("change", () => { PSUpdateScript(sc.id, { caseSensitive: caseCb.checked }); PSUIRenderAll(); }); const caseRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const caseLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); caseLab.textContent = PST("caseSensitive"); caseRow.appendChild(caseLab); caseRow.appendChild(caseCb); sec1.appendChild(caseRow); const cdInp = PSUIInput(String(sc.cooldown), (v) => { PSUpdateScript(sc.id, { cooldown: v }); PSUIRenderAll(); }, { type: "number", min: "0", max: String(PS_MAX_DELAY), step: "1" }); sec1.appendChild(PSUIEditorRow(PST("cooldown"), cdInp)); const retrySel = PSUISelect(sc.onRetrigger, [ '<option value="ignore">' + PSEsc(PST("retryIgnore")) + "</option>", '<option value="restart">' + PSEsc(PST("retryRestart")) + "</option>", '<option value="queue">' + PSEsc(PST("retryQueue")) + "</option>", ].join(""), (v) => { PSUpdateScript(sc.id, { onRetrigger: v }); PSUIRenderAll(); }); sec1.appendChild(PSUIEditorRow(PST("onRetrigger"), retrySel)); const targetHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, margin: "6px 0 8px", lineHeight: "1.5" }); targetHint.textContent = PST("triggerTargetHint"); sec1.appendChild(targetHint); const actSec = PSEl("div", { padding: "8px", borderRadius: "6px", background: "#1a2134", border: "1px solid " + PS_BORDER, marginBottom: "8px", }); const actHead = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "6px" }); actHead.appendChild(PSEl("span", { fontSize: "14px", color: PS_ACCENT, fontWeight: "700" }, "▶")); const actTitle = PSEl("span", { fontWeight: "700", fontSize: "13px", flex: "1" }); actTitle.textContent = PST("actTrigger"); actHead.appendChild(actTitle); const actCb = document.createElement("input"); actCb.type = "checkbox"; actCb.checked = !!sc.actionEnabled; actCb.addEventListener("change", () => { PSUpdateScript(sc.id, { actionEnabled: actCb.checked }); PSUIRenderAll(); }); actHead.appendChild(actCb); actSec.appendChild(actHead); const actHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "6px" }); actHint.textContent = PST("actHint"); actSec.appendChild(actHint); const actNameInp = PSUIInput(sc.actionName, (v) => { PSUpdateScript(sc.id, { actionName: v }); PSUIRenderAll(); }, { placeholder: PST("actNamePh") }); actSec.appendChild(PSUIEditorRow(PST("actName"), actNameInp)); const actGroupRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const actGroupLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); actGroupLab.textContent = PST("actGroup"); actGroupRow.appendChild(actGroupLab); const pickBtn = PSSmallBtn("▶ " + PST("actPick"), PSUIActWinOpen); pickBtn.style.flex = "1"; actGroupRow.appendChild(pickBtn); actGroupRow.appendChild(PSEl("span", { padding: "6px 10px", borderRadius: "6px", background: "#10141f", border: "1px solid " + PS_BORDER, fontSize: "13px", color: PS_ACCENT, }, PSEsc(PSActGroupLabel(sc.actionGroup || "any")))); actSec.appendChild(actGroupRow); const actTargetSel = PSUISelect(sc.actionTarget || "others", [ '<option value="self">' + PSEsc(PST("actTargetSelf")) + "</option>", '<option value="others">' + PSEsc(PST("actTargetOthers")) + "</option>", ].join(""), (v) => { PSUpdateScript(sc.id, { actionTarget: v }); PSUIRenderAll(); }); actSec.appendChild(PSUIEditorRow(PST("actTarget"), actTargetSel)); sec1.appendChild(actSec); const otherSec = PSEl("div", { padding: "8px", borderRadius: "6px", background: "#1a2134", border: "1px solid " + PS_BORDER, marginBottom: "8px", }); const otherHead = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "6px" }); const otherTitle = PSEl("span", { fontWeight: "700", fontSize: "13px", flex: "1" }); otherTitle.textContent = PST("otherTrigger"); otherHead.appendChild(otherTitle); otherSec.appendChild(otherHead); const otherHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "6px" }); otherHint.textContent = PST("otherHint"); otherSec.appendChild(otherHint); const roomRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const roomLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); roomLab.textContent = PST("roomTrigger"); roomRow.appendChild(roomLab); const roomCb = document.createElement("input"); roomCb.type = "checkbox"; roomCb.checked = !!sc.roomTrigger; roomCb.addEventListener("change", () => { PSUpdateScript(sc.id, { roomTrigger: roomCb.checked }); PSUIRenderAll(); }); roomRow.appendChild(roomCb); otherSec.appendChild(roomRow); if (sc.roomTrigger) { const rcRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px", paddingLeft: "18px" }); const rcLab = PSEl("label", { width: "92px", minWidth: "92px", fontSize: "12px", color: PS_TEXT_DIM }); rcLab.textContent = PST("roomTriggerOnReconnect"); rcRow.appendChild(rcLab); const rcCb = document.createElement("input"); rcCb.type = "checkbox"; rcCb.checked = sc.roomTriggerOnReconnect !== false; rcCb.addEventListener("change", () => { PSUpdateScript(sc.id, { roomTriggerOnReconnect: rcCb.checked }); PSUIRenderAll(); }); rcRow.appendChild(rcCb); otherSec.appendChild(rcRow); } const plRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const plLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); plLab.textContent = PST("playerTrigger"); plRow.appendChild(plLab); const plCb = document.createElement("input"); plCb.type = "checkbox"; plCb.checked = !!sc.playerTrigger; plCb.addEventListener("change", () => { PSUpdateScript(sc.id, { playerTrigger: plCb.checked }); PSUIRenderAll(); }); plRow.appendChild(plCb); otherSec.appendChild(plRow); const plIdInp = PSUIInput((Array.isArray(sc.playerTargets) ? sc.playerTargets : []).join("、"), (v) => { const nums = String(v || "").split(/[、,，\s]+/).map((x) => Number(x)).filter((n) => Number.isInteger(n) && n > 0).slice(0, 50); PSUpdateScript(sc.id, { playerTargets: nums }); PSUIRenderAll(); }, { type: "text", placeholder: "12345、67890" }); otherSec.appendChild(PSUIEditorRow(PST("playerTargetId"), plIdInp)); const ttRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const ttLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); ttLab.textContent = PST("timeTrigger"); ttRow.appendChild(ttLab); const ttCb = document.createElement("input"); ttCb.type = "checkbox"; ttCb.checked = !!sc.timeTrigger; ttCb.addEventListener("change", () => { PSUpdateScript(sc.id, { timeTrigger: ttCb.checked }); PSUIRenderAll(); }); ttRow.appendChild(ttCb); otherSec.appendChild(ttRow); if (sc.timeTrigger) { const tsec = PSEl("div", { padding: "6px 8px", borderRadius: "6px", background: "#141a2c", marginBottom: "8px" }); const tHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "6px" }); tHint.textContent = PST("timeTriggerHint"); tsec.appendChild(tHint); const tRules = Array.isArray(sc.timeRules) ? sc.timeRules.slice() : []; const commitT = (next) => { PSUpdateScript(sc.id, { timeRules: next }); PSUIRenderAll(); }; const setTField = (rid, key, val) => commitT(tRules.map((r) => (r.id === rid ? Object.assign({}, r, { [key]: val }) : r))); const delTRule = (rid) => commitT(tRules.filter((r) => r.id !== rid)); tRules.forEach((r) => { const row = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", marginBottom: "6px" }); const fromInp = PSUIInput(r.from || "06:00", (v) => setTField(r.id, "from", v), { type: "text", placeholder: "06:00" }); fromInp.title = PST("timeRuleFrom"); fromInp.style.flex = "0 1 70px"; row.appendChild(fromInp); row.appendChild(PSEl("span", { color: PS_TEXT_DIM, fontSize: "12px" }, "~")); const toInp = PSUIInput(r.to || "11:00", (v) => setTField(r.id, "to", v), { type: "text", placeholder: "11:00" }); toInp.title = PST("timeRuleTo"); toInp.style.flex = "0 1 70px"; row.appendChild(toInp); const tzInp = PSUIInput(r.tz || "", (v) => setTField(r.id, "tz", v), { placeholder: "UTC+8 / local" }); tzInp.title = PST("timeRuleTz"); tzInp.style.minWidth = "86px"; tzInp.style.flex = "1 1 120px"; row.appendChild(tzInp); const delBtn = PSSmallBtn("✕", () => delTRule(r.id), { bg: "#5a2c3a" }); delBtn.title = PST("delScript"); row.appendChild(delBtn); tsec.appendChild(row); }); const addT = PSSmallBtn(PST("timeRuleAdd"), () => commitT(tRules.concat([{ id: PSUid("tt"), from: "06:00", to: "11:00", tz: "UTC+8" }]))); tsec.appendChild(addT); otherSec.appendChild(tsec); } const orgHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "6px" }); orgHint.textContent = PST("orgasmHint"); otherSec.appendChild(orgHint); const orgRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const orgLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); orgLab.textContent = PST("orgasmTrigger"); orgRow.appendChild(orgLab); const orgCb = document.createElement("input"); orgCb.type = "checkbox"; orgCb.checked = !!sc.orgasmTrigger; orgCb.addEventListener("change", () => { PSUpdateScript(sc.id, { orgasmTrigger: orgCb.checked }); PSUIRenderAll(); }); orgRow.appendChild(orgCb); otherSec.appendChild(orgRow); const orgDenyRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const orgDenyLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); orgDenyLab.textContent = PST("orgasmDenyTrigger"); orgDenyRow.appendChild(orgDenyLab); const orgDenyCb = document.createElement("input"); orgDenyCb.type = "checkbox"; orgDenyCb.checked = !!sc.orgasmDenyTrigger; orgDenyCb.addEventListener("change", () => { PSUpdateScript(sc.id, { orgasmDenyTrigger: orgDenyCb.checked }); PSUIRenderAll(); }); orgDenyRow.appendChild(orgDenyCb); otherSec.appendChild(orgDenyRow); const interRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const interLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); interLab.textContent = PST("interactTrigger"); interRow.appendChild(interLab); const interCb = document.createElement("input"); interCb.type = "checkbox"; interCb.checked = !!sc.interactTrigger; interCb.addEventListener("change", () => { PSUpdateScript(sc.id, { interactTrigger: interCb.checked }); PSUIRenderAll(); }); interRow.appendChild(interCb); otherSec.appendChild(interRow); if (sc.interactTrigger) { const interHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "6px", paddingLeft: "18px" }); interHint.textContent = PST("interactHint"); otherSec.appendChild(interHint); const interPickRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px", paddingLeft: "18px" }); const interPickBtn = PSSmallBtn("▶ " + PST("interactPick"), PSInteractWinOpen); interPickBtn.style.flex = "1"; interPickRow.appendChild(interPickBtn); interPickRow.appendChild(PSEl("span", { padding: "6px 10px", borderRadius: "6px", background: "#10141f", border: "1px solid " + PS_BORDER, fontSize: "13px", color: "#9fd8a0", }, PSEsc(PST("interactGroupsN", PSInteractGroups(sc).length, (Array.isArray(sc.interactActions) ? sc.interactActions.length : 0))))); otherSec.appendChild(interPickRow); } sec1.appendChild(otherSec); box.appendChild(sec1); } function PSUINodeEditor(box, sc, node) { const sec2 = PSEl("div", { padding: "10px", borderRadius: "8px", background: PS_PANEL }); sec2.appendChild(PSEl("div", { fontWeight: "700", marginBottom: "8px", color: PS_ACCENT }, PSEsc(PST("nodeEdit")))); const typeRow = PSEl("div", { display: "flex", gap: "6px", flexWrap: "wrap", marginBottom: "10px" }); PSTypeNames.forEach((t) => { const active = node.type === t; const b = PSEl("button", { padding: "8px 10px", fontSize: "13px", fontWeight: "600", fontFamily: "sans-serif", background: active ? PSUINodeColor(t) : "#2c3350", color: active ? "#10141f" : "#ffffff", border: "1px solid " + (active ? "#ffffff" : PS_BORDER), borderRadius: "8px", cursor: "pointer", }, PSEsc(PSUITypeLabel(t))); b.addEventListener("click", () => { if (t === "judge" && node.type !== "judge") { const idx = sc.nodes.indexOf(node); const yesN = PSAddNode(sc.id, { type: "chat", text: PST("judgeYesPh"), delay: 0, enabled: true }, idx + 1); const noN = PSAddNode(sc.id, { type: "chat", text: PST("judgeNoPh"), delay: 0, enabled: true }, idx + 2); PSUpdateNode(sc.id, node.id, { type: "judge", text: PST("judgeDefaultLine"), showResult: true, yesId: yesN.id, noId: noN.id, nextId: null }); } else { PSUpdateNode(sc.id, node.id, { type: t }); } PSUIRenderAll(); }); typeRow.appendChild(b); }); sec2.appendChild(typeRow); const delayInp = PSUIInput(String(node.delay), (v) => { PSUpdateNode(sc.id, node.id, { delay: v }); PSUIRenderAll(); }, { type: "number", min: "0", max: String(PS_MAX_DELAY), step: "0.1" }); sec2.appendChild(PSUIEditorRow(PST("nodeDelay"), delayInp)); const isLeave = node.type === "leave"; const isOutfit = node.type === "outfit"; const isJudge = node.type === "judge"; if (isLeave) { const leaveHint = PSEl("div", { padding: "10px", borderRadius: "6px", background: "#2a1a1e", border: "1px solid #f08080", color: "#f3c3c3", fontSize: "13px", marginBottom: "8px", }); leaveHint.textContent = PST("leaveHint"); sec2.appendChild(leaveHint); const forceRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const forceLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); forceLab.textContent = PST("forceLeave"); forceRow.appendChild(forceLab); const forceCb = document.createElement("input"); forceCb.type = "checkbox"; forceCb.checked = !!node.force; forceCb.addEventListener("change", () => { PSUpdateNode(sc.id, node.id, { force: forceCb.checked }); PSUIRenderAll(); }); forceRow.appendChild(forceCb); sec2.appendChild(forceRow); const forceHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "8px" }); forceHint.textContent = PST("forceLeaveHint"); sec2.appendChild(forceHint); PSUI.textareaEl = null; } else if (isOutfit) { const outfitHint = PSEl("div", { padding: "10px", borderRadius: "6px", background: "#1e2a1e", border: "1px solid #9fd8a0", color: "#cfeccf", fontSize: "13px", marginBottom: "8px", }); outfitHint.textContent = PST("outfitHint"); sec2.appendChild(outfitHint); const ta = document.createElement("textarea"); ta.rows = 5; ta.value = node.text; PSStyle(ta, { width: "100%", boxSizing: "border-box", padding: "8px 10px", fontSize: "12px", borderRadius: "6px", background: "#10141f", color: PS_TEXT, border: "1px solid " + PS_BORDER, fontFamily: "monospace", resize: "vertical", marginBottom: "8px", wordBreak: "break-all", }); const applyInput = () => { PSUpdateNode(sc.id, node.id, { text: ta.value }); const card = PSUI.flowCards.find((c) => c.id === node.id); if (card && card.previewEl) card.previewEl.textContent = PST("outfitPreview") + "：" + PSUINodePreview(ta.value); }; ta.addEventListener("input", applyInput); sec2.appendChild(ta); PSUI.textareaEl = ta; const outfitTargetSel = PSUISelect(node.outfitTarget === "self" ? "self" : "target", [ '<option value="target">' + PSEsc(PST("outfitTargetTarget")) + "</option>", '<option value="self">' + PSEsc(PST("outfitTargetSelf")) + "</option>", ].join(""), (v) => { PSUpdateNode(sc.id, node.id, { outfitTarget: v }); PSUIRenderAll(); }); sec2.appendChild(PSUIEditorRow(PST("outfitTargetLabel"), outfitTargetSel)); const flags = PSOutfitFlagsFromNode(node); const scopeRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", flexWrap: "wrap", marginBottom: "8px" }); const scopeLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); scopeLab.textContent = PST("outfitModeLabel"); scopeRow.appendChild(scopeLab); const mkFlag = (key, labelKey, val) => { const box = PSEl("div", { display: "flex", alignItems: "center", gap: "4px" }); const cb = document.createElement("input"); cb.type = "checkbox"; cb.checked = !!val; cb.addEventListener("change", () => { PSUpdateNode(sc.id, node.id, { [key]: cb.checked }); PSUIRenderAll(); }); box.appendChild(cb); const lab = PSEl("span", { fontSize: "13px", color: PS_TEXT }); lab.textContent = PST(labelKey); box.appendChild(lab); return box; }; scopeRow.appendChild(mkFlag("outfitBody", "outfitBodyLabel", flags.outfitBody)); scopeRow.appendChild(mkFlag("outfitClothes", "outfitClothesLabel", flags.outfitClothes)); scopeRow.appendChild(mkFlag("outfitItems", "outfitItemsLabel", flags.outfitItems)); scopeRow.appendChild(mkFlag("outfitCos", "outfitCosLabel", flags.outfitCos)); sec2.appendChild(scopeRow); const locksRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const locksLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); locksLab.textContent = PST("outfitLocksLabel"); locksRow.appendChild(locksLab); const locksCb = document.createElement("input"); locksCb.type = "checkbox"; locksCb.checked = !!node.outfitLocks; locksCb.addEventListener("change", () => { PSUpdateNode(sc.id, node.id, { outfitLocks: locksCb.checked }); PSUIRenderAll(); }); locksRow.appendChild(locksCb); sec2.appendChild(locksRow); const locksHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "8px" }); locksHint.textContent = PST("outfitLocksHint"); sec2.appendChild(locksHint); if (flags.outfitItems) { const modeRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const modeLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); modeLab.textContent = PST("outfitItemsModeLabel"); modeRow.appendChild(modeLab); const modeSel = PSUISelect(node.outfitItemsMode === "override" ? "override" : "add", [ '<option value="add">' + PSEsc(PST("outfitItemsModeAdd")) + "</option>", '<option value="override">' + PSEsc(PST("outfitItemsModeOverride")) + "</option>", ].join(""), (v) => { PSUpdateNode(sc.id, node.id, { outfitItemsMode: v }); PSUIRenderAll(); }); modeSel.style.flex = "1"; modeRow.appendChild(modeSel); sec2.appendChild(modeRow); const pickRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const pickLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); pickLab.textContent = PST("outfitPickLabel"); pickRow.appendChild(pickLab); const pickBtn = PSSmallBtn(PST("outfitPickBtn"), PSUIOutfitWinOpen); pickBtn.style.flex = "1"; pickRow.appendChild(pickBtn); const chipText = Array.isArray(node.outfitGroups) ? PST("outfitGroupsN", node.outfitGroups.length, 0) : PST("outfitGroupsAll"); pickRow.appendChild(PSEl("span", { padding: "6px 10px", borderRadius: "6px", background: "#10141f", border: "1px solid " + PS_BORDER, fontSize: "13px", color: "#9fd8a0", }, PSEsc(chipText))); sec2.appendChild(pickRow); } } else if (isJudge) { PSUIJudgeEditor(sec2, sc, node); } else { const ta = document.createElement("textarea"); ta.rows = 4; ta.value = node.text; PSStyle(ta, { width: "100%", boxSizing: "border-box", padding: "8px 10px", fontSize: "13px", borderRadius: "6px", background: "#10141f", color: PS_TEXT, border: "1px solid " + PS_BORDER, fontFamily: "sans-serif", resize: "vertical", marginBottom: "8px", }); const hadTime = String(node.text || "").indexOf(PS_TIME_TOKEN) >= 0; const applyInput = () => { PSUpdateNode(sc.id, node.id, { text: ta.value }); if ((String(ta.value || "").indexOf(PS_TIME_TOKEN) >= 0) !== hadTime) { PSUIRenderAll(); return; } const card = PSUI.flowCards.find((c) => c.id === node.id); if (card && card.previewEl) card.previewEl.textContent = PSUINodePreview(ta.value); }; ta.addEventListener("input", applyInput); sec2.appendChild(ta); PSUI.textareaEl = ta; const tagRow = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", flexWrap: "wrap", marginBottom: "8px" }); const insertToken = (token) => { const pos = (typeof ta.selectionStart === "number") ? ta.selectionStart : ta.value.length; const end = (typeof ta.selectionEnd === "number") ? ta.selectionEnd : pos; ta.value = ta.value.slice(0, pos) + token + ta.value.slice(end); applyInput(); try { ta.focus(); } catch (e) { } }; const tagBtn = PSSmallBtn(PST("insertNick"), () => insertToken(PS_NICK_TOKEN)); tagBtn.title = PS_NICK_TOKEN; tagRow.appendChild(tagBtn); const tagBtn2 = PSSmallBtn(PST("insertTarget"), () => insertToken(PS_TARGET_TOKEN)); tagBtn2.title = PS_TARGET_TOKEN; tagRow.appendChild(tagBtn2); const tagBtn3 = PSSmallBtn(PST("insertTime"), () => { insertToken(PS_TIME_TOKEN); PSUIRenderAll(); }); tagBtn3.title = PS_TIME_TOKEN; tagRow.appendChild(tagBtn3); tagRow.appendChild(PSEl("span", { color: PS_TEXT_DIM, fontSize: "12px" }, PSEsc(PST("nickHint")))); sec2.appendChild(tagRow); if (String(node.text || "").indexOf(PS_TIME_TOKEN) >= 0) { PSUITimeRuleSection(sec2, sc, node); } PSUIConnectEditorRow(sec2, sc, node, "next"); } const enRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const enLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); enLab.textContent = PST("nodeEnabled"); enRow.appendChild(enLab); const enCb = document.createElement("input"); enCb.type = "checkbox"; enCb.checked = node.enabled !== false; enCb.addEventListener("change", () => { PSUpdateNode(sc.id, node.id, { enabled: enCb.checked }); PSUIRenderAll(); }); enRow.appendChild(enCb); sec2.appendChild(enRow); const nbtnRow = PSEl("div", { display: "flex", gap: "6px", flexWrap: "wrap" }); const idx = sc.nodes.indexOf(node); nbtnRow.appendChild(PSSmallBtn(PST("insBefore"), () => { const n = PSAddNode(sc.id, { type: node.type, text: "", delay: 0, enabled: true }, idx); if (n) { PSUI.selNodeId = n.id; PSUI.focusRequest = true; PSUIRenderAll(); } })); nbtnRow.appendChild(PSSmallBtn(PST("insAfter"), () => { const n = PSAddNodeAfter(sc.id, node.id, { type: node.type, text: "", delay: 0, enabled: true }); if (n) { PSUI.selNodeId = n.id; PSUI.focusRequest = true; PSUIRenderAll(); } })); nbtnRow.appendChild(PSSmallBtn(PST("moveUp"), () => { PSMoveNode(sc.id, node.id, -1); PSUIRenderAll(); })); nbtnRow.appendChild(PSSmallBtn(PST("moveDown"), () => { PSMoveNode(sc.id, node.id, +1); PSUIRenderAll(); })); const delNodeBtn = PSSmallBtn(PST("delNode"), () => PSArm("delnode:" + node.id, delNodeBtn, PST("delNodeConfirm"), () => { PSDeleteNode(sc.id, node.id); PSUIRenderAll(); }), { bg: "#7a2c3a" }); nbtnRow.appendChild(delNodeBtn); sec2.appendChild(nbtnRow); box.appendChild(sec2); } function PSUITimeRuleSection(box, sc, node) { const sec = PSEl("div", { padding: "8px", borderRadius: "6px", background: "#1a2134", border: "1px solid " + PS_BORDER, marginBottom: "8px" }); sec.appendChild(PSEl("div", { fontWeight: "700", fontSize: "13px", color: PS_ACCENT, marginBottom: "6px" }, PSEsc(PST("timeRuleSection")))); const hint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "8px", lineHeight: "1.5" }); hint.textContent = PST("timeRuleHint"); sec.appendChild(hint); const rules = Array.isArray(node.timeRules) ? node.timeRules.slice() : []; const commit = (nextRules) => { PSUpdateNode(sc.id, node.id, { timeRules: nextRules }); PSUIRenderAll(); }; const setField = (rid, key, val) => { const next = rules.map((r) => (r.id === rid ? Object.assign({}, r, { [key]: val }) : r)); commit(next); }; const delRule = (rid) => commit(rules.filter((r) => r.id !== rid)); rules.forEach((r) => { const row1 = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", marginBottom: "4px" }); const fromInp = PSUIInput(r.from || "06:00", (v) => setField(r.id, "from", v), { type: "text", placeholder: "06:00" }); fromInp.title = PST("timeRuleFrom"); fromInp.style.flex = "0 1 70px"; row1.appendChild(fromInp); row1.appendChild(PSEl("span", { color: PS_TEXT_DIM, fontSize: "12px" }, "~")); const toInp = PSUIInput(r.to || "11:00", (v) => setField(r.id, "to", v), { type: "text", placeholder: "11:00" }); toInp.title = PST("timeRuleTo"); toInp.style.flex = "0 1 70px"; row1.appendChild(toInp); const tzInp = PSUIInput(r.tz || "", (v) => setField(r.id, "tz", v), { placeholder: "UTC+8 / local" }); tzInp.title = PST("timeRuleTz"); tzInp.style.minWidth = "86px"; tzInp.style.flex = "1 1 130px"; row1.appendChild(tzInp); sec.appendChild(row1); const row2 = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", marginBottom: "8px" }); const textInp = PSUIInput(r.text || "", (v) => setField(r.id, "text", v), { placeholder: PST("timeRuleTextPh") }); textInp.title = PST("timeRuleText"); row2.appendChild(textInp); const delBtn = PSSmallBtn("✕", () => delRule(r.id), { bg: "#5a2c3a" }); delBtn.title = PST("delScript"); row2.appendChild(delBtn); sec.appendChild(row2); }); const addBtn = PSSmallBtn(PST("timeRuleAdd"), () => { const next = rules.concat([{ id: PSUid("tr"), from: "06:00", to: "11:00", tz: "UTC+8", text: "" }]); commit(next); }); sec.appendChild(addBtn); box.appendChild(sec); } function PSUIConnectEditorRow(box, sc, node, port) { const id = port === "yes" ? node.yesId : (port === "no" ? node.noId : node.nextId); const branchSet = PSBranchNodeSet(sc.nodes); const idx = sc.nodes.indexOf(node); const implicitNext = (!id && !branchSet.has(node.id) && idx >= 0 && idx < sc.nodes.length - 1) ? sc.nodes[idx + 1] : null; const row = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const lab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); lab.textContent = port === "yes" ? PST("connectYes") : (port === "no" ? PST("connectNo") : PST("connectNextLabel")); row.appendChild(lab); if (id || implicitNext) { const t = id ? PSUINodeTargetLabel(sc, id) : (PSUITypeLabel(implicitNext.type) + "：" + (implicitNext.type === "judge" ? PSUIJudgePreview(implicitNext) : PSUINodePreview(implicitNext.text))); const info = PSEl("div", { flex: "1", minWidth: "0", fontSize: "12px", color: PS_ACCENT, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }); info.textContent = t || id; info.title = t || id; row.appendChild(info); if (id) { const disc = PSSmallBtn(PST("connectDisconnect"), () => { PSNodeDisconnect(sc.id, node.id, port); PSUIRenderAll(); }); row.appendChild(disc); } } else { const btn = PSSmallBtn(PST("connectBtn"), () => PSUIConnectWinOpen(sc.id, node.id, port)); row.appendChild(btn); row.appendChild(PSEl("span", { fontSize: "12px", color: PS_TEXT_DIM }, PSEsc(PST("connectHint")))); } box.appendChild(row); } function PSUIJudgeEditor(box, sc, node) { const sec = PSEl("div", { padding: "10px", borderRadius: "8px", background: "#1f1830", border: "1px solid #9f7fd0", marginBottom: "8px" }); sec.appendChild(PSEl("div", { fontWeight: "700", marginBottom: "8px", color: "#f0b3ff" }, PSEsc(PST("judgeEdit")))); const typeSel = PSUISelect(PSNormalizeJudgeType(node.judgeType), [ '<option value="coin">' + PSEsc(PST("judgeCoin")) + "</option>", '<option value="dice">' + PSEsc(PST("judgeDice")) + "</option>", '<option value="player">' + PSEsc(PST("judgePlayer")) + "</option>", '<option value="relation">' + PSEsc(PST("judgeRelation")) + "</option>", ].join(""), (v) => { PSJudgeSwitchType(sc.id, node.id, v); PSUIRenderAll(); }); sec.appendChild(PSUIEditorRow(PST("judgeType"), typeSel)); if (node.judgeType === "dice") { const exprInp = PSUIInput(node.diceExpr || "1d6", (v) => { PSUpdateNode(sc.id, node.id, { diceExpr: v }); PSUIRenderAll(); }, { placeholder: "1d6 / 1d100 / 2d100" }); sec.appendChild(PSUIEditorRow(PST("judgeDiceExpr"), exprInp)); } else if (node.judgeType === "player") { const playerHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "8px" }); playerHint.textContent = PST("judgePlayerHint"); sec.appendChild(playerHint); } else if (node.judgeType === "relation") { const relHint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "8px" }); relHint.textContent = PST("judgeRelationHint"); sec.appendChild(relHint); } else { const coinSel = PSUISelect(node.coinYesIs === "tails" ? "tails" : "heads", [ '<option value="heads">' + PSEsc(PST("judgeCoinHeads")) + "</option>", '<option value="tails">' + PSEsc(PST("judgeCoinTails")) + "</option>", ].join(""), (v) => { PSUpdateNode(sc.id, node.id, { coinYesIs: v }); PSUIRenderAll(); }); sec.appendChild(PSUIEditorRow(PST("judgeCoinYesIs"), coinSel)); } const showRow = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }); const showLab = PSEl("label", { width: "110px", minWidth: "110px", fontSize: "13px", color: PS_TEXT_DIM }); showLab.textContent = PST("judgeShowResult"); showRow.appendChild(showLab); const showCb = document.createElement("input"); showCb.type = "checkbox"; showCb.checked = node.showResult !== false; showCb.addEventListener("change", () => { PSUpdateNode(sc.id, node.id, { showResult: showCb.checked }); PSUIRenderAll(); }); showRow.appendChild(showCb); sec.appendChild(showRow); if (node.showResult !== false) { const lineSel = PSUISelect(node.judgeLineType || "chat", [ '<option value="chat">' + PSEsc(PST("typeChat")) + "</option>", '<option value="rp">' + PSEsc(PST("typeRp")) + "</option>", '<option value="narr">' + PSEsc(PST("typeNarr")) + "</option>", '<option value="action">' + PSEsc(PST("typeAction")) + "</option>", ].join(""), (v) => { PSUpdateNode(sc.id, node.id, { judgeLineType: v }); PSUIRenderAll(); }); sec.appendChild(PSUIEditorRow(PST("judgeLineType"), lineSel)); const ta = document.createElement("textarea"); ta.rows = 3; ta.value = node.text; PSStyle(ta, { width: "100%", boxSizing: "border-box", padding: "8px 10px", fontSize: "13px", borderRadius: "6px", background: "#10141f", color: PS_TEXT, border: "1px solid " + PS_BORDER, fontFamily: "sans-serif", resize: "vertical", marginBottom: "8px" }); const applyInput = () => { PSUpdateNode(sc.id, node.id, { text: ta.value }); const card = PSUI.flowCards.find((c) => c.id === node.id); if (card && card.previewEl) card.previewEl.textContent = PSUIJudgePreview(node) + (node.showResult !== false ? " · " + PSUINodePreview(ta.value) : ""); }; ta.addEventListener("input", applyInput); sec.appendChild(ta); PSUI.textareaEl = ta; const trow = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", flexWrap: "wrap", marginBottom: "8px" }); const ins = (token) => { const pos = (typeof ta.selectionStart === "number") ? ta.selectionStart : ta.value.length; const end = (typeof ta.selectionEnd === "number") ? ta.selectionEnd : pos; ta.value = ta.value.slice(0, pos) + token + ta.value.slice(end); applyInput(); try { ta.focus(); } catch (e) { } }; trow.appendChild(PSSmallBtn(PST("insertNick"), () => ins(PS_NICK_TOKEN))); trow.appendChild(PSSmallBtn(PST("insertTarget"), () => ins(PS_TARGET_TOKEN))); trow.appendChild(PSSmallBtn(PST("insertTime"), () => { ins(PS_TIME_TOKEN); PSUIRenderAll(); })); trow.appendChild(PSSmallBtn(PST("insertRoll"), () => { ins(PS_ROLL_TOKEN); PSUIRenderAll(); })); sec.appendChild(trow); } if (node.judgeType === "dice") { PSUIJudgeDiceBranches(sec, sc, node); } else if (node.judgeType === "player") { PSUIJudgePlayerBranches(sec, sc, node); } else if (node.judgeType === "relation") { PSUIJudgeRelationBranches(sec, sc, node); } else { PSUIConnectEditorRow(sec, sc, node, "yes"); PSUIConnectEditorRow(sec, sc, node, "no"); } box.appendChild(sec); } function PSUIJudgeDiceBranches(box, sc, node) { const sec = PSEl("div", { padding: "8px", borderRadius: "6px", background: "#141a2c", border: "1px solid " + PS_BORDER, marginBottom: "8px" }); sec.appendChild(PSEl("div", { fontWeight: "700", fontSize: "13px", color: "#f0b3ff", marginBottom: "6px" }, PSEsc(PST("judgeDiceBranches")))); const hint = PSEl("div", { fontSize: "12px", color: PS_TEXT_DIM, marginBottom: "8px" }); hint.textContent = PST("judgeDiceBranchesHint"); sec.appendChild(hint); const branches = PSNormalizeDiceBranches(node.diceBranches); const commit = (next) => { PSUpdateNode(sc.id, node.id, { diceBranches: next }); PSUIRenderAll(); }; const setField = (bid, key, val) => commit(branches.map((b) => (b.id === bid ? Object.assign({}, b, { [key]: val }) : b))); const del = (bid) => commit(branches.filter((b) => b.id !== bid)); branches.forEach((b, i) => { const row1 = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", marginBottom: "4px" }); const fromInp = PSUIInput(String(b.from), (v) => setField(b.id, "from", v), { type: "number", min: "1", step: "1" }); fromInp.style.flex = "0 1 70px"; row1.appendChild(fromInp); row1.appendChild(PSEl("span", { color: PS_TEXT_DIM, fontSize: "12px" }, "~")); const toInp = PSUIInput(String(b.to), (v) => setField(b.id, "to", v), { type: "number", min: "1", step: "1" }); toInp.style.flex = "0 1 70px"; row1.appendChild(toInp); const t = b.nodeId ? PSUINodeTargetLabel(sc, b.nodeId) : null; const info = PSEl("span", { flex: "1", minWidth: "0", fontSize: "12px", color: t ? PS_ACCENT : "#6a7290", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, PSEsc(t || PST("connectNone"))); row1.appendChild(info); if (b.nodeId) { const disc = PSSmallBtn(PST("connectDisconnect"), () => setField(b.id, "nodeId", null)); row1.appendChild(disc); } const delBtn = PSSmallBtn("✕", () => del(b.id), { bg: "#5a2c3a" }); row1.appendChild(delBtn); sec.appendChild(row1); }); if (branches.length < 5) { const addBtn = PSSmallBtn(PST("judgeBranchAdd"), () => { PSJudgeAddDiceBranch(sc.id, node.id); PSUIRenderAll(); }); sec.appendChild(addBtn); } else { sec.appendChild(PSEl("div", { fontSize: "12px", color: "#6a7290" }, PSEsc(PST("judgeBranchMax")))); } box.appendChild(sec); } function PSUIJudgePlayerBranches(box, sc, node) { const sec = PSEl("div", { padding: "8px", borderRadius: "6px", background: "#141a2c", border: "1px solid " + PS_BORDER, marginBottom: "8px" }); const branches = PSNormalizePlayerBranches(node.playerBranches); const commit = (next) => { PSUpdateNode(sc.id, node.id, { playerBranches: next }); PSUIRenderAll(); }; const setIds = (bid, val) => commit(branches.map((b) => (b.id === bid ? Object.assign({}, b, { ids: PSNormalizePlayerIds(String(val || "").split(/[、,，\s]+/).map((x) => Number(x))) }) : b))); const del = (bid) => commit(branches.filter((b) => b.id !== bid)); branches.forEach((b, i) => { const row = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", marginBottom: "6px" }); row.appendChild(PSEl("span", { width: "46px", minWidth: "46px", fontSize: "12px", color: "#f0b3ff" }, PSEsc(PST("judgePlayerBranchPh", i + 1)))); const idsInp = PSUIInput((b.ids || []).join("、"), (v) => setIds(b.id, v), { type: "text", placeholder: PST("judgePlayerIdsPh") }); idsInp.style.flex = "1 1 110px"; idsInp.style.minWidth = "80px"; row.appendChild(idsInp); const t = b.nodeId ? PSUINodeTargetLabel(sc, b.nodeId) : null; if (t) { const info = PSEl("span", { flex: "1", minWidth: "0", fontSize: "12px", color: PS_ACCENT, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, PSEsc(t)); info.title = t; row.appendChild(info); const disc = PSSmallBtn(PST("connectDisconnect"), () => { PSNodeDisconnect(sc.id, node.id, "player:" + b.id); PSUIRenderAll(); }); row.appendChild(disc); } else { const btn = PSSmallBtn(PST("connectBtn"), () => PSUIConnectWinOpen(sc.id, node.id, "player:" + b.id)); row.appendChild(btn); } const delBtn = PSSmallBtn("✕", () => del(b.id), { bg: "#5a2c3a" }); row.appendChild(delBtn); sec.appendChild(row); }); if (branches.length < 5) { const addBtn = PSSmallBtn(PST("judgeBranchAdd"), () => { PSJudgeAddPlayerBranch(sc.id, node.id); PSUIRenderAll(); }); sec.appendChild(addBtn); } else { sec.appendChild(PSEl("div", { fontSize: "12px", color: "#6a7290" }, PSEsc(PST("judgeBranchMax")))); } const elseRow = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", marginBottom: "6px" }); elseRow.appendChild(PSEl("span", { width: "46px", minWidth: "46px", fontSize: "12px", color: "#f0b3ff" }, PSEsc(PST("judgePlayerElse")))); const t = node.elseId ? PSUINodeTargetLabel(sc, node.elseId) : null; if (t) { const info = PSEl("span", { flex: "1", minWidth: "0", fontSize: "12px", color: PS_ACCENT, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, PSEsc(t)); info.title = t; elseRow.appendChild(info); const disc = PSSmallBtn(PST("connectDisconnect"), () => { PSNodeDisconnect(sc.id, node.id, "else"); PSUIRenderAll(); }); elseRow.appendChild(disc); } else { const btn = PSSmallBtn(PST("connectBtn"), () => PSUIConnectWinOpen(sc.id, node.id, "else")); elseRow.appendChild(btn); elseRow.appendChild(PSEl("span", { fontSize: "12px", color: PS_TEXT_DIM }, PSEsc(PST("connectHint")))); } sec.appendChild(elseRow); box.appendChild(sec); } function PSUIJudgeRelationBranches(box, sc, node) { const sec = PSEl("div", { padding: "8px", borderRadius: "6px", background: "#141a2c", border: "1px solid " + PS_BORDER, marginBottom: "8px" }); const branches = PSNormalizeRelationBranches(node.relationBranches); const commit = (next) => { PSUpdateNode(sc.id, node.id, { relationBranches: next }); PSUIRenderAll(); }; const setRel = (bid, rel) => commit(branches.map((b) => (b.id === bid ? Object.assign({}, b, { rel }) : b))); const del = (bid) => commit(branches.filter((b) => b.id !== bid)); branches.forEach((b) => { const row = PSEl("div", { display: "flex", gap: "6px", alignItems: "center", marginBottom: "6px" }); const relSel = PSUISelect(b.rel, PS_RELATION_ORDER.map((r) => '<option value="' + r + '">' + PSEsc(PSRelationLabel(r)) + "</option>").join(""), (v) => setRel(b.id, v)); relSel.style.flex = "1 1 110px"; relSel.style.minWidth = "84px"; row.appendChild(relSel); const t = b.nodeId ? PSUINodeTargetLabel(sc, b.nodeId) : null; if (t) { const info = PSEl("span", { flex: "1", minWidth: "0", fontSize: "12px", color: PS_ACCENT, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, PSEsc(t)); info.title = t; row.appendChild(info); const disc = PSSmallBtn(PST("connectDisconnect"), () => { PSNodeDisconnect(sc.id, node.id, "relation:" + b.id); PSUIRenderAll(); }); row.appendChild(disc); } else { const btn = PSSmallBtn(PST("connectBtn"), () => PSUIConnectWinOpen(sc.id, node.id, "relation:" + b.id)); row.appendChild(btn); } const delBtn = PSSmallBtn("✕", () => del(b.id), { bg: "#5a2c3a" }); row.appendChild(delBtn); sec.appendChild(row); }); if (branches.length < 5) { const addBtn = PSSmallBtn(PST("judgeBranchAdd"), () => { PSJudgeAddRelationBranch(sc.id, node.id); PSUIRenderAll(); }); sec.appendChild(addBtn); } else { sec.appendChild(PSEl("div", { fontSize: "12px", color: "#6a7290" }, PSEsc(PST("judgeBranchMax")))); } box.appendChild(sec); } function PSUIRenderStatus() { if (!PSUI.statusEl) return; let html; if (PSActive) { const A = PSActive; const remain = Math.max(0, A.nextAt - PSNow()); html = PST("statusRunning", PSEsc(A.name), A.idx + 1, A.nodes.length, (remain / 1000).toFixed(1)); } else if (PSQueue.length) { html = PST("statusQueued", PSQueue.length); } else { html = PST("statusIdle"); } PSUI.statusEl.innerHTML = html; } function PSUIImportBuild(root) { const overlay = PSEl("div", { position: "absolute", left: "0", top: "0", right: "0", bottom: "0", background: "rgba(10,12,20,.92)", zIndex: "5", display: "none", padding: "24px", overflowY: "auto", }); const panel = PSEl("div", { maxWidth: "560px", margin: "0 auto", padding: "16px", borderRadius: "10px", background: PS_PANEL, border: "1px solid #ffffff", }); panel.appendChild(PSEl("div", { fontWeight: "700", fontSize: "16px", marginBottom: "8px", color: PS_ACCENT }, PSEsc(PST("importTitle")))); panel.appendChild(PSEl("div", { color: PS_TEXT_DIM, fontSize: "13px", marginBottom: "10px" }, PSEsc(PST("importHint")))); const ta = document.createElement("textarea"); ta.rows = 8; PSStyle(ta, { width: "100%", boxSizing: "border-box", padding: "10px", fontSize: "12px", borderRadius: "6px", background: "#10141f", color: PS_TEXT, border: "1px solid " + PS_BORDER, fontFamily: "monospace", resize: "vertical", marginBottom: "10px", }); panel.appendChild(ta); const fileInp = document.createElement("input"); fileInp.type = "file"; fileInp.accept = ".txt,.json"; fileInp.style.marginBottom = "10px"; fileInp.addEventListener("change", () => { const f = fileInp.files && fileInp.files[0]; if (!f) return; try { if (typeof FileReader === "function") { const fr = new FileReader(); fr.onload = () => { ta.value = String(fr.result || ""); PSUIImportApply(ta.value); }; fr.readAsText(f); } else { PSUI.importFileEl = f; } } catch (e) { PSErr("读取文件失败", e); } }); panel.appendChild(fileInp); const btnRow = PSEl("div", { display: "flex", gap: "8px" }); const goBtn = PSBigBtn(PST("importFromText"), () => PSUIImportApply(ta.value), { bg: "#2c7a70" }); const closeBtn = PSBigBtn(PST("importClose"), PSUIImportToggle); btnRow.appendChild(goBtn); btnRow.appendChild(closeBtn); panel.appendChild(btnRow); overlay.appendChild(panel); root.appendChild(overlay); PSUI.importEl = overlay; PSUI.importTextEl = ta; PSUI.importFileEl = fileInp; } function PSUIImportApply(text) { if (!String(text || "").trim()) { PSToast(PST("toastImportBad")); return; } const r = PSImportJSON(text); if (!r) { PSToast(PST("toastImportBad")); return; } PSToast(PST("toastImported", r.added, r.replaced)); PSUIImportToggle(); PSUIRenderAll(); } function PSUIImportToggle() { if (!PSUI.importEl) return; const show = PSUI.importEl.style.display !== "flex"; PSUI.importEl.style.display = show ? "flex" : "none"; } function PSUIExportDeliver(text, fileName) { let copied = false; try { if (typeof navigator !== "undefined" && navigator.clipboard && typeof navigator.clipboard.writeText === "function") { navigator.clipboard.writeText(text); copied = true; } } catch (e) { } if (!copied) { try { const ta = document.createElement("textarea"); ta.value = text; ta.style.position = "fixed"; ta.style.opacity = "0"; document.body.appendChild(ta); ta.select && ta.select(); if (document.execCommand) document.execCommand("copy"); ta.remove(); copied = true; } catch (e) { } } try { const blob = new Blob([text], { type: "application/json" }); const a = document.createElement("a"); a.href = URL.createObjectURL(blob); a.download = fileName; document.body.appendChild(a); a.click(); a.remove(); } catch (e) { } PSToast(copied ? PST("toastExported") : PST("toastExportFail")); } function PSUIExport() { let text; try { text = PSExportJSON(); } catch (e) { PSErr("导出失败", e); PSToast(PST("toastExportFail")); return; } PSUIExportDeliver(text, "PlayScript-backup-" + new Date().toISOString().slice(0, 10) + ".txt"); } function PSUIExportScript(id) { const sc = PSFindScript(id); if (!sc) { PSToast(PST("toastNoSelScript")); return; } let text; try { text = PSExportScriptJSON(id); } catch (e) { PSErr("导出失败", e); PSToast(PST("toastExportFail")); return; } if (!text) { PSToast(PST("toastNoSelScript")); return; } let safeName = ""; const BAD_NAME_CHARS = '\\/:*?"<>| \t\n\r'; const rawName = String(sc.name || "script").slice(0, 40); for (let i = 0; i < rawName.length; i++) { const c = rawName[i]; safeName += (BAD_NAME_CHARS.indexOf(c) >= 0) ? "_" : c; } while (safeName.charAt(0) === "_") safeName = safeName.slice(1); while (safeName.charAt(safeName.length - 1) === "_") safeName = safeName.slice(0, -1); if (!safeName) safeName = "script"; PSUIExportDeliver(text, "PlayScript-" + safeName + "-" + new Date().toISOString().slice(0, 10) + ".txt"); } function PSUIConnectWinBuild() { if (PSUI.connectWin || typeof document === "undefined" || !document.body) return; const win = document.createElement("div"); win.id = "ps-connectwin"; PSStyle(win, { position: "fixed", left: "50%", top: "50%", transform: "translate(-50%, -50%)", width: "360px", maxHeight: "70vh", zIndex: "2147483560", background: PS_BG, border: "2px solid #ffffff", borderRadius: "10px", boxShadow: "0 8px 40px rgba(0,0,0,.7)", display: "none", color: PS_TEXT, fontFamily: "sans-serif", fontSize: "14px", overflow: "hidden", display2: "none", }); const title = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "8px 12px", background: "#141826", borderBottom: "1px solid " + PS_BORDER }); title.appendChild(PSEl("span", { color: "#f0b3ff", fontSize: "16px" }, "↳")); PSUI.connectTitleEl = PSEl("span", { fontWeight: "700", fontSize: "14px", flex: "1" }); title.appendChild(PSUI.connectTitleEl); const closeBtn = PSSmallBtn("✕", PSUIConnectWinClose, { title: PST("closeTitle") }); title.appendChild(closeBtn); win.appendChild(title); PSUI.connectListEl = PSEl("div", { overflowY: "auto", maxHeight: "calc(70vh - 44px)", padding: "8px" }); win.appendChild(PSUI.connectListEl); document.body.appendChild(win); PSUI.connectWin = win; } function PSUIConnectWinOpen(scriptId, nodeId, port) { PSUIConnectWinBuild(); if (!PSUI.connectWin) return; const sc = PSFindScript(scriptId); const node = sc ? sc.nodes.find((n) => n.id === nodeId) : null; if (!sc || !node) return; PSUI.connectScriptId = scriptId; PSUI.connectNodeId = nodeId; PSUI.connectPort = port; PSUI.connectWin.style.display = "block"; PSUIConnectWinRender(); } function PSUIConnectWinClose() { if (PSUI.connectWin) PSUI.connectWin.style.display = "none"; } function PSUIConnectWinRender() { const box = PSUI.connectListEl; if (!box) return; box.innerHTML = ""; const sc = PSFindScript(PSUI.connectScriptId); if (!sc) return; const port = PSUI.connectPort; let portLabel = PST("connectNext"); if (port === "yes") portLabel = PST("connectYes"); else if (port === "no") portLabel = PST("connectNo"); else if (port === "else") portLabel = PST("judgePlayerElse"); else if (typeof port === "string" && port.indexOf("player:") === 0) { const bid = port.slice("player:".length); const node = sc.nodes.find((n) => n.id === PSUI.connectNodeId); const idx = node && Array.isArray(node.playerBranches) ? node.playerBranches.findIndex((b) => b.id === bid) : -1; portLabel = PST("judgePlayerBranchPh", idx >= 0 ? idx + 1 : 1); } else if (typeof port === "string" && port.indexOf("relation:") === 0) { const bid = port.slice("relation:".length); const node = sc.nodes.find((n) => n.id === PSUI.connectNodeId); const br = node && Array.isArray(node.relationBranches) ? node.relationBranches.find((b) => b.id === bid) : null; portLabel = br ? PSRelationLabel(br.rel) : PST("judgeRelation"); } if (PSUI.connectTitleEl) PSUI.connectTitleEl.textContent = PST("connectWinTitle", portLabel); const others = sc.nodes.filter((n) => n.id !== PSUI.connectNodeId); if (!others.length) { box.appendChild(PSEl("div", { color: PS_TEXT_DIM, padding: "10px", textAlign: "center" }, PSEsc(PST("connectNone")))); return; } others.forEach((n) => { const row = PSEl("div", { display: "flex", alignItems: "center", gap: "8px", padding: "8px", marginBottom: "6px", borderRadius: "8px", background: "#20263a", border: "1px solid " + PSUINodeColor(n.type), cursor: "pointer", }); row.appendChild(PSEl("span", { width: "18px", fontSize: "14px", color: PSUINodeColor(n.type) }, n.type === "judge" ? "🎲" : "▶")); const mid = PSEl("div", { flex: "1", minWidth: "0" }); mid.appendChild(PSEl("div", { fontSize: "11px", color: PSUINodeColor(n.type), fontWeight: "700" }, PSEsc(PSUITypeLabel(n.type)))); const pv = n.type === "judge" ? PSUIJudgePreview(n) : PSUINodePreview(n.text); mid.appendChild(PSEl("div", { fontSize: "12px", color: PS_TEXT, whiteSpace: "pre-wrap", overflow: "hidden", maxHeight: "30px", wordBreak: "break-word" }, PSEsc(pv))); row.appendChild(mid); row.addEventListener("click", () => { const r = PSNodeConnect(PSUI.connectScriptId, PSUI.connectNodeId, PSUI.connectPort, n.id); if (r.ok) { PSUIConnectWinClose(); PSUIRenderAll(); } }); box.appendChild(row); }); } function PSUIRenderAll() { if (!PSUI.el) PSUIRoot(); if (!PSUI.el) return; PSUIListBuild(); PSUIFlowBuild(); PSUIEditorBuild(); PSUIRenderStatus(); if (PSUI.langBtnEl) PSUI.langBtnEl.textContent = PST("langBtn"); if (PSUI.titleText) PSUI.titleText.textContent = PST("title"); if (PSUI.btnNew) PSUI.btnNew.textContent = PST("newScript"); if (PSUI.btnTest) PSUI.btnTest.textContent = PST("testRun"); if (PSUI.btnStop) PSUI.btnStop.textContent = PST("stop"); if (PSUI.btnExport) PSUI.btnExport.textContent = PST("exportBtn"); if (PSUI.btnImport) PSUI.btnImport.textContent = PST("importBtn"); if (PSUI.btnCloud) PSUI.btnCloud.textContent = PST("cloudCheckBtn"); if (PSUI.cloudCapInp && PSStore.state) PSUI.cloudCapInp.value = String(PSStore.cloudCapKB()); if (PSUI.btnClear && !PSUI.armed["clearall"]) PSUI.btnClear.textContent = PST("clearAll"); if (PSUI.focusRequest && PSUI.textareaEl) { PSUI.focusRequest = false; try { PSUI.textareaEl.focus(); } catch (e) { } } } function PSUILangToggle() { PSUI.lang = PSUI.lang === "zh" ? "en" : "zh"; PSStorage.set("PlayScriptLang", PSUI.lang); PSUIRenderAll(); } function PlayScriptOpen() { if (PSUI.open) return; if (typeof document === "undefined" || !document.body) return; PSUI.open = true; const el = PSUIRoot(); if (!el) return; PSUIDotBuild(); PSUI.win.minimized = false; PSUIWinClamp(); PSStyle(el, { left: PSUI.win.x + "px", top: PSUI.win.y + "px", width: PSUI.win.w + "px", height: PSUI.win.h + "px" }); el.style.display = "block"; PSUI.bodyEl.style.display = "flex"; PSUI.toolbarEl.style.display = "flex"; PSUI.statusEl.style.display = "block"; PSUI.cornerEl.style.display = "block"; PSUIRenderAll(); if (!PSUI.tickId) { PSUI.tickId = setInterval(() => { if (PSActive) PSUIRenderStatus(); }, 250); } } function PlayScriptClose() { if (!PSUI.open) return; PSUI.open = false; if (PSUI.tickId) { clearInterval(PSUI.tickId); PSUI.tickId = null; } if (PSUI.el) PSUI.el.style.display = "none"; } function PlayScriptToggle() { if (PSUI.open) PlayScriptClose(); else PlayScriptOpen(); } function PSUINewScript() { if (!PSStore.state) return; const s = PSAddScript("", ""); if (s) { PSUI.selScriptId = s.id; PSUI.selNodeId = null; PSUIRenderAll(); } } function PSUITestRun() { const sc = PSFindScript(PSUI.selScriptId); if (!sc) { PSToast(PST("toastNoSelScript")); return; } if (!sc.keyword) PSToast(PST("toastKwEmpty")); const r = PSFire(sc.id, { manual: true }); if (r && r.ok) PSUIRenderStatus(); } function PSExposeAPI() { const g = (typeof globalThis !== "undefined") ? globalThis : (typeof window !== "undefined" ? window : null); if (!g) return; const api = { Open: PlayScriptOpen, Close: PlayScriptClose, Toggle: PlayScriptToggle, State: () => ({ open: PSUI.open, running: !!PSActive, active: PSActive ? { scriptId: PSActive.scriptId, name: PSActive.name, idx: PSActive.idx, total: PSActive.nodes.length, nextAt: PSActive.nextAt, } : null, queue: PSQueue.slice(), scripts: (PSStore.state ? PSStore.state.scripts.length : 0), }), AddScript: (name, keyword) => PSAddScript(name, keyword), DeleteScript: (id) => PSDeleteScript(id), DuplicateScript: (id) => PSDuplicateScript(id), RenameScript: (id, name) => PSUpdateScript(id, { name }), UpdateScript: (id, patch) => PSUpdateScript(id, patch), AddNode: (scriptId, node, atIdx) => PSAddNode(scriptId, node, atIdx), UpdateNode: (scriptId, nodeId, patch) => PSUpdateNode(scriptId, nodeId, patch), DeleteNode: (scriptId, nodeId) => PSDeleteNode(scriptId, nodeId), MoveNode: (scriptId, nodeId, dir) => PSMoveNode(scriptId, nodeId, dir), Fire: (scriptId, opts) => PSFire(scriptId, opts || { manual: true }), Stop: PSStop, IsRunning: () => !!PSActive, Version: () => PS_VERSION, LastOutfitBlocked: () => PSLastOutfitBlocked.slice(), LastOutfitCleared: () => PSLastOutfitCleared.slice(), StorageInfo: PSStorageInfo, CleanLSCGBackups: PSCleanLSCGBackups, CloudInfo: PSCloudInfo, CloudLimit: PSCloudLimit, DebugOutfit: (code) => { const bundle = PSDecodeOutfitCode(PSNormalizeCode(code)); if (!bundle) return { version: PS_VERSION, error: "decode-failed" }; const family = (typeof Player !== "undefined" && Player && typeof Player.AssetFamily === "string" && Player.AssetFamily) ? Player.AssetFamily : "Female3DCG"; const flags = PSOutfitFlagsFromNode({}); const items = bundle.map((b) => { const cls = PSOutfitClassOf(b, family); return { Group: b.Group, Name: b.Name, cls, checkedByDefault: PSOutfitClassAllowed(cls, flags), assetFound: (typeof AssetGet === "function") ? !!(AssetGet(family, b.Group, b.Name)) : null, }; }); return { version: PS_VERSION, family, items }; }, Export: PSExportJSON, ExportScript: PSExportScriptJSON, Import: PSImportJSON, Clear: PSClearAll, }; g.PlayScript = api; g.PlayScriptOpen = PlayScriptOpen; g.PlayScriptClose = PlayScriptClose; g.PlayScriptToggle = PlayScriptToggle; } if (typeof window !== "undefined" && typeof window.document !== "undefined") { PSMain(); } if (typeof module !== "undefined" && module.exports) { module.exports = { PSStore, PSStorage, PSNow, PSText, PST, PSMatchText, PSMatchInfo, PSTriggerTargetOf, PSTriggerTargetAttempt, PSScriptUsesTarget, PSAddScript, PSDeleteScript, PSDuplicateScript, PSUpdateScript, PSAddNode, PSDeleteNode, PSMoveNode, PSMoveNodeTo, PSUpdateNode, PSExportJSON, PSExportScriptJSON, PSImportJSON, PSClearAll, PSNormalizeText, PSNodeTextCap, PSNormalizeCode, PSSendChat, PSSendRp, PSSendNarr, PSSendAction, PSSendNode, PSSendLeave, PSSendOutfit, PSDecodeOutfitCode, PSApplyOutfit, PSOutfitClassOf, PSOutfitFlagsFromNode, PSOutfitClassAllowed, PSOutfitPrepareBundle, PSSendEmoteDirect, PSEmoteBlocked, PSChatReplyId, PS_NICK_TOKEN, PSCharDisplayName, PSCharObjectOf, PSTriggerName, PSApplyTokens, PS_TARGET_TOKEN, PSCharIsSelf, PSTargetInRoom, PS_TIME_TOKEN, PSTimeHMToMin, PSTimeRuleMinutes, PSTimeRuleMatch, PSTimeRuleText, PSNormalizeTimeRules, PSNormalizeTimeTriggerRules, PSTimeTriggerScan, PS_ROLL_TOKEN, PSJudgeRoll, PSNormalizeDiceBranches, PSJudgeAddDiceBranch, PSJudgeBranchSubtree, PSJudgeSwitchType, PSNodeEdges, PSNodeConnectionWouldCycle, PSBranchNodeSet, PSNodeConnect, PSNodeDisconnect, PSAddNodeAfter, PSValidDiceExpr, PSNormalizeJudgeType, PSNormalizePlayerIds, PSNormalizePlayerBranches, PSJudgeAddPlayerBranch, PSPortTarget, PS_RELATION_ORDER, PSRelationLabel, PSNormalizeRelationBranches, PSJudgeAddRelationBranch, PSRelationOf, PS_ACT_ALIASES, PSFire, PSRun, PSFinish, PSAbortCore, PSStop, PSTriggerFromText, PSCanSend, PSInstallHooks, PSInputClear, PSHookWhen, PS_ACTION_GROUPS, PSActGroupLabel, PSActEntriesFor, PSInstallActivityButtonHook, PSActZones, PSActCanvasMap, PSActZoneHitTest, PSUIActPreviewDraw, PSUIActWinOpen, PSUIActWinClose, PSUIActWinSelect, PSActBaseGroup, PSInteractNormalizeActions, PSInteractGroups, PSInteractGroupCount, PSInteractHas, PSInteractActivityNames, PSInteractActivityLabel, PSInteractWinOpen, PSInteractWinClose, PSInteractWinRender, PSInteractActRender, PSActZonesAll, PSActZoneHitTestAll, PSPreviewDrawChar, PSPreviewDrawZones, PSUIOutfitWinOpen, PSUIOutfitWinClose, PSUIOutfitToggle, PSUIOutfitAll, PSUIOutfitNone, PSUIOutfitWinRender, PSUI, PSUIRenderAll, PSUIRenderStatus, PSUIListBuild, PSUIFlowBuild, PSUIEditorBuild, PSUIDotBuild, PSToast, PSUINodePreview, PSUIWinClamp, PSUIExport, PSUIExportScript, PSUITimeRuleSection, PSUIJudgePreview, PSUIConnectWinOpen, PSUIConnectWinClose, PlayScriptOpen, PlayScriptClose, PlayScriptToggle, PSVersion: () => PS_VERSION, PSLastOutfitBlocked: () => PSLastOutfitBlocked.slice(), PSLastOutfitCleared: () => PSLastOutfitCleared.slice(), PSStorageInfo, PSCleanLSCGBackups, PSCloudInfo, PSCloudLimit, PSCloudSelfBytes, PSObfuscate, PSDeobfuscate, PSBioPack, PSBioUnpack, PSBioBlockBounds, PSUTF8Bytes, PSMeasureDataSize, PSByteToKB, PSDebugOutfit: (code) => { const bundle = PSDecodeOutfitCode(PSNormalizeCode(code)); if (!bundle) return { version: PS_VERSION, error: "decode-failed" }; const family = (typeof Player !== "undefined" && Player && typeof Player.AssetFamily === "string" && Player.AssetFamily) ? Player.AssetFamily : "Female3DCG"; return { version: PS_VERSION, family, items: bundle.map((b) => { const cls = PSOutfitClassOf(b, family); return { Group: b.Group, Name: b.Name, cls, assetFound: (typeof AssetGet === "function") ? !!(AssetGet(family, b.Group, b.Name)) : null }; }), }; }, PSActive: () => PSActive, PSQueueOf: () => PSQueue.slice(), PSLastFireOf: () => PSLastFire, }; } })();